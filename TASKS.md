# Task Tracker

Granular sub-tasks per milestone. `README.md` tracks milestone-level status;
this file is the working log underneath it — update a task's status (and
add a date/note) as it moves, rather than waiting until the whole milestone
is done. Full rationale for each item lives in `DESIGN.md`.

Status values: `todo` (not started, actionable anytime) / `in-progress` /
`done` / `blocked` (cannot be advanced by the coding agent regardless of
effort — needs the user, e.g. an external account signup).

## M0 — Project scaffolding

| Task | Status | Date / Notes |
|---|---|---|
| Repo layout per DESIGN.md §7 | done | 2026-07-21 — clarified DESIGN.md §7 during review: the tree is the `munger` repo root itself, no nested package directory |
| `config.py` — every threshold/toggle as named constants | done | 2026-07-21 — reviewed by python-reviewer + staff-engineer-reviewer; runtime paths anchored to `BASE_DIR` per review finding |
| `requirements.txt` | done | 2026-07-21 — pinned with `~=` per python-reviewer finding (was unpinned) |
| `tests/` directory | done | 2026-07-21 — `tests/test_config.py`, 7 passing tests |
| `.gitignore` for `state.json`, `journal.db`, `screen_results*.csv`, `KILL_SWITCH` flag file | done | 2026-07-21 |
| Alpaca paper-trading account + API key provisioning | done | 2026-07-23 — user created the paper account and provided the keys. **Handling note**: the keys were pasted directly into chat rather than added straight to a secrets store; the coding agent flagged this immediately, refused to embed them in any Bash command's literal arguments (blocked once by the environment's own credential-leakage guard, which was correct to do so), and instead wrote them to the repo's existing gitignored `.env` (which already held a stale, unrelated `FMP_API_KEY` from the reverted M1 hybrid-sourcing attempt — replaced, not appended) before sourcing that file into the shell for verification calls. `ALPACA_API_KEY`/`ALPACA_SECRET_KEY` confirmed working and reporting `paper` mode — the `verify_account_access()` *function* was called live and succeeded, though only via a standalone script against `ExecutionModule` directly, not through `bot.run()`'s own startup call site (see M10's row for that residual gap). **Still outstanding**: these same two values need to be added as GitHub repo secrets (Settings → Secrets and variables → Actions) for `quarterly-run.yml`/`heartbeat.yml` to actually use them — not done yet, tracked in M11. |
| **New (pm-reviewer, 2026-07-23): rotate the Alpaca key pasted into chat** | todo | the key/secret above passed through this conversation's transcript when the user pasted it directly rather than adding it straight to a secrets store — recommended at the time but not yet a tracked action item until this row. Low real-world stakes (paper-trading keys, no access to real money), but cheap to do: regenerate the key pair in Alpaca's dashboard, update the local `.env` and (once added) the GitHub repo secrets to match. |

## M1 — Universe module

Broadened mid-milestone from S&P 500-only to the full S&P Composite 1500
(S&P 500 + S&P 400 MidCap + S&P 600 SmallCap) per user decision 2026-07-21
— see DESIGN.md §3.1.

**Sourcing history (all resolved 2026-07-21):** three alternatives to a
pure Wikipedia scrape were evaluated for the S&P 500 leg and rejected,
in order: (1) ETF-issuer holdings files (SLY 404s, SPY's Sector column
blank); (2) Finnhub (no S&P 400/600 constituents endpoint at any price);
(3) Financial Modeling Prep's `sp500_constituent` REST API — initially
adopted as a hybrid (FMP for 500, Wikipedia for 400/600, since the user
acts on this system's output with real money and API sourcing looked
like the more reliable option for the largest slice), fully implemented
and reviewed, but then found **live** to require a paid plan: the
legacy endpoint (`/api/v3/`) was sunset 2025-08-31 (`HTTP 403`), and the
current endpoint (`/stable/sp500-constituent`) returned `HTTP 402`
("Restricted Endpoint... upgrade your plan") even with a real, valid key.
User declined to pay for FMP's Starter plan (~$19/mo) — **reverted to a
uniform Wikipedia scrape for all three indices**, the same hardening
(per-index validate-and-fallback, dedup, alert-severity tiering) applied
throughout, just without a second source technology to reconcile.

**Fallback is per-index, not all-or-nothing** (DESIGN.md §3.1): each of
the three indices is fetched/validated/falls-back independently, so the
static fallback file needs a source-index column to slice from, and each
per-index fallback gets its own alert (DESIGN.md §3.6) rather than
relying on the aggregate ticker count, which would still look healthy.
Combined results are de-duplicated by ticker (500 → 400 → 600 precedence
on conflict) to handle the rare index-reclassification window where a
ticker briefly appears in two source lists at once — confirmed live: a
real ticker (BTSG) currently appears on both the S&P 400 and 600
Wikipedia pages.

**Fully shipped 2026-07-21**, reviewed by `code-reviewer` (substituting
for `python-reviewer`, which turned out to have the same workspace-root
agent-discovery scoping issue as the hooks — copied to
`/Users/jimmyok/workspace/.claude/agents/` for next session),
`staff-engineer-reviewer`, and `pm-reviewer`, across both the original
hybrid-sourcing pass and the FMP-to-Wikipedia revert. All findings fixed
before merge (see per-row notes below); every task is unblocked and
verified live — no external dependency left to provision.

| Task | Status | Date / Notes |
|---|---|---|
| `get_universe()`: fetch, validate, exclude, normalize, combine, and de-duplicate all three indices via Wikipedia | done | 2026-07-21 — required a custom User-Agent header (default urllib UA gets HTTP 403 from Wikipedia's bot policy); found live, not theoretically. All three indices share one `_fetch_wikipedia_index(index)` function; the FMP-specific `_fetch_fmp_sp500()` path built earlier was removed on revert (see sourcing history above), returning the module to a single source technology. |
| Sector-name handling | done | 2026-07-21 — `_canonicalize_sector()` trims whitespace and tolerates non-string input (`float('nan')`) without raising, after code-reviewer found a blank/NaN sector cell would otherwise discard an entire index's live fetch over one bad row. The FMP-vs-Wikipedia alias-mapping table built during the hybrid-sourcing phase was removed on revert — unverified guesses about a vendor's field format that's no longer in use would have been dead, misleading code. |
| Live full-composite validation run (all three sources, real network) | done | 2026-07-21 — live run returns 1505 tickers (1506 combined − 1 real cross-index duplicate, BTSG), all three indices fetched live with zero fallback triggers; sector exclusion verified end-to-end (excluding Financials drops JPM, keeps AAPL, across the combined universe: 1246 tickers with the exclusion applied). |
| `config.py`: per-index validation bands, fallback file path/schema | done | 2026-07-21 — `UNIVERSE_TICKER_COUNT_BANDS` dict keyed by index ("500"/"400"/"600"). `FMP_API_KEY` was added during the hybrid-sourcing phase and removed on revert. |
| Static fallback ticker file: `data/universe_fallback.csv`, full composite with source-index column | done | 2026-07-21 — 1506 rows (503 + 400 + 603) generated from live Wikipedia scrapes of all three index pages, `source_index` column added. Confirmed live: exactly one cross-index duplicate exists in the real data today (BTSG, in both 400 and 600) — the dedup logic this milestone added is not theoretical. |
| Ticker normalization (`BRK.B` → `BRK-B`) | done | 2026-07-21 |
| Optional sector-exclusion list | done | 2026-07-21 — verified live: excluding Financials correctly drops JPM and keeps AAPL across the full combined S&P 1500 universe. `_load_static_fallback()` calls the same `_apply_sector_exclusions()` the live path uses (was a second, independently-drifting inline implementation before code-reviewer flagged it as untested), with a test proving the fallback path excludes JPM too. |
| Scrape validation (row-count/format sanity check) + fallback on validation failure | done | 2026-07-21 — validates the *raw* result before sector exclusion, not after (validating post-exclusion was a real bug found live in the single-index version: any configured exclusion would make the count fall outside the band and force fallback every run). Per-index bands and per-index fallback slicing implemented and tested (`test_validate_universe_uses_per_index_band` is a direct regression test for this). |
| Widen exception handling to cover the whole pipeline (fetch → validate → exclude → normalize), not just the fetch call | done | 2026-07-21 — found by staff-engineer-reviewer in the single-index version: a missing/renamed GICS Sector column would raise uncaught inside `_apply_sector_exclusions`, crashing instead of falling back. Covered by `test_get_universe_falls_back_on_missing_sector_column`. |
| Fallback-of-the-fallback isolation | done | 2026-07-21 — the fallback-load call was originally reachable from both the validation-failure branch *and* the exception handler, so a broken fallback file would be silently retried and its failure misattributed to "the live fetch failed twice." Split into `_fetch_and_validate_index()` (returns `None` on any live-fetch problem) and `_fetch_index()` (calls the fallback only once, outside that function's exception boundary), so a genuinely broken fallback file now fails loudly and distinctly instead. |
| Unit tests: `_apply_sector_exclusions` (pure function) | done | 2026-07-21 — in-memory DataFrame fixtures, including a whitespace-trimming case |
| Unit tests: `get_universe()` branches via monkeypatching the fetch call | done | 2026-07-21 — covers: an index raises → that index falls back while the others stay live; an index fails validation → falls back; missing sector column → falls back (regression test); all three succeed live |
| Unit tests: cross-index de-duplication | done | 2026-07-21 — a ticker shared between two fixture lists collapses to one entry in the combined result |
| `requirements.txt`: pin `lxml` | done | 2026-07-21 — critical finding, independently raised by both code-reviewer and staff-engineer-reviewer: `pd.read_html()` requires `lxml` or `html5lib`+`beautifulsoup4` as an optional pandas dependency, and neither was declared anywhere in `requirements.txt`. On a clean install this would make every Wikipedia fetch silently and permanently fall back to the static file, with only a generic "fetch failed" warning masking the real cause. |

## M2 — Data fetcher

**Environment note:** this machine's default `python3` is 3.9.6, but
`yfinance`'s pinned dependency `curl_cffi>=0.15` has no wheels for 3.9 and
DESIGN.md §7 specifies Python 3.11+ anyway. Installed Python 3.11 via
Homebrew (`/opt/homebrew/bin/python3.11`) and created a project venv at
`munger/.venv` (already gitignored from M0) — `source .venv/bin/activate`
before running `pytest`/`ruff`/`mypy` in this repo from now on.

| Task | Status | Date / Notes |
|---|---|---|
| `fetch_metrics(symbol)` via yfinance | done | 2026-07-21 — returns `Metrics \| None`: `None` only if every retry fails outright (bad/delisted symbol, network error); a successful fetch with individual fields missing returns a `Metrics` record with those fields `None`, not a failure — that distinction is what M3's `data_missing:<field>` tagging needs downstream. |
| Thread pool (~10–15 workers) | done | 2026-07-21 — `fetch_all_metrics()`, `concurrent.futures.ThreadPoolExecutor(max_workers=config.DATA_FETCH_THREAD_POOL_WORKERS)` |
| Retry-with-backoff on transient/rate-limit errors | done | 2026-07-21 — exponential backoff with jitter (`config.DATA_FETCH_RETRY_BACKOFF_SECONDS * 2**attempt`, plus up to 25% jitter), up to `config.DATA_FETCH_MAX_RETRIES`. Changed from a flat fixed delay per staff-engineer-reviewer: with 12 concurrent workers all hitting a rate limit around the same time, a fixed delay would retry them all in near-lockstep, recreating the burst that caused the 429. Verified live against a real invalid ticker (3 attempts, then `None`) — that exercises the permanent-failure path, not an actual transient/rate-limit recovery, which can't be reliably forced against a live third-party service; recovery is covered by a mocked test (`test_fetch_metrics_retries_then_succeeds`) instead. |
| Malformed field values (e.g. non-numeric `debtToEquity`) must not fail the whole ticker | done | 2026-07-21 — staff-engineer-reviewer finding: `_normalize_percent_field`'s `float()` conversion originally ran inside `fetch_metrics`'s retry-catching `try` block, so one bad field would burn all 3 retries and discard every other successfully-fetched field on that ticker. Now caught locally, logged, and treated as a missing field (`None`), not a fetch failure. |
| Batch-level timeout so a hung worker can't block the whole run forever | done | 2026-07-21 — staff-engineer-reviewer finding: neither `fetch_metrics` nor `future.result()` had any timeout, so a single stalled connection (no exception, just hangs) could block `fetch_all_metrics` indefinitely. `concurrent.futures.wait(..., timeout=config.DATA_FETCH_BATCH_TIMEOUT_SECONDS)` now bounds the wait; any still-pending ticker is reported as failed. **Known residual limitations**, not fully solved here: (1) this cannot forcibly kill an already-running worker thread — a stdlib `ThreadPoolExecutor` limitation — so the *logical* result is bounded but the OS process may not exit cleanly until the stuck thread finishes; the outer safety net for that is now its own explicit M11 task, not just a cross-reference. (2) `DATA_FETCH_BATCH_TIMEOUT_SECONDS` (600s) is one fixed ceiling regardless of ticker count — staff-engineer-reviewer noted the 400-ticker/17.5s live test below almost certainly never entered the retry/backoff path (0 failures), so it validates happy-path speed, not whether 600s is actually enough headroom for the full ~1500-ticker universe under real throttling with only 12 workers; a batch that's throttled broadly could push close to or past 600s, converting slow-but-recoverable tickers into `None` indistinguishably from a genuinely hung one. Revisit once a true full-scale (~1500-ticker) live run has been done. |
| Cache the raw response on the rejection path too, not just on success | done | 2026-07-21 — staff-engineer-reviewer finding: the one case an operator is most likely to want to inspect ("why did we reject this ticker?") was the one case that left no cached artifact. `_fetch_raw` now caches before raising on a symbol mismatch, and logs the raw key count for the same reason (see next row). |
| **Batch-scale live test** of `fetch_all_metrics()` against real concurrency | done | 2026-07-21 — a 400-ticker (27% of universe) run showed 0 failures, but a **full 1505-ticker run hit a real, significant bug**: `yfinance.exceptions.YFRateLimitError` on 991/1505 tickers (66%). The rate limit is session/IP-wide, not per-ticker, so each worker's independent backoff didn't help. Fixed with a shared cross-thread cooldown (`data._rate_limited_until`, lock-guarded): any worker hitting the error extends a shared cooldown every worker waits out before its next attempt. Retest: fetch failures dropped to 146/1505 (10%) — a ~85% reduction, and now right at `config.MIN_UNIVERSE_FETCH_FRACTION` (0.90)'s own threshold. Not fully eliminated; `config.DATA_RATE_LIMIT_COOLDOWN_SECONDS` (20s) is a first-pass value, not exhaustively tuned. **pm-reviewer flagged this has zero margin**: 10% failure sits exactly at `MIN_UNIVERSE_FETCH_FRACTION`'s 0.90 abort line, so nothing currently forces a retune before that check (M10) actually fires at full scale — treat the first real M10 abort as the signal to increase the cooldown, not a surprise. |
| **Real finding, live-verified:** a non-numeric field value crashed the entire run, not just one ticker | done | 2026-07-21 — the full-scale run above also hit `TypeError: bad operand type for abs()` mid-run and aborted completely: only the two percent-scaled fields (`debtToEquity`, `dividendYield`) had defensive type coercion (`_normalize_percent_field`); every other numeric field (`trailingPE`, `marketCap`, etc.) was trusted raw from yfinance. Generalized into `_coerce_float()`, applied to every numeric `Metrics` field. Also added defense-in-depth in `screener.run_screen()`: an unexpected exception computing one ticker's gates/score is now caught and logged (`data_invalid_outlier:unhandled`) rather than aborting the whole batch — the same per-ticker isolation DESIGN.md 3.2 requires at the fetch layer, now applied at the screening layer too. |
| Per-ticker failure tolerance (no run-abort) | done | 2026-07-21 — `fetch_all_metrics()` maps each symbol to `Metrics \| None` independently; a `None` for one ticker (or even an unexpected exception escaping `fetch_metrics`, which shouldn't happen but is caught defensively) never aborts the batch |
| Raw response caching per run | done | 2026-07-21 — `data_cache/<SYMBOL>.json` per ticker, overwritten each run (debugging aid, not a permanent audit trail — that's `journal.db`, M9); gitignored |
| Unit tests: pure functions (`_normalize_percent_field`, `_consecutive_positive_years`) | done | 2026-07-21 |
| Unit tests: `_fetch_raw` invalid-symbol detection and income-statement-failure tolerance | done | 2026-07-21 — via a fake `yf.Ticker` monkeypatched onto the `yfinance` module directly (not `data.yf`, to keep mypy's implicit-reexport check happy) |
| Unit tests: `fetch_metrics`/`fetch_all_metrics` retry, success, missing-field, and batch-tolerance branches | done | 2026-07-21 |
| **Real finding, live-verified:** yfinance's raw fields use inconsistent units | done | 2026-07-21 — `returnOnEquity`/`grossMargins`/`operatingMargins` are already decimal fractions (0.15 = 15%), but `debtToEquity`/`dividendYield` are raw percentage numbers (79.5 = 79.5%), confirmed against AAPL/KO/T/MSFT/JPM. `Metrics` normalizes both to decimal fractions so `config.py`'s all-decimal-fraction thresholds (`MAX_DEBT_TO_EQUITY = 1.0`, etc.) apply consistently — this would have been a silent, systematic Graham-gate miscalculation in M4 if not caught here. Test coverage strengthened per pm-reviewer: the original test only asserted the two converted fields, not that ROE/margins pass through *unchanged* — a regression that started dividing those by 100 too would have gone uncaught. Also confirmed `config.py`'s `MAX_PLAUSIBLE_DEBT_TO_EQUITY = 100` was always ratio-scale (matching `MAX_DEBT_TO_EQUITY = 1.0`'s units), not a stale raw-percentage assumption — added an explicit comment there so the convention isn't ambiguous for whoever implements M3. |
| **Real finding, live-verified:** yfinance doesn't raise for an invalid ticker | done | 2026-07-21 — returns a near-empty `info` dict (`{"trailingPegRatio": None}`, 1 key, vs. ~170+ for a real ticker) instead of an exception. `_fetch_raw` detects this explicitly via `info.get("symbol") != symbol` rather than relying on an exception that never comes. Staff-engineer-reviewer noted this same near-empty-dict shape can also occur under rate-limiting, not just a genuinely bad symbol, so retrying it is still correct — but the two causes were indistinguishable in logs; `_fetch_raw` now logs the raw key count and caches the raw response on rejection (see below) so an operator can tell them apart after the fact. |
| **Real finding, live-verified:** financial-sector tickers commonly lack `marketCap`/`debtToEquity` in yfinance | done | 2026-07-21 — confirmed for JPM (166 keys returned, neither present) and reproduced again in the batch-scale test below (e.g. `AAL`'s `debt_to_equity`). Not a bug — legitimate missing data, correctly surfaces as `None` fields today and will become `data_missing:market_cap`/`data_missing:debt_to_equity` once M3's `validate_metrics` lands. **Follow-up decision point flagged for M5 (pm-reviewer):** `EXCLUDED_SECTORS = ()` is the current live default, so this isn't hypothetical — financial-sector names will fail Graham gates on missing data rather than on substantive criteria unless `EXCLUDED_SECTORS` is set to exclude Financials. M5's tuning-pass task should explicitly decide (and record) whether that's the intended behavior or whether Financials should be excluded by default. |

## M3 — Data validation layer

Landed together with M4/M5 in one MVP-pace push, `data.py`/`screener.py`,
single review round (see [[munger-mvp-pacing]]).

| Task | Status | Date / Notes |
|---|---|---|
| `validate_metrics(metrics) -> list[str]` sanity/outlier checks | done | 2026-07-21 — every `Metrics` field except `symbol` checked for `None`; `trailing_pe`/`debt_to_equity` also outlier-checked (`abs(value) > MAX_PLAUSIBLE_*`) |
| Distinct `data_missing:<field>` fail code | done | 2026-07-21 |
| Distinct `data_invalid_outlier:<field>` fail code | done | 2026-07-21 |

## M4 — Graham gate logic

| Task | Status | Date / Notes |
|---|---|---|
| `pass_graham_gates(metrics)` — 7 criteria (DESIGN.md §3.3 Stage 1) | done | 2026-07-21 — starts from `validate_metrics()`'s reasons, so it's correct even called standalone |
| Returns `(passed, fail_reasons)` | done | 2026-07-21 |
| Thresholds pulled from `config.py` | done | 2026-07-21 |
| Unit tests against hand-computed fixtures (DESIGN.md §6, layer 1) | done | 2026-07-21 — one fixture per gate + combined pass/fail case |
| **Real bug found (staff-engineer-reviewer):** negative `debt_to_equity` silently passed the debt gate and scored as "better than zero debt" | done | 2026-07-21 — same trap already guarded for `trailing_pe` (negative numerically compares less than a positive max threshold) but missed for D/E. A company with negative book equity would have ranked competitively for the eventual buy queue. Fixed in both the gate and `calculate_munger_score`'s low-debt component; reviewed again to confirm no other field has the same shape (none found: ROE/margins/FCF are `< MIN` checks, which correctly fail on negative input by construction). |

## M5 — Munger scorer

| Task | Status | Date / Notes |
|---|---|---|
| `calculate_munger_score(metrics)` | done | 2026-07-21 — 0-100 weighted composite, each component clamped to [0,1] before weighting |
| `run_screen(tickers) -> DataFrame` | done | 2026-07-21 — also wraps each ticker's gate/score computation in a try/except (defense in depth, see M2's crash finding above) |
| **Real bug found and fixed while building M11**: `run_screen([])` crashed with `KeyError: 'score'` | done | 2026-07-22 — `pd.DataFrame([])` on an empty `tickers` list has zero *columns*, not just zero rows (the same empty-DataFrame quirk already fixed once in M6/M7's `test_portfolio.py`), so `sort_values("score", ...)` raised. Found live via a manual `runpy`-based test of `bot.py`'s new `__main__` crash-logging path (M11), which happened to call `run_screen([])` through the new "alert on empty universe fetch" path — exactly the scenario this would have crashed on in production. Fixed with an explicit empty-input guard returning a properly-columned empty `DataFrame` instead of falling through to the crash. Covered by `test_run_screen_handles_an_empty_ticker_list`. |
| `screen_results.csv` output | done | 2026-07-21 — key columns (symbol, buyable, score, fail_reasons) first, sorted by score |
| Unit tests for `calculate_munger_score` against hand-computed fixtures (DESIGN.md §6, layer 1) | done | 2026-07-21 — one fixture per weighted component, a clamp-above-target case, a combined-100 case, and a negative-D/E regression case |
| `screen-sanity-reviewer` subagent — reads `screen_results.csv` and flags anything that looks like a data-error artifact rather than a real high-quality compounder | todo | scoped as a plausibility/sanity checker, not stock-picking advice — same lane as the deterministic gates, formalizes the "eyeball test" DESIGN.md §6 already calls for as a manual step |
| Tune thresholds against live data (~20–50 buyable names) | todo | **First live full-universe run: 7/1505 buyable** (well below the 20-50 target band) — see DESIGN.md's own build-order note ("zero or 300 means a threshold is wrong") for why this is expected before tuning, not a bug. Failure breakdown (of 1505): `graham_pe_times_pb` 861, `munger_roe` 764, `graham_pe` 723, `graham_current_ratio` 611. Spot-checked several real tickers (CB, JNJ, KO) against known-real numbers to confirm this isn't a data bug — today's market genuinely has elevated valuations (matches the user's own earlier observation). **Not yet done**: an actual tuning pass adjusting `config.py` thresholds; this needs the user's input on how aggressive to be, since loosening Graham's valuation gates is an investment-philosophy decision, not a pure engineering one. **Explicitly decide the `EXCLUDED_SECTORS`/Financials question flagged in M2**: yfinance commonly lacks `marketCap`/`debtToEquity` for financial-sector tickers, so with the current `EXCLUDED_SECTORS = ()` default those names fail on missing data rather than substantive criteria — decide and record whether that's acceptable. |

## M6 — Strike state machine

Implemented in `portfolio.py` alongside M7, single review round (MVP pace).

| Task | Status | Date / Notes |
|---|---|---|
| `StateTracker` — strike counters only, atomic writes to `state.json` | done | 2026-07-21 — never holds current holdings; corrupt/unreadable file falls back to empty state (fail-safe, not fail-dangerous: worst case is a missed liquidation, not a wrongful one) |
| `process_sells(current_holdings, new_market_data, state)` | done | 2026-07-21 — `current_holdings: dict[str, float]` (ticker -> market value), fetched live from broker by the caller, not read from state; `new_market_data: dict[str, Metrics \| None]` |
| Two-strike rule (missing-ticker-as-strike, reset-on-clean) | done | 2026-07-21 |
| **Real bug found (staff-engineer-reviewer):** strike counters never cleared on liquidation | done | 2026-07-21 — a ticker's strike count survived leaving `current_holdings`; if ever re-bought, it would inherit the stale count and could liquidate after just one bad check instead of two. Fixed: `process_sells` now resets strikes to zero at the moment it adds a ticker to `to_liquidate`, so a future re-buy starts a fresh streak. Regression test added. |
| Reconciliation warning on live-vs-expected holdings divergence | todo | needs the trade journal (M9) to know what a prior run "expected" — not implemented yet, tracked here explicitly rather than silently skipped |
| Unit tests for the strike/reset state machine against hand-computed fixtures (DESIGN.md §6, layer 1) | done | 2026-07-21 — covers: one strike then clean (resets), two consecutive strikes (liquidates), missing ticker (counts as strike), `None` metrics (counts as strike), corrupt state file, save/reload roundtrip |

## M7 — Buy queue / target construction

| Task | Status | Date / Notes |
|---|---|---|
| `generate_buy_queue(current_holdings, screen_results, available_cash)` | done | 2026-07-21 |
| ~1/15 target weights, 12% single-name cap, 2% cash buffer | done | 2026-07-21 — per-position cap is `min(portfolio_value/TARGET_POSITION_COUNT, portfolio_value*MAX_SINGLE_POSITION_WEIGHT)`; under default config the 1/15 target (~6.7%) never actually hits the 12% cap, so a dedicated test forces a small `TARGET_POSITION_COUNT` to exercise the cap explicitly |
| Top up existing positions before opening new ones | done | 2026-07-21 |
| $50 dust filter | done | 2026-07-21 — applied to both top-up and new-position orders |
| Never sell-to-buy (no churn) | done | 2026-07-21 — trivially true by construction: this function has no sell code path at all, sells are entirely `process_sells`'s responsibility with no shared state between the two beyond `StateTracker` |
| **Live-verified** against the real M5 screen output | done | 2026-07-21 — starting from an empty portfolio with $100k cash and the real 7-buyable screen result, correctly generated 7 orders at ~$6,667 each (1/15 of equity), no cap/dust issues at this scale |

## M8 — Execution module

**Live-verified 2026-07-23.** Alpaca account provisioning (M0) is
unblocked — the user created a paper account and this session ran
`ExecutionModule` directly against the real paper API (`.env`,
gitignored, holds the keys locally; not committed, not embedded in any
Bash command — see the M0 row below for how this was handled). Every
method was exercised live and confirmed: `verify_account_access()`,
`get_current_holdings()` (empty on a fresh account), `get_available_cash()`
($100,000 starting paper balance), `market_buy("AAPL", 10.0)` (accepted,
`PENDING_NEW` → `FILLED`, resolving the `notional`-on-limit-order
ambiguity below for real), `has_already_submitted()` (correctly `True`
against the order just placed), the crash-recovery path (a second
`market_buy("AAPL", 10.0)` call under the same `client_order_id`
returned the *same* order instead of submitting a duplicate — confirmed
via the returned `order.id` matching), and `liquidate("AAPL")` (fetched
the exact fractional qty via `get_open_position`, submitted an explicit
sell `LimitOrderRequest`, accepted). Final state: holdings back to `{}`,
cash back to `$100,000` — a fully clean round trip.

| Task | Status | Date / Notes |
|---|---|---|
| `ExecutionModule` wrapping alpaca-py | done | 2026-07-21 — `execution.py`; live-verified 2026-07-23, see banner note above |
| `market_buy(symbol, notional)`, `liquidate(symbol)` | done | 2026-07-21 — both submit a `LimitOrderRequest` (not a market order) with the price band applied; `liquidate` fetches the exact held quantity via `get_open_position` rather than using `close_position`, since that endpoint has no limit-price control (confirmed against the SDK's `ClosePositionRequest`, which only has `qty`/`percentage` fields). Both live-verified 2026-07-23 against the real paper API. |
| **Real risk flagged (staff-engineer-reviewer), resolved live 2026-07-23:** whether `notional` works on a `LimitOrderRequest` at all is genuinely unclear from Alpaca's own docs | done, **verified live** | 2026-07-21 — checked three sources, got three different signals: Alpaca's general API reference says `notional` "can only work for market order types"; Alpaca's dedicated fractional-trading page says limit orders "are supported for both fractional and notional orders"; the installed `alpaca-py` SDK's own docstring says fractional qty is "for market orders" only, but that sentence is identical boilerplate across every order-type class (market, limit, stop-limit), so it reads like an unmaintained copy-paste, not a real per-type constraint. **2026-07-23: resolved definitively** — submitted a real `market_buy("AAPL", 10.0)` against the live paper API; Alpaca accepted it (`status=PENDING_NEW`, `notional=10`, `limit_price` set), later settled to `FILLED` with `holdings["AAPL"] == 9.985954`. `notional` on a `LimitOrderRequest` genuinely works — the fractional-trading docs page was right, the general API reference and the SDK's boilerplate docstring were both stale/misleading. No code change needed; `market_buy` already submitted `notional` directly. |
| `get_current_holdings()` — live portfolio fetch from the broker | done | 2026-07-21 — ticker -> market value; a position with a `None` market_value (the SDK types it optional) is excluded with a warning rather than coerced to 0.0, which would corrupt `generate_buy_queue`'s weight math. Live-verified 2026-07-23: correctly returned `{}` on a fresh account and `{"AAPL": ...}` after the test buy settled. |
| Deterministic `client_order_id` (run-date + ticker + side) | done | 2026-07-21 — plain readable string (`"{run_date}-{symbol}-{side}"`), not a cryptographic hash — DESIGN.md's own example (`2026q3-AAPL-buy`) is readable, and a hash adds no idempotency value here while making Alpaca's order list harder to debug. `run_date` is same-day granularity (e.g. `"2026-07-21"`), matching DESIGN.md §4/M11's actual "same-day idempotency" requirement, not the quarter-label the doc's illustrative example uses. |
| `has_already_submitted(client_order_id)` — secondary idempotency guard | done | 2026-07-21 — direct `get_order_by_client_id` lookup rather than a broader "today's open+filled orders" batch fetch; simpler and equally correct since `client_order_id` is already the deterministic key being checked. **Live-verified 2026-07-23, including the actual crash-recovery scenario**: called `has_already_submitted` directly (returned `True` against the order just placed), then called `market_buy("AAPL", 10.0)` a second time under the same `run_date` — it correctly recovered and returned the *original* order (`order.id` identical to the first call's) instead of submitting a duplicate. This is the first live proof the M10 idempotency-guard wiring fix actually works, not just unit-tested. |
| Raise (abort run) if the broker pre-check query itself fails | done | 2026-07-21 — `has_already_submitted` re-raises on any non-404 `APIError`, fail-closed; unlike `market_buy`/`liquidate`, which catch their own errors so one rejected order never aborts the run |
| Limit-price band (±2%) on every order, including liquidations | done | 2026-07-21 — buys get a ceiling above last trade, sells/liquidations get a floor below it (band always favors the requester, not against them) |
| **Real gap found (staff-engineer-reviewer):** a same-response rejection wasn't distinguished from a successful submission | done | 2026-07-21 — `submit_order` can return HTTP 200 with an `Order` whose `status` is `REJECTED` (e.g. wash-trade prevention) rather than raising; the original code only checked `isinstance(order, Order)`, so a rejected order would have been returned as if it succeeded — a caller/journal treating any non-`None` return as "order placed" would have logged a phantom buy. Fixed: `_submit_and_check()` also inspects `order.status` against `{REJECTED, CANCELED, EXPIRED, SUSPENDED}` and returns `None` for any of those. Regression test added (mocks a rejected-status response, asserts `market_buy` returns `None`). |
| Paper mode by default | done | 2026-07-21 — `TradingClient(paper=config.PAPER_TRADING)`, and `config.PAPER_TRADING` already defaults to `True` (M0) |
| Unit tests against a mocked Alpaca SDK | done | 2026-07-21 — 13 tests: client_order_id determinism, limit-price band direction (buy above/sell below), idempotency guard (found/404/other-error-reraises), holdings mapping, both order types' submitted request shape (including `side`/`time_in_force`, added per staff-engineer-reviewer — a flipped buy/sell would previously have passed), both order types' per-order fault tolerance, paper flag plumbing. Fake latest-trade object is `spec=Trade` (not a bare class), same reasoning as the `Order`/`Position` fakes: an unspec'd fake would stay green even if the real response shape diverged. |
| `get_current_holdings()` fails closed, explicitly (staff-engineer-reviewer) | done | 2026-07-21 — no try/except, documented as deliberate: this is the primary input to both the sell and buy decisions, so a broker outage returning an empty dict would look identical to "you own nothing" and could drive duplicate buys. Caller (bot.py, M10) must let it propagate and abort. |
| `verify_account_access()` — startup check that keys match configured paper/live mode | done | 2026-07-22 — added while building M10, once `bot.py` needed a concrete startup assertion. Calls `get_account()` with no try/except; a successful call under this client's configured `paper` flag inherently proves the keys match that mode, so a mismatch (e.g. stale `.env` live keys with `PAPER_TRADING=True`) raises and aborts. Same fail-closed posture as `get_current_holdings()`. **Live-verified 2026-07-23** against the real paper account. |
| `get_available_cash()` — account cash balance for `generate_buy_queue`'s input | done | 2026-07-22 — added alongside `verify_account_access()` for the same reason. No try/except; raises if `account.cash` is `None` (the SDK types it optional) rather than silently proceeding with a wrong figure (DESIGN.md 3.4). **Live-verified 2026-07-23**: correctly returned `$100,000.0` (Alpaca's default fresh-paper-account balance), and settled back to exactly that after the test buy/sell round trip. |
| **Deferred, not blocking:** no retry/backoff distinguishing transient broker errors (e.g. 429) from genuine order rejections | todo | staff-engineer-reviewer noted a rate-limited request currently looks identical in logs to a real rejection. DESIGN.md 3.5 doesn't call for retry-with-backoff on this module the way §3.2 does for the data fetcher; not adding it preemptively without a live account to observe real failure patterns against. Revisit once M8 has actually run against Alpaca. |

## M9 — Trade journal & logging

| Task | Status | Date / Notes |
|---|---|---|
| Append-only journal as **SQLite** (`journal.db`), not CSV | done | 2026-07-22 — `journal.py`, `record_order()`; schema created on first connect (`CREATE TABLE IF NOT EXISTS`) |
| Reason string per order | done | 2026-07-22 — free-text `reason` column; caller (bot.py, M10) constructs the exact DESIGN.md 3.6 format (e.g. `"NEW_POSITION score=78.2"`), journal.py doesn't know about score/strikes semantics |
| Timestamped `screen_results.csv` archive | done | 2026-07-22 — `archive_screen_results(run_date)`, copies into `screen_results_archive/` (gitignored, like the other runtime artifacts) rather than littering timestamped files in the repo root |
| Structured logging to file and stdout | done | 2026-07-22 — `configure_logging()`, idempotent (safe to call more than once without duplicating handlers) |
| Reconciliation check against journal-expected holdings | done | 2026-07-22 — `get_expected_holdings()` (most recent action per symbol) + `check_reconciliation(actual_holdings)`, returning human-readable mismatch strings for the caller to log. Closes the gap M6 explicitly deferred ("needs the trade journal, which doesn't exist yet"). Not an abort condition — a signal, per DESIGN.md 3.4/3.6. |
| `record_order`'s `side` validated against `{"buy", "sell"}` | done | 2026-07-22 — staff-engineer-reviewer finding: an unvalidated typo (`"Buy"`, `"buy "`) would be silently treated as a sell by `get_expected_holdings`, which only special-cases the exact string `"buy"`. Now raises `ValueError` instead. |
| **Deferred to M10 (staff-engineer-reviewer):** no dedup guard on `record_order`/`client_order_id` | todo | `client_order_id` is captured as a label but nothing enforces uniqueness (no `UNIQUE` constraint, no check-before-insert) — a caller retrying after a crash between "order submitted" and "journal write" could double-append. The right fix depends on the write-then-order vs. order-then-write sequencing bot.py (M10) actually uses, which doesn't exist yet — tracking here so it isn't forgotten once it does. |
| **Deferred (staff-engineer-reviewer):** no schema migration path | todo | `CREATE TABLE IF NOT EXISTS` means a column added to the schema later won't retroactively apply to an existing `journal.db` file. Not a problem until the schema actually needs to change; noting it now so a future change doesn't silently diverge across environments with different `journal.db` ages. |
| Unit tests | done | 2026-07-22 — append-only behavior across multiple calls, reconciliation using the *most recent* action per symbol (not just "any buy ever"), both mismatch directions, archive copy, logging idempotency. Live-smoke-tested end to end (record → expected holdings → reconciliation → archive). |

## M10 — Bot orchestration & safety controls

| Task | Status | Date / Notes |
|---|---|---|
| `bot.py` — ties all modules together | done | 2026-07-22 — `run()`: universe -> screener -> archive always; fetch-fraction abort; kill-switch check; then reconciliation, sells, buys, budgets, journaling, all gated on the broker actually being touched. |
| Exclude `process_sells`'s `to_liquidate` tickers from `generate_buy_queue`'s `current_holdings` input | done | 2026-07-22 — added 2026-07-21 (staff-engineer-reviewer, M6/M7 review) — `generate_buy_queue` has no visibility into what `process_sells` decided to liquidate this run; without this, a position slated for sale could get topped up in the same run it's about to be closed. Was documented as a caller contract in both functions' docstrings; now enforced in `bot.run()` via `remaining_holdings = {t: v for t, v in current_holdings.items() if t not in to_liquidate}`, covered by `test_run_full_happy_path_places_liquidations_and_buys`. |
| Startup assertion: API key account mode matches configured paper/live flag | done | 2026-07-22 — `execution.ExecutionModule.verify_account_access()` (added to `execution.py` in this M10 window, see M8's table), called first thing in `bot.run()` after the kill-switch check, no try/except so a mismatch aborts the run. **Updated 2026-07-23**: the function itself is now live-verified against the real Alpaca API (M8's table) — but only via a standalone script calling `ExecutionModule` directly, not via `bot.run()`'s actual call site. The assertion *as wired into the orchestrator's startup sequence* is still unexercised; don't read M8's live verification as having proven this specific integration point. Closing that requires a real run through `bot.py` itself or the GitHub Actions workflow (M11). |
| `KILL_SWITCH` (config flag + filesystem flag file) | done | 2026-07-22 — `bot._kill_switch_active()`; checked after the fetch-fraction abort and before any broker construction. Unit-tested (`test_run_screen_only_when_kill_switch_active`); the live smoke test below did not reach this branch. |
| `GLOBAL_ORDER_BUDGET` abort (max order count) | done | 2026-07-22 — checked after `to_liquidate`/`buy_orders` are computed but before either loop submits anything, so a budget breach aborts with zero partial execution. |
| `GLOBAL_NOTIONAL_BUDGET_PCT` abort (max % equity moved per run) | done | 2026-07-22 — added in design review round 2; same zero-partial-execution ordering as the order-count budget. |
| Universe-fetch-fraction sanity abort | done | 2026-07-22 — `bot._fetched_fraction()`, checked first (before kill-switch), aborts before archiving is the only side effect. Live-verified this actually fires under real degraded conditions (see below), not just unit-tested. |
| **Real gap found (staff-engineer-reviewer):** the universe-fetch-fraction abort doesn't cover the *second*, holdings-only metrics fetch | done | 2026-07-22 — `bot.py` calls `data.fetch_all_metrics` a second, independent time for currently-held tickers, feeding straight into `process_sells`'s strike logic (a missing ticker counts as a strike). A degraded fetch here — e.g. the same rate-limit cooldown from the first fetch still in effect, exactly what today's live smoke test hit — could silently strike real holdings toward liquidation with no distinguishing signal from a genuine quality failure. Fixed: `bot._process_sells_if_data_is_healthy()` computes the same fetched-fraction check against `MIN_UNIVERSE_FETCH_FRACTION` for the holdings-only fetch and skips `process_sells` entirely (logging an error, no strikes recorded) if it's too degraded to trust. Covered by `test_run_skips_sell_evaluation_when_holdings_data_mostly_missing`. |
| **Real bug found (staff-engineer-reviewer):** two disagreeing `portfolio_value` computations in the same run | done | 2026-07-22 — the `GLOBAL_NOTIONAL_BUDGET_PCT` check computed `portfolio_value` from the full `current_holdings` (including positions about to be liquidated), while `generate_buy_queue` independently recomputed its own `portfolio_value` from `remaining_holdings` (excluding them) to size every buy order. The two differed by exactly the liquidated positions' value, making the budget check systematically more permissive than the sizing logic it was supposed to validate. Fixed: `bot.py` now computes `portfolio_value` from `remaining_holdings`, matching what `generate_buy_queue` actually used. |
| **Real gap found (staff-engineer-reviewer):** `has_already_submitted` (built in M8) was never called from anywhere in the real call path | done | 2026-07-22 — the secondary idempotency guard existed and was unit-tested in isolation but `market_buy`/`liquidate` never used it, so the crash-and-restart scenario the module's own docstring describes (order fills, process crashes before the journal write) would hit Alpaca's duplicate-`client_order_id` rejection on retry, get swallowed by the blanket `except Exception` as an ordinary failed order, and that already-successful order would permanently never get journaled — `check_reconciliation` would flag the resulting mismatch forever with no repair path. Fixed: `market_buy`/`liquidate` now call `has_already_submitted` first, **outside** their own try/except (so a broken idempotency check still fails closed and aborts the run, rather than being swallowed like a genuine rejection); if already submitted, they fetch and return the existing order instead of resubmitting, so the caller journals it as normal. Covered by `test_market_buy_recovers_existing_order_instead_of_resubmitting`, `test_liquidate_recovers_existing_order_instead_of_resubmitting`, and two propagation tests for the fail-closed case. |
| Unit tests for `bot.py`'s wiring (`tests/test_bot.py`) | done | 2026-07-22 — 16 tests, all external modules mocked (this file tests wiring/ordering, not each module's own correctness): kill-switch via config flag and via flag file, fetch-fraction abort, happy path (asserts the liquidated ticker is excluded from the buy queue's input, asserts journal reasons), both budget aborts (zero orders submitted on breach), reconciliation warnings logged without aborting, a failed order not journaled while the run continues (staff-engineer-reviewer finding — this was previously zero-coverage), fail-closed propagation out of `bot.run()` when `verify_account_access`/`get_current_holdings` raise (pins the "no try/except" contract against a future well-meaning regression), and the holdings-fetch-fraction skip above. Hit the same mypy implicit-reexport issue as `test_data.py`/`test_execution.py` — fixed by importing `universe`/`screener`/`execution`/`journal`/`data`/`portfolio` directly and monkeypatching those modules rather than `bot.<module>`. 162/162 tests pass across the whole suite (`test_execution.py` grew from 13 to 21 tests covering the `has_already_submitted` wiring above). |
| Live smoke test: universe-fetch-fraction abort fires safely under degraded conditions | done | 2026-07-22 — ran `bot.run()` live (no mocks) with the `KILL_SWITCH` flag file present. Fetched the real S&P 1500 universe, ran the real screener, archived results — process exited 0, no unhandled exceptions. yfinance rate-limited unusually hard this run (only 84.5% of tickers fetched cleanly, worse than M2's ~90% retest — the "zero margin" risk flagged in M2 materialized live), so the fetch-fraction abort fired and the run returned before ever reaching the kill-switch check or constructing `ExecutionModule`. Confirms the abort ordering fails safe (never touches the broker) under real degraded conditions. |
| **Split out by pm-reviewer — was incorrectly bundled into "done" above:** live smoke test of the `KILL_SWITCH` screen-only branch specifically, against live data | todo | pm-reviewer finding, 2026-07-22: the row above validated a *different* safety gate (fetch-fraction) than the one this task names. `bot.run()` checks fetch-fraction before kill-switch, so on any day with degraded yfinance behavior this branch is structurally hard to reach live — re-run deliberately (not opportunistically) on a day without heavy rate limiting to actually exercise it. |
| **New, pm-reviewer finding — gating item before M11 scheduling:** get one live run that actually reaches `ExecutionModule` against the real Alpaca paper API (even with zero orders placed) | done | 2026-07-22 (gate identified), **closed 2026-07-23** — user created the Alpaca paper account; this session called `verify_account_access()`, `get_current_holdings()`, `get_available_cash()`, `market_buy()`, `has_already_submitted()`, and `liquidate()` directly (a local script, not yet via `bot.py`'s full `run()` or the GitHub Actions workflow — that's still open, see M11) against the real paper API. All six worked correctly; see M8's table for the full account. This closes the gate pm-reviewer set here: the broker-facing code path has now executed outside mocks, with a real (if small, $10) order placed, filled, and liquidated cleanly. |

## M11 — Scheduling & observability

**Alerting mechanism (decided 2026-07-22):** DESIGN.md doesn't specify a
delivery channel (no Slack/email/PagerDuty account exists for this solo
project, and standing one up would be its own external-account blocker
like Alpaca's). Instead, `bot.py`'s `run()` collects every alert-worthy
condition into a list and returns exit code 1 if non-empty (`_finish()`),
also printing each as a GitHub Actions `::error::` annotation. A non-zero
exit marks the workflow run failed, which triggers GitHub's own built-in
failure-notification email to the repo owner — using CI failure
signaling as the alert channel, deliberately, rather than a new service.

**Sequencing note (pm-reviewer, M11 delta review, 2026-07-22; updated
2026-07-23): M11's code is done and unit-tested; M10's `ExecutionModule`
gating item is now closed (see M10's table) via a direct local
verification — but this milestone's own *GitHub Actions* mechanisms are
still unverified against anything real.** The Alpaca account now exists
and its keys were confirmed to work live (M8/M10 tables), but only via a
one-off local script run directly against `execution.py` — not via
`bot.py`'s full `run()`, and not via the `quarterly-run.yml` workflow
itself. Two concrete things remain, independent of each other:
1. `ALPACA_API_KEY`/`ALPACA_SECRET_KEY` still need to be added as GitHub
   repo secrets (Settings → Secrets and variables → Actions) — currently
   only sitting in the local, gitignored `.env`. Until that's done, a
   scheduled or dispatched `quarterly-run.yml` run would still crash at
   `verify_account_access()` with an auth error (a safe failure, and one
   that would exercise the `__main__` exception-logging path and GitHub's
   failed-run notification — but *not* the `_alert()`/`_finish()`
   list-based alerting scheme below, which only fires for a condition
   detected without an exception being raised; the two are distinct
   mechanisms this milestone deliberately keeps separate).
2. Once those secrets exist, the `bot-state` persistence mechanism and
   the `heartbeat.yml` watchdog are *still* unverified against real
   GitHub Actions (see their own rows below) — closing M10's gate locally
   doesn't substitute for that.

**Required next step:** add the two secrets, then manually trigger
`quarterly-run.yml` via `workflow_dispatch` with `screen_only` unchecked
— this becomes the first real end-to-end run through `bot.py` itself
(not just `execution.py` in isolation) and the first live test of the
`bot-state` persistence mechanism. **Operational gap (pm-reviewer):**
nothing in code enforces this sequence — if secrets are added shortly
before a scheduled quarterly date, the cron could fire for real before
the manual verification happens. Relies on operator diligence; not worth
a code gate for a cron that only fires 4x/year.

| Task | Status | Date / Notes |
|---|---|---|
| Cron / GitHub Action wiring for quarterly cadence **(superseded, see M12: cadence changed to daily 2026-07-26, file renamed to `daily-trade.yml`)** | done | 2026-07-22 — `.github/workflows/quarterly-run.yml`; `cron: "0 13 15 1,4,7,10 *"` (13:00 UTC, 15th of Jan/Apr/Jul/Oct, matching DESIGN.md 4's "~2-3 weeks after quarter-end"), plus `workflow_dispatch` with a `screen_only` boolean input (defaults `true` — a manual trigger is screen-only unless explicitly unchecked) for safe manual verification/reruns. |
| Outer process-level timeout wrapping the whole `python bot.py` invocation | done | 2026-07-22 — `timeout-minutes: 45` on the workflow job (bumped from an initial 30 for margin, plus `cache: pip` on `actions/setup-python` to shave install time off that budget — both staff-engineer-reviewer suggestions). M2's `fetch_all_metrics()` bounds its own *logical* wait via `concurrent.futures.wait(timeout=...)`, but can't force-kill an already-hung worker thread (stdlib `ThreadPoolExecutor` limitation, see `data.py`'s docstring); this is the outer safety net at the scheduler layer. **Known, accepted, bounded risk (staff-engineer-reviewer):** this is a *job-level* timeout — if it fires mid-run, the "Persist bot state" step gets only a short grace period and could be cut off, regressing `state.json`/`journal.db` to the prior snapshot. Order idempotency is unaffected (broker-anchored via `client_order_id`, not this workflow's persistence), so the worst case is spurious reconciliation warnings next run, not double-orders or corruption — not solved further given that bound. |
| Same-day idempotency via `client_order_id` + today's orders/positions | done | Already satisfied by M8/M10: `client_order_id` is deterministic per run-date/symbol/side, and `has_already_submitted` (M8) is now actually wired into `market_buy`/`liquidate` (M10) as the secondary pre-check against an existing order under that same ID — this row was really asking for what those two milestones already built; no new code needed here. **Known, narrow edge case (staff-engineer-reviewer, M11 review):** `run_date` defaults to `datetime.date.today()` when `bot.py` is invoked with no argument (the normal case). If an operator manually re-dispatches the workflow to recover from a crash on a *later calendar date* than the original attempt, `has_already_submitted` checks a different `client_order_id` (yesterday's date is baked in) and no longer recognizes the earlier attempt — a real, if narrow, double-order window on manual recovery across a day boundary. Not solved this milestone; worth an explicit operational runbook note (pass the original `run_date` explicitly when manually recovering a crashed run) rather than a code change. |
| **New (staff-engineer-reviewer, M10 review):** lock against overlapping/concurrent `bot.run()` invocations | done | 2026-07-22 — solved at the scheduling layer as originally scoped: the workflow's `concurrency: {group: quarterly-run, cancel-in-progress: false}` queues a second trigger rather than letting it race an in-flight run against `state.json`/`journal.db`. |
| **New (staff-engineer-reviewer, M10 review):** per-run abort paths (kill switch, fetch-fraction, both budgets) are indistinguishable from a full success by exit code alone | done | 2026-07-22 — `bot.run()` now collects every alert-worthy condition (aborts, liquidations, reconciliation mismatches, universe fallbacks, empty universe, stale-run gap) into an `alerts: list[str]`, returning exit code 1 if non-empty, 0 otherwise (`_finish()`). Kill-switch-active is deliberately *not* alert-worthy on its own (an intentional operator action), unlike every other condition here. |
| **New (staff-engineer-reviewer, M10 review):** `GLOBAL_ORDER_BUDGET`/`GLOBAL_NOTIONAL_BUDGET_PCT` bound a single run correctly but have no cross-run/cumulative counterpart | todo | 2026-07-22 — deliberately **not** implemented this milestone: the concurrency-lock row above now prevents true overlapping runs, which was the main scenario this would guard against; the residual risk (an operator manually re-running the same day after a run already completed) is a narrower edge case involving deliberate operator action. Revisit if that actually happens in practice rather than adding an untested extra state file preemptively for a real-money system under time pressure. **pm-reviewer noted (2026-07-22):** there's currently no signal that would let anyone notice this edge case occurred — no log line or journal marker distinguishes "two runs completed today" from normal operation. If revisited, the fix should include that signal, not just the budget tracking itself. |
| Alert on run failure | done | 2026-07-22 — any uncaught exception (e.g. `verify_account_access`/`get_current_holdings` failing) already propagates and crashes the process with a non-zero exit by default, which GitHub Actions treats as a failed run/notification exactly like the `_finish()`-driven alerts above. **Real gap found (staff-engineer-reviewer) and fixed:** Python's default excepthook writes a crash traceback to stderr only — `munger.log` (the copy actually persisted across runs via the `bot-state` branch) would never have recorded *why* a run crashed, only the GitHub Actions console output for that one run. Fixed: `__main__` now wraps `run()` in a `try/except` that logs the exception via `logger.exception()` before re-raising, so the traceback survives in the persisted log too. Manually verified locally via `runpy.run_module("bot", run_name="__main__")` with a forced crash — confirmed the traceback appears in the log file and the process still exits non-zero. **Related real gap found and fixed**: alerts appended to the `alerts` list were previously only logged when `_finish()` was finally reached — if an exception fired earlier in the same run (e.g. a universe fallback alert followed by `verify_account_access` raising), that earlier alert was silently lost, never logged anywhere. Fixed: a new `_alert()` helper logs and prints each alert immediately at the point of discovery, not just at the end. **Not yet verified against real GitHub Actions** (pm-reviewer caught an earlier draft of this row overclaiming "verified live" with a dangling reference to a note that didn't exist — corrected here); that verification is pending the `workflow_dispatch` runs described in the sequencing note above. |
| Alert on empty universe fetch | done | 2026-07-22 — `bot.run()` checks `if not tickers` after `universe.get_universe_with_diagnostics()` and appends an alert. Covered by `test_run_alerts_on_an_empty_universe_fetch`; not yet forced against the *live* Wikipedia source (would require breaking the live fetch, not attempted — unit-tested only, same caveat as everything else not yet run against real Alpaca). **Manually testing this exact path surfaced a real crash bug in `screener.run_screen([])`** (`KeyError: 'score'` on an empty ticker list) — fixed in M5's table (its own module), not repeated here. |
| Alert on per-index universe fallback (S&P 500/400/600), severity tiered by consequence | done | 2026-07-22 — `universe.py` gained `get_universe_with_diagnostics() -> UniverseResult` (tickers + `fallback_indices`), a thin diagnostics layer over the existing per-index fallback logic (`get_universe()` itself is now a wrapper over it, unchanged for other callers). `bot.py` alerts per fallen-back index, tagging S&P 500 "HIGH" severity vs. 400/600 "normal" in the alert text, per DESIGN.md 3.1/3.6. Covered by `test_get_universe_with_diagnostics_reports_fallback_indices` and `test_run_alerts_on_a_universe_index_fallback`. **Known, accepted limitation (staff-engineer-reviewer):** the severity tier is textual only — the exit code is always `1` regardless of tier, so GitHub's failure-notification email (the sole alert channel here) doesn't itself differentiate a high-severity S&P 500 fallback from a routine reconciliation blip; the "HIGH" label is only visible if the log is actually opened. Acceptable for a solo project without a tiered paging system; not solved further. |
| Alert on any liquidation event | done | 2026-07-22 — `bot.run()` alerts whenever `to_liquidate` is non-empty. Covered by `test_run_full_happy_path_places_liquidations_and_buys`'s exit-code assertion; not yet forced in an actual paper-account two-strike scenario (blocked on the same Alpaca account as M8/M12). |
| Alert on state/broker reconciliation mismatch | done | 2026-07-22 — `bot.run()` alerts on every string `journal.check_reconciliation()` returns (already logged as a warning since M9/M10; now also alert-worthy). Covered by `test_run_logs_reconciliation_warnings_without_aborting`'s exit-code assertion. |
| Data-freshness check | done | 2026-07-22 — `bot._check_data_freshness()`: a dead-man's-switch comparing the most recent archived run's own date against `config.DATA_FRESHNESS_MAX_HOURS`. **Reconciled a stale value while wiring this up**: `DATA_FRESHNESS_MAX_HOURS` was set to `24` back in M0 (matching DESIGN.md's PM-recommendations narrative text literally), which predates the quarterly-cadence decision (M1) — a literal 24-hour threshold would have alerted on every single healthy quarterly gap. Changed to `130 * 24` hours (quarterly ~91 days + the "2-3 weeks after quarter-end" scheduling offset + buffer), so it only fires if a scheduled run was actually missed. **Critical bug found (staff-engineer-reviewer) and fixed before this ever ran live**: the first implementation compared `f.stat().st_mtime` (filesystem mtime) — but git does not preserve mtimes across a checkout, and M11's own GitHub Actions workflow restores `screen_results_archive/` from the `bot-state` branch via `git worktree` every run. Every restored file would have been stamped with "now," permanently reading as fresh regardless of how long the scheduler had actually been silent — the exact failure this check exists to catch, silently neutralized by the very persistence mechanism built alongside it in the same milestone. Fixed by deriving the age from the `run_date` embedded in each archive's filename (`screen_results_{run_date}.csv`, content/name — survives a git round-trip) instead of filesystem metadata. This only catches a *resumed*-after-a-gap scenario (the next run that does fire notices the gap); it cannot detect a *total, permanent* scheduler failure on its own — see the heartbeat workflow row below, which closes that gap independently. |
| **New, this milestone — GitHub Actions persistence for `state.json`/`journal.db` across ephemeral runners** | done, **verification pending** | 2026-07-22 — GitHub Actions runners are ephemeral; without this, every scheduled run would silently reset the two-strike counters and the append-only journal to empty, breaking reconciliation and the sell discipline without any error. Solved via a dedicated `bot-state` git branch used as a durable key-value store (not `actions/cache`, which evicts after 7 days — far shorter than one quarter): a `Prepare persisted bot-state branch` step restores `state.json`/`journal.db`/`munger.log`/`screen_results_archive/` from that branch before `python bot.py` runs, and a `Persist bot state` step (`if: always()`) commits the updated files back to it afterward, using `git worktree` rather than a second full checkout. Staff-engineer-reviewer confirmed the git mechanics themselves (orphan-branch bootstrap, `--allow-empty` commit, `set -e`-safe `[ -f ] &&` idioms) are sound; `permissions: contents: write` should be sufficient for the default `GITHUB_TOKEN` to push, **assuming no repo-level branch-protection ruleset blocks direct pushes** — worth a one-time manual check against the real repo settings, not inferable from the YAML alone. **This entire mechanism is unverified against real GitHub Actions as of this writing** — it cannot be tested locally; verify via `workflow_dispatch` (2+ manual runs, confirming the second one actually restores what the first left behind) before trusting the quarterly cron with it. **Blocked on tooling, not just time**: the coding agent's environment doesn't have the `gh` CLI installed, so it can't dispatch or inspect these runs itself (and won't work around that by scanning the environment for credentials) — this verification needs the user to trigger `workflow_dispatch` via GitHub's web UI (Actions tab, both `quarterly-run.yml` and `heartbeat.yml`) at least twice each, or to set up `gh` CLI access for the agent. |
| **New, this milestone (pm-reviewer finding) — GitHub auto-disables scheduled workflows after 60 days of repo inactivity** **(the ">130 days" threshold and monthly heartbeat cadence below are superseded, see M12: threshold now 3 days, heartbeat now runs daily)** | done, **verification pending** | 2026-07-22 — this project's ~91-day quarterly cadence is longer than GitHub's 60-day auto-disable window; if the only repo activity between quarters were the `bot-state` branch commit (once every ~91 days, itself past that window), `quarterly-run.yml`'s schedule could silently deactivate with no alert. This is also, independently, the "total scheduler failure" gap the data-freshness row above explicitly couldn't close (nothing inside `bot.py` can observe the scheduler never firing at all). Fixed with a second, monthly workflow, `.github/workflows/heartbeat.yml`, which independently checks `quarterly-run.yml`'s own GitHub Actions run history via `gh run list` — if the last recorded run is >130 days old, it fails (same GH-failure-email channel as everything else). **Verified GitHub's actual policy before relying on it (pm-reviewer caught this)**: GitHub's docs state only "scheduled workflows are automatically disabled when no repository activity has occurred in 60 days" — they do not specify whether a scheduled workflow merely *executing* counts as activity, and neither does the community discussion checked; this is genuinely as ambiguous as M8's `notional`-on-limit-order documentation conflict. Rather than gamble on an unverified reading, `heartbeat.yml` now actually pushes a commit every month (to a dedicated `heartbeat` branch, not `main`, to avoid mixing automated noise into the human-curated history or hitting a branch-protection rule more likely to apply to `main` specifically) — an unambiguous git push counts as "activity" under any reading of the policy. **Also unverified against real GitHub Actions** — verify alongside the `bot-state` persistence mechanism above. |

## M12 — Paper trading validation

**First live invocation of `bot.py`'s full `run()`, 2026-07-23** (not yet
a quarter cycle, but the first real one): the user gave explicit
authorization ("Yes, run it fully autonomous," via `AskUserQuestion`, one
step after an initial bare "go" was correctly judged insufficient
authorization for this specific action and the run was stopped/redone
with unambiguous confirmation). Result: `bot.run()` fetched the live S&P
1500 universe, screened it, and **aborted safely at the fetch-fraction
check** (only 80.5% of tickers fetched cleanly against yfinance's rate
limiting today — below `MIN_UNIVERSE_FETCH_FRACTION`'s 0.90 — 6 buyable
tickers out of 1504 screened) — before the kill-switch check, before
`ExecutionModule` was even constructed, before any broker call. No
orders placed, `journal.db` untouched by this run (it was separately
created empty by an unrelated `report.py` invocation the same day — the
two are independent, confirmed by inspecting `journal.db`'s row count
directly: 0). This is exactly the safety behavior DESIGN.md 5 calls for
under degraded data conditions, working correctly on a real run against
the live paper account for the first time — a genuinely useful, if
inconclusive, data point: the abort path is proven live; the
buy/sell/journal path through `bot.run()` (as opposed to `execution.py`
called directly, see M8/M10) still isn't, since this run never reached
it. A lingering OS process from unkillable rate-limited worker threads
(the known `ThreadPoolExecutor` limitation from M2/M11) outlived the
logical run by a few minutes; harmless, self-resolved.

**Real bug found via GitHub Actions run history, 2026-07-25/26: a cold-start
deadlock.** A second real attempt (`quarterly-run.yml` manual dispatch,
`screen_only` unchecked, 2026-07-25) got past the fetch-fraction check this
time and reached the buy queue — 7 buyable tickers, `TARGET_POSITION_COUNT`
= 15, so each new position targets ~1/15 of the ~$100k paper account
(~$6,667); opening all 7 from zero holdings wanted ~$46,667, which exceeded
`GLOBAL_NOTIONAL_BUDGET_PCT`'s 25%-of-equity cap (~$25,000). Old behavior:
exceeding that budget aborted the *entire* run, so zero orders were placed
— and since nothing gets bought on abort, holdings stay at zero, so the
next run would build the identical over-budget queue and abort again,
forever. **Fixed 2026-07-26** (user request, "let's start trading our
selected tickers on paper money"): `portfolio.generate_buy_queue` now
self-limits its own notional to the run budget as it builds the queue, and
`bot.py`'s `_cap_buy_orders_to_budget` truncates any remaining excess
(preserving priority order) and defers it to a later run instead of
aborting the whole run — liquidations are never truncated. 242/242 tests
passing (new coverage: the drift band, the notional self-limit against
the exact real 7-candidates/$100k scenario above, the truncate/defer
helper). DESIGN.md §1/§3.4/§4/§5/§6/§8/§9 updated to match
(pm-reviewer-approved); staff-engineer-reviewer pass on the code pending
before push.

**Same session: cadence changed quarterly → daily** (user request, "it
should rebalance daily on whatever its current results are, within a
margin of error"). New `config.REBALANCE_DRIFT_BAND_PCT` (10%) is the
"margin of error" — a top-up only fires once a holding is more than that
far below its own target dollar value, so ordinary daily price noise
doesn't trigger a trade on its own. `quarterly-run.yml` renamed to
`daily-trade.yml`, cron changed from `15 1,4,7,10` to daily at 14:00 UTC
(staggered an hour after `daily-screen.yml`'s 13:00 UTC to avoid both
hitting yfinance/Wikipedia simultaneously). `heartbeat.yml`'s dead-man's
-switch threshold shrunk from 130 days (quarterly + buffer) to 3 days
(daily + buffer); `config.DATA_FRESHNESS_MAX_HOURS` shrunk from `130*24`
to `48` for the same reason. **Not yet done as of this entry:** the
renamed/re-cadenced workflow has not yet had its first real run under the
new code — see the row below.

| Task | Status | Date / Notes |
|---|---|---|
| **First real run of the cold-start fix + daily cadence, via manual `workflow_dispatch` — succeeded exactly as designed** | done | 2026-07-27 — run [`30229531560`](https://github.com/jimmyokusa/munger/actions/runs/30229531560) (`screen_only` unchecked). Screened 1503 tickers, 10 buyable; **4 real buy orders placed, 0 liquidations, 0 alerts of any kind** (confirmed via `journal.db`, downloaded from the run's artifact): TROW $6,666.67 (score 79.6), NMIH $6,666.67 (score 79.3), LULU $6,666.67 (score 75.7), HIG $5,000.00 (score 68.4, trimmed to fit remaining budget) — total **$25,000.01, ≈25% of equity**, the notional budget cap binding exactly as intended instead of the old all-or-nothing abort (the 2026-07-25 incident above, same shape of scenario: several buyable candidates from a cold start). The other 6 buyable candidates were correctly left for a later run (not deferred-with-alert, since the budget bound *before* the queue was even built this time — `generate_buy_queue`'s self-limit, not `bot.py`'s backstop, did the capping here). First-ever live order submissions from this codebase's trading path. |
| **First scheduled (unattended) run — `_cap_buy_orders_to_budget`'s defer-and-alert path confirmed live** | done | 2026-07-27 — run [`30284674051`](https://github.com/jimmyokusa/munger/actions/runs/30284674051), the cron's first automatic fire (not a manual `workflow_dispatch`). `bot.py` correctly deferred one buy order (`SIG`) to fit the notional budget and alerted by name — exactly the designed behavior, not a bug, though it does mean the run shows as "failed" in GitHub's UI (this project's own established "any alert exits 1" convention, not new here). The one real problem this run surfaced was unrelated to trading logic: the "Upload P&L snapshot to GCS" step failed on a permissions error — see the IAM row above for the fix. |
| Observe the initial convergence window (~1 week from cold start) | todo | Per DESIGN.md §6's revised testing plan: deferred-order alerts on most runs during this window are *expected* (a cold start ramping in over several days), not themselves a bad sign — record daily outcomes here so "expected ramp-up" isn't quietly indistinguishable from "actually stuck" without a written trail. |
| Observe steady state (≥1 month post-convergence), superseding "run a full quarter cycle" | todo | DESIGN.md §6 revision: the daily-cadence testing bar is now "most days place zero orders of either kind" once converged, checked over a month, not a full quarter. The same symbol deferred on many *consecutive* days after the convergence window ended would mean the budget isn't actually converging (a real bug), not healthy behavior — that's the concrete signal to investigate. |
| No persisted, queryable record of deferred orders | todo | 2026-07-26 (staff-engineer-reviewer) — DESIGN.md §6 names a specific falsification signal ("the same symbol deferred on many consecutive days") but nothing computes it: deferred symbols today exist only as one log line per run (`bot._alert` -> stdout/GH annotation), not written to `journal.db` or anywhere else queryable. Detecting the §6 signal currently requires a human to manually diff several days of separate GitHub Actions run logs. Not fixed yet — options: a `journal.py` table for deferrals (mirrors `record_order`), or a simpler per-symbol counter in `state.json`. Worth doing before leaning on the convergence-window observation above as a real signal rather than a manual chore. |
| Daily cadence amplifies the known `state.json`/`journal.db` persist-step timeout risk ~90x | todo | 2026-07-26 (staff-engineer-reviewer) — `daily-trade.yml`'s "Persist bot state" step is `if: always()` but the workflow's `timeout-minutes: 45` can still cut it off mid-push, already called out in the workflow's own comments as a "bounded, known risk (spurious reconciliation warnings next run, not corruption)." That assessment was made at quarterly cadence (~4 exposures/year); daily cadence means ~365 exposures/year, and each occurrence can now also mean a liquidation's journal entry is lost for that run even though the trade executed on the broker. Not fixed — re-confirm this residual risk is still acceptable at the new frequency (it may well be, given "not corruption," just re-decide deliberately rather than let the old quarterly-era assessment carry over unexamined), or harden the persist step (e.g. a retry, or extending the timeout specifically around that step). |
| Audit journal: near-zero sells | todo | pass criterion unchanged: sells stay rare; any more than a documented two-strike justification per ticker in the journal is a flag. Record actual count + pass/fail here. |
| Audit journal: buys concentrated at top of score ranking | todo | pass criterion: new-position buys are drawn from the top half of that run's buyable score ranking, with any exception explained. Record actual result + pass/fail here. |
| Audit journal: weights landing near target | todo | pass criterion: each position's weight within ±2% of its 1/15 target once converged (allow more slack during the initial ramp-up window above). Record actual result + pass/fail here. |
| **Go/no-go: enable live trading** | todo | per DESIGN.md's PM Recommendations — at least two weeks reviewing `journal.db` output before flipping `paper`→`live` (now measured against the daily-cadence steady-state window above, not a quarter cycle). Record the decision, date, and reasoning here; if any M12 audit above failed, this defaults to no-go until re-observed. |

## M13 — HTML report (added 2026-07-23, user request)

Not part of the original M0–M12 build order — the user asked mid-session
for a simple web front end showing which stocks were picked and the
reasoning, so the pattern established elsewhere (DESIGN.md documents the
rationale before/alongside the code, TASKS.md tracks the work log) is
applied here too. See DESIGN.md §3.7.

| Task | Status | Date / Notes |
|---|---|---|
| `report.py` — static HTML generator, no server | done | 2026-07-23 — reads `screen_results.csv` + `journal.db`, writes `index.html`/`tickers.html` to `config.REPORT_DIR` (gitignored, regenerated on demand via `python report.py`; not wired into `bot.py`'s own run, since generating a report is a display concern, not a trading decision). |
| `index.html` — current picks with an expandable reason/metrics panel per ticker | done | 2026-07-23 — uses native HTML `<details>`/`<summary>` for the expand/collapse (the user's "dropdowns" ask), no JS needed for that part. Shows the journal's reason string, notional, timestamp, and the full metrics row (market cap, P/E, ROE, etc.) that produced that ticker's score. |
| `tickers.html` — every other screened ticker, sortable/filterable | done | 2026-07-23 — vanilla JS (no framework/build step): click a column header to sort, type in the filter box to narrow by symbol. Held tickers (already on `index.html`) are excluded here so the two pages don't duplicate the same ticker. |
| `journal.get_holdings_detail()` — new function | done | 2026-07-23 — same "most recent action per symbol" logic as the existing `get_expected_holdings()`, but returns the full row (reason, notional, timestamp) instead of just the symbol set, since the report needs to display *why*, not just *that*. |
| **Real bug found and fixed via this module's own tests**: numpy `int64` silently treated as non-numeric | done | 2026-07-23 — `_format_metric`'s first draft checked `isinstance(value, (int, float))`, which is `True` for numpy's `float64` (it subclasses Python's `float`) but `False` for numpy's `int64` (it does not, on the numpy version installed here) — so any whole-number CSV column (e.g. `market_cap`, which pandas infers as an integer dtype when every value in a test fixture happens to be a whole number) silently rendered as "—" instead of its real value. Caught by `test_generate_report_shows_a_pick_with_its_reason_and_metrics` failing, not by inspection. Fixed with a shared `_is_numeric()` helper (a proper `typing.TypeGuard`, so mypy narrows the type afterward instead of needing a `# type: ignore`) checking `(int, float, np.integer, np.floating)` and rejecting `NaN`. |
| Unit tests (`tests/test_report.py`) | done | 2026-07-23 — 7 tests: empty-state rendering (no picks yet), a pick with its full reason/metrics/notional shown, held tickers excluded from `tickers.html`, HTML-escaping of reason strings (a `<script>` tag in a `reason` string must not render unescaped — reason strings are internally generated today, but this is cheap defense in depth), and `_format_metric`'s missing/NaN/dollar/percent formatting, including the numpy-int64 regression above. `numpy` added to `requirements.txt` as a direct, pinned dependency (previously only a transitive one via `pandas`) since it's now imported directly. |
| `report/` added to `.gitignore` | done | 2026-07-23 — generated output, same treatment as `screen_results_archive/`/`munger.log`; regenerated from committed source data (`screen_results.csv`, `journal.db`), not itself a permanent artifact. |
| **User has opened the actual rendered report and confirmed it satisfies the request** | todo | pm-reviewer noted (2026-07-23): every row above is verified by unit tests, which is a different bar from "the user finds this usable" for a display feature built to a specific ask. The report is now running locally (see the Docker row below) and the user has been given the URL, but hasn't yet explicitly confirmed it satisfies the request — not marking this done on the assistant's own say-so. |
| **Exposure/access-control note for once live trading begins** | todo | pm-reviewer noted (2026-07-23): the report currently shows paper-account picks/notionals only; DESIGN.md §3.7 scopes it as local/static/no-server, which is fine today. Revisit before M12's paper→live go/no-go flips — the same report would then display real position sizes, and nothing currently states it must stay local-only (e.g., never published to a public GitHub Pages site without deliberately deciding that's acceptable). |
| **Second review round (staff-engineer-reviewer) — 5 more real issues found and fixed** | done | 2026-07-23 — (1) `bool(row["buyable"])` mistreated malformed data: a CSV column with any missing cell can't stay bool dtype in pandas, so clean cells fall back to literal strings, and `bool("False")` is `True` — a row whose own displayed text says "False" would have rendered un-dimmed as if buyable. Fixed with a proper `_is_buyable()` string/bool-aware parser (conservative default: anything not unambiguously `True` counts as not-buyable). (2) `tickers.html`'s column-sort silently sorted dollar-formatted columns (Market Cap, Free Cash Flow) as *text*, since JS's `parseFloat("$2.50B")` is `NaN` — fixed with a `data-sort` attribute on numeric cells carrying the raw value, which the sort comparator now prefers over parsing displayed text. (3) `index.html`/`tickers.html` were written with a plain `Path.write_text()`, not the temp-file+rename pattern this codebase uses elsewhere (`StateTracker.save()`) — a crash mid-write could leave a truncated page on disk indefinitely; fixed with a new `_write_text_atomically()` helper. (4) The same non-atomic-write risk existed in `screener.py`'s own `run_screen()` (`results.to_csv(...)` directly to the final path) — since `report.py` can run standalone at any time, including mid-write by a scheduled `bot.py`, a concurrent read could see a torn CSV; fixed with a matching `_write_csv_atomically()` helper in `screener.py`, used at both of its two write sites (the normal path and the empty-tickers guard clause from M5/M11). (5) `journal.py`'s `_connect()` had no busy-timeout, so a `report.py` read racing a `record_order()` write would immediately raise `sqlite3.OperationalError: database is locked` instead of waiting briefly — fixed with `timeout=5.0` on the connection. `generate_report()` also now calls `journal.configure_logging()` and logs any unhandled exception via `logger.exception()` before re-raising (mirroring `bot.py`'s `__main__` pattern), so a crash here leaves a trace in `munger.log` instead of a bare traceback. Two new regression tests added (`test_is_buyable_handles_real_bools_and_pandas_object_dtype_strings`, `test_tickers_page_marks_a_string_valued_false_row_as_not_buyable`, `test_tickers_page_carries_raw_numeric_value_for_js_sorting`) — 181 tests passing total. |
| Deployed locally in Docker for the user to view | done | 2026-07-23 — `report.Dockerfile` (nginx:alpine serving `report/` as static files) + a new `.dockerignore` (the naive first build sent the *entire* repo, 388MB including the local `.env` with live Alpaca keys, as build context with no `.dockerignore` in place — fixed before running anything, context dropped to ~730KB). Hit an unrelated environment quirk while verifying: port 8080 had stale port-forwarding state from earlier, unrelated Docker/colima work in this workspace (`gvproxy` was answering some paths itself rather than routing to the container — confirmed via nginx's own access log showing zero of the failing requests) — resolved by using a fresh port (18080) rather than debugging `gvproxy` internals, out of scope for this feature. Verified both `index.html` and `tickers.html` serve correctly over the container. |
| Glass ("glassmorphism") visual redesign | done | 2026-07-23 — user feedback after viewing the deployed site. Translucent, blurred cards (`backdrop-filter: blur()`) over a soft gradient page background (`light-dark()`-aware for both themes), applied to the picks list, the metrics tables, and the ticker-table wrapper (blur applied to the container, not per-row/per-cell, to avoid a real performance cost across up to ~1500 rows). |
| Live progress bar while a screen is running (user request, replacing the plain "no picks yet" message) | done | 2026-07-23 — the user asked for something showing live progress ("[####------] 342/1504 tickers, currently checking: MSFT"), not just a better static empty-state message. Implemented as a genuinely live view: `data.py` gained thread-safe progress tracking (`_start_progress`/`_mark_ticker_started`/`_mark_ticker_done`/`_finish_progress`), writing a small `progress.json` directly into `config.REPORT_DIR` (not `report.py`'s job — `report.py` only generates the *static* page shell once; a separate polling `<script>` embedded in `index.html` fetches `progress.json` every 2s and updates the DOM live, so the same generated page reflects a run that starts long after the page was last regenerated). `fetch_all_metrics` gained an optional `phase` label (`"screening"` vs. `bot.py`'s `"holdings check"` call) purely for display. `fetch_metrics` was refactored into a thin wrapper (`_mark_ticker_started` → try/finally around the original logic, now `_fetch_metrics_inner`, unchanged) so every return path — success, retry-exhausted, or an unexpected exception — reliably marks the ticker done. |
| **Real concurrency bug found and fixed via this module's own test suite**: `_write_progress_file()`'s lock didn't cover the actual file write | done | 2026-07-23 — the first draft held `_progress_lock` only while building the JSON payload dict, then released it before the temp-file write + rename. Since every worker thread shares the same temp filename, two threads racing past that unlocked section could have one overwrite the other's temp file before renaming, and the loser's `Path.replace()` then raises `FileNotFoundError` — reproduced by `test_fetch_all_metrics_writes_live_progress_for_the_report` failing under the `ThreadPoolExecutor`'s real concurrency (not a mocked/sequential test). Fixed by extending the lock to cover the entire write+rename sequence, serializing all progress-file writes (acceptably cheap given they're small, infrequent relative to per-ticker network I/O). Verified stable across 5 repeated runs of the full `data.py` test suite after the fix. |
| Test isolation for the new progress file | done | 2026-07-23 — `tests/test_data.py` gained an autouse fixture redirecting `config.REPORT_DIR`/`config.PROGRESS_FILE_PATH` to `tmp_path`, since every `fetch_all_metrics`/`fetch_metrics` call now writes progress unconditionally; without it, tests would have written into this repo's real `report/` directory. `tests/test_bot.py`'s `fetch_all_metrics` mocks were updated (`lambda symbols:` → `lambda symbols, **_kwargs:`) since `bot.py`'s real call site now passes `phase="holdings check"`, which the old single-arg lambdas would have rejected with a `TypeError`. |
| Unit tests | done | 2026-07-23 — 3 new tests in `tests/test_data.py` (progress state transitions across started/done/finished, and `fetch_all_metrics` writing a correct final payload through the real thread pool), 1 new test in `tests/test_report.py` (the progress banner markup and polling script are present in generated `index.html`). 184 tests passing total. |
| **Third review round (staff-engineer-reviewer) on the live-progress concurrency code — most severe finding this milestone, fixed** | done | 2026-07-23 — the progress-tracking side channel (display-only) could affect real trading data: `_mark_ticker_started` ran *before* `fetch_metrics`'s `try` block, so a raise there (e.g. `config.REPORT_DIR` unwritable, disk full) would skip the real yfinance fetch entirely; worse, `_mark_ticker_done` runs in the `finally` block *after* a real fetch already succeeded, so a raise there would — via Python's finally-supersedes-return semantics — discard an already-fetched `Metrics` record and report the ticker as failed, purely because a cosmetic progress write failed. Fixed: `_write_progress_file()` now wraps its entire body in `try/except Exception: logger.warning(..., exc_info=True)` and never raises, so a filesystem fault on the progress side channel can never affect a real fetch's control flow or return value. New regression test `test_progress_write_failure_never_discards_a_successful_fetch` forces the write to raise and asserts `fetch_metrics` still returns the real, successfully-fetched result. **Also found**: the earlier concurrency test (`test_fetch_all_metrics_writes_live_progress_for_the_report`) only submitted 2 symbols against the 12-worker pool with no forced synchronization, so it could pass even if the lock-scope fix regressed — added `test_fetch_all_metrics_survives_sustained_thread_contention_on_progress_writes` (200 symbols, sustained contention across every worker for the whole batch) as a more reliable regression guard. **Known, deliberately-not-solved edge case**: `_finish_progress()` (now also clearing `in_flight` for cleaner state) can still be followed by a stale, still-running hung-worker thread (the pre-existing, already-accepted `ThreadPoolExecutor`-can't-be-killed limitation, TASKS.md M2/M11) calling `_mark_ticker_done` afterward, corrupting either the just-finished batch's counters or — if a new batch already started — the next batch's. Fixing this properly needs a per-batch generation tag, more machinery than a cosmetic progress display warrants; not solved here, since the underlying hung-worker case is already known, low-frequency, and display-only in its consequences. 186 tests passing total, stable across 5 repeated runs of the full suite. |
| User-reported: lots of NaN/`data_missing` visible in `tickers.html` — investigated and partially addressed | done | 2026-07-23 — broke down a real 1504-ticker run's `fail_reasons`: 293 were genuine `data_missing:fetch_failed` (yfinance rate-limiting), 372 were `data_missing:dividend_yield` (confirmed **not** a bug — Graham's classic defensive-investor criteria require an uninterrupted dividend record, so non-dividend payers like LULU are correctly excluded by design), and the rest (free_cash_flow, debt_to_equity, trailing_pe, current_ratio, return_on_equity) are per-field gaps in yfinance's own data for those specific tickers, not fetch errors. User chose: (1) tune the existing free retry/backoff logic rather than add a second (mostly paid, or too rate-limited at this scale) data source — `DATA_FETCH_THREAD_POOL_WORKERS` 12→6 (yfinance's rate limit is session-wide, not per-worker, so fewer concurrent requests should trigger it less often without losing real throughput once already limited), `DATA_RATE_LIMIT_COOLDOWN_SECONDS` 20→45 (20s wasn't enough given today's 80.5%-clean run, worse than M2's 90% retest), `DATA_FETCH_BATCH_TIMEOUT_SECONDS` 600→1200 (a quarterly batch job has no minute-level time pressure; more real wall-clock time to cycle through cooldowns is free). Documented as best-effort tuning against an undocumented, opaque provider limit, not a guaranteed fix. (2) Make the UI say explicitly when missing data is due to rate-limiting, not a permanent gap: `progress.json` gained `rate_limited_until`/`now` fields (from `data.py`'s existing shared-cooldown state), and `index.html`'s live progress banner now shows "paused, waiting out a shared yfinance rate limit (~Ns)" instead of a generic ticker name when that's the actual state. `tickers.html` gained a persistent explanatory note (visible even browsing a static, non-live report) whenever any row shows `fetch_failed`, plus a tooltip on the affected `fail_reasons` cells. Tests: 3 new (`test_progress_file_reports_an_active_rate_limit_cooldown`, `test_tickers_page_shows_a_note_when_any_row_has_fetch_failed`, `test_tickers_page_omits_the_fetch_failed_note_when_no_row_has_it`), plus an assertion added to the existing progress-banner test. 189 tests passing total. |

## M14 — Daily screen-only snapshot + report calendar (added 2026-07-24, user request)

A read-only daily job to build up richer report history than the quarterly
trading cadence alone produces, plus a calendar view over it. Screen-only
by construction — `daily_screen.py` never imports `execution.py`, so it is
architecturally incapable of placing an order. See DESIGN.md §3.7 (report)
and §4 (cadence).

| Task | Status | Date / Notes |
|---|---|---|
| `daily_screen.py` — screen-only daily run | done | 2026-07-24 — fetches the universe, runs `screener.run_screen`, archives via `journal.archive_screen_results`, regenerates the report. Deliberately imports neither `execution` nor `bot`; a test (`test_daily_screen_never_imports_execution`) asserts `execution` is absent from `sys.modules` after import, so the no-trading guarantee is verified, not just documented. The k3s CronJob wiring is **verified** (seed run completed, report serves). The `.github/workflows/daily-screen.yml` wiring is **code-complete but not yet clicked** — unlike M11's mechanisms (genuinely blocked on the user adding repo secrets), this workflow carries no Alpaca secrets and has no external prerequisite, so it can be verified today via a single `workflow_dispatch` run; not the same class of blocker as M11 (pm-reviewer correction). Interim note: until v2's cache (D1) lands, this daily job re-fetches the full ~1500-ticker universe every day and will run visibly degraded under yfinance throttling — the very pain v2 addresses. |
| Two independent daily-screen pipelines now exist (pm-reviewer — needs a stated relationship) | todo | 2026-07-24 — the k3s CronJob (verified, writing to the cluster's own PVC/report) and `.github/workflows/daily-screen.yml` (unverified, archiving to its own `daily-screen-archive` git branch) are two separate mechanisms producing two separate, disconnected report/calendar histories, with no documented relationship between them. Working intent, to be confirmed and written down once the GH Actions path is clicked: the GH Actions history is the durability fallback / off-cluster mirror of the calendar data (relevant until v2's D4/Litestream gives the cluster its own off-node durability), not a second independent source of truth an operator needs to reconcile by hand. |
| Fetch-quality gate on the daily archive (staff-engineer-reviewer) | done | 2026-07-24 — the trading path aborts a run that fetched too little of the universe (`bot.py`, `config.MIN_UNIVERSE_FETCH_FRACTION`), but `daily_screen` had no equivalent, so a throttled 80%-`fetch_failed` day was archived and rendered on the calendar identically to a genuine "nothing passed" day — polluting the permanent history. Fixed: promoted the fetch-fraction calc to `screener.fetched_fraction()` (single source of truth; `bot._fetched_fraction` now delegates, so the safety threshold can't drift), and `daily_screen` now **skips archiving** a sub-floor run (still regenerates the live report, whose M13 UI already flags `fetch_failed` rows). New test `test_run_skips_archive_when_fetch_quality_is_too_low`. |
| `report.py` — daily calendar view (`calendar.html`) | done | 2026-07-24 — month-grid calendar keyed off the archived `screen_results_*.csv` files, each day linking to that day's results and colored by whether any buyable names were found. `_load_daily_summaries()` reads only the `symbol`/`buyable` columns (`usecols`) so it scales to a year+ of archives cheaply, and requires both columns so a wrong-schema file is skipped rather than miscounted. |
| Two failing WIP tests fixed | done | 2026-07-24 — a malformed-file test failed because the summary loader keyed only on a `buyable` column (a junk file with a coincidental `buyable` column slipped through); fixed by requiring the real schema. A calendar-note test failed because the "never places orders" phrase wrapped across a source line so the contiguous substring wasn't present; reworded. |
| Self-contained report links (real bug) | done | 2026-07-24 — `calendar.html` (written under `REPORT_DIR`) linked to `screen_results_archive/...`, which is `REPORT_DIR`'s *sibling* under `BASE_DIR` — a browser serving `report/` as web root (local nginx, k3s, or the GH archive branch) could not resolve it. Fixed by having `generate_report()` copy the archive CSVs under `REPORT_DIR/screen_results_archive/` so the site is self-contained wherever it's served. |
| Unit + e2e tests | done | 2026-07-24 — `tests/test_daily_screen.py` (the no-execution guarantee + the full run() fetch→screen→archive→report sequence), plus calendar tests in `tests/test_report.py`. Full suite 195 passing. |

## Infra — Deploy to Raspberry Pi k3s cluster (added 2026-07-24, user request)

Bring munger up on a 4-node Raspberry Pi **k3s** cluster (arm64, ~900MB
RAM/node) as a self-updating stack: an in-cluster CI test job gating a
daily-screen CronJob + nginx report viewer. See the project skill
`.claude/skills/munger-workflow/SKILL.md` for the full CI/CD flow.

| Task | Status | Date / Notes |
|---|---|---|
| Cluster health fix (prereq) | done | 2026-07-24 — pi1/pi2/pi3 were `NotReady`: pi4 (control-plane) IP had drifted via DHCP (.227→.204) and every `k3s-agent` still pointed at the dead address (`K3S_URL` in `/etc/systemd/system/k3s-agent.service.env`). Repointed to .204, cleared the stale agent load-balancer cache, restarted. pi2 additionally had a leftover **microk8s** snap squatting kubelet port 10250 — stopped + disabled it. All 4 nodes now `Ready`. **Durable fix still open: pi4 needs a static IP/DHCP reservation** or this recurs. |
| `config.py` `MUNGER_DATA_DIR` env override | done | 2026-07-24 — writable runtime paths (report, archives, `journal.db`, `data_cache`, logs, state, kill-switch flag) now live under a `DATA_DIR` that defaults to the code dir (unchanged local/test behavior) but can be repointed at a mounted PVC. Enables persisting cluster output. |
| `deploy/Dockerfile` (+ `Dockerfile.dockerignore`) — arm64 app image | done | 2026-07-24 — `python:3.11-slim` (prebuilt aarch64 wheels, no compiler needed), runs as non-root uid 1000, `MUNGER_DATA_DIR=/data`. Per-Dockerfile ignore (BuildKit) keeps its context separate from the existing `report.Dockerfile`; ships source+tests, excludes `.env`/state. Required installing the `docker-buildx` plugin locally. Smoke-tested: no secrets baked in, `execution` not imported. |
| `deploy/k8s/` manifests | done | 2026-07-24 — namespace, local-path PVC (RWO, node-pinned), nginx report Deployment+Service (NodePort 30080, `Recreate` strategy for the RWO PVC), daily-screen CronJob (no Alpaca secrets — screen-only), and an in-cluster pytest CI Job. Workloads pinned to pi1 via `nodeSelector` since local-path is node-local. |
| `deploy/build-and-deploy.sh` — one-command pipeline | done | 2026-07-24 — build arm64 image → `docker save | ssh … | k3s ctr images import` (no registry; ~5min full transfer to a Pi) → apply+wait on the CI Job (abort on fail) → apply CD workloads → seed first screen. kubectl runs on pi4 over SSH; image imported to pi1. |
| CI gate caught two real issues | done | 2026-07-24 — the first in-cluster pytest run failed (2 tests) where the local run passed: `DATA_RAW_CACHE_DIR` was still `BASE_DIR`-relative (unwritable as non-root in-container) — moved under `DATA_DIR`; and two `test_data.py` tests didn't isolate that cache dir — added it to the module's autouse fixture. Re-ran: **195 passed in-cluster** on the arm64 image. |
| Deployed + serving (verified) | done | 2026-07-24 — namespace/PVC/CI/CD applied; PVC Bound; nginx Running; the seed screen ran in-cluster to completion (1503 tickers, 8 buyable, ~11min under throttling) and wrote report+archives to the PVC. Report **verified serving end-to-end**: `http://192.168.20.204:30080` (and .201/.202/.203) returns HTTP 200 with real content for `index.html`/`calendar.html`/`tickers.html`. |
| Real bug: Service silently dropped on apply | done | 2026-07-24 — the initial NodePort probe returned `HTTP 000`/blank because the Service was never created: `kubectl apply` was fed several manifest files concatenated with no `---` between them, so the trailing Service doc merged (key-clobbered) into the CronJob doc and vanished (apply output showed deployment+cronjob, no service). Fixed by applying `20-report-web.yaml` alone, and fixed `build-and-deploy.sh` to apply each manifest file separately so a fresh run can't repeat it. |
| **pi4 needs a static IP / DHCP reservation** | todo | 2026-07-24 — **whole-cluster SPOF, pulled out of the cluster-health row (pm-reviewer) so it isn't lost as a footnote.** pi4's DHCP IP drifting is exactly what took all three workers `NotReady` (agents hard-code `K3S_URL` at pi4's IP). Until pi4 has a reservation, any lease change silently breaks the cluster and the deployment. **Owner/trigger:** whoever next touches the router/DHCP config; close before the next intentional network change (new router, firmware update) that could reassign leases. |
| Cluster `journal.db` has no trades → "Current picks" page was empty | done | 2026-07-24 (staff-engineer-reviewer; user "implement the todo") — the cluster runs only the screen-only daily job (no `bot.py` quarterly CronJob, by design), so its PVC `journal.db` has no holdings and `index.html` always showed "No current picks yet." Took the "scope the page to what the daily job knows" option: when no positions are journaled, the home page now shows the latest screen's **buyable candidates** (`_render_candidate`/`_render_candidates_or_empty`), honestly labeled "buyable in the latest screen — not a held position / screen output, not holdings," ranked by score; falls back to the original empty state only when the screen finds nothing buyable. Tests `test_index_shows_buyable_candidates_when_no_positions_are_journaled` + `test_index_falls_back_to_empty_state_when_screen_finds_nothing_buyable`. |
| PVC has no backup; pi1 loss is total | todo | 2026-07-24 (staff-engineer-reviewer) — daily archives are a **non-regenerable** time series on node-local, unreplicated local-path storage pinned to pi1; an SD-card/disk failure loses the entire calendar history and takes the report offline. This is what v2's D4 (Litestream) solves. **Owner/trigger:** closes when v2's D4 lands (see v2 section below); until then, the GH Actions daily-screen archive branch (once verified) is the closest thing to an off-node copy. |
| Unbounded archive growth vs fixed 2Gi PVC | todo | 2026-07-24 (staff-engineer-reviewer) — one archive CSV/day forever with no retention, against a 2Gi PVC; would eventually `ENOSPC` mid-write. `_copy_archives_into_report` was made incremental (only new/changed files copied) so it's no longer O(all history) per run, but a retention/pruning policy is still needed. **Owner/trigger:** revisit once the PVC crosses ~50% utilization (`kubectl -n munger exec deploy/munger-report -- df -h /usr/share/nginx/html`), or before v2's D4 durability work, whichever comes first. |

## v2 — Distributed screening architecture (design drafted 2026-07-24)

Rearchitect the heavy, rate-limited data-fetch stage as a distributed
scatter-gather across all 4 nodes, behind a shared fundamentals cache.
**Design only so far** — see [DESIGN_DISTRIBUTED.md](DESIGN_DISTRIBUTED.md).
Not a microservices decomposition (rejected on 900MB nodes); execution
stays centralized and safety-gated.

| Task | Status | Date / Notes |
|---|---|---|
| `DESIGN_DISTRIBUTED.md` drafted | done | 2026-07-24 — coordinator shards the universe → worker Indexed Job (1/node, 4 independent rate-limit IPs) fetches+scores through a Redis fundamentals cache → workers POST partials → coordinator reduces + executes centrally. Redis for cache/lock/snapshot/progress; Litestream streams `journal.db` off-cluster for durability. Failure modes degrade to screen-only via the existing fetch-fraction abort. Milestones D0–D5. |
| D0 — validate the per-IP rate-limit assumption | todo | **Gate for everything past the cache.** Empirically confirm yfinance throttles per source IP by running simultaneous fetch bursts from two Pis. If the limit is shared, the parallel lever collapses and only the cache (D1) ships, on the monolith. |
| D1 — fundamentals cache (single-node) | todo | Redis-backed `cache.py` wired through `data.fetch_metrics` (get→miss→fetch→set+TTL); the highest-value, lowest-risk piece; delivers the incremental-daily-screen win with zero distribution risk. Ships regardless of D0. |
| D1.5 — measure the cache payoff (gate) | todo | pm-reviewer — before building D2+, confirm the realized cache hit-rate actually drops ordinary-day cache-misses to near zero. If D1 alone makes daily screens near-instant, the distributed build's marginal value collapses and we stop here. Second load-bearing gate alongside D0. |
| D2 → D3a/D3b → D4 → D5a/D5b | todo | Per DESIGN_DISTRIBUTED.md §10 (re-scoped after pm review): D3 split into D3a (coordinator happy-path, manual trigger — isolates k8s-API/RBAC risk) + D3b (run-lock/token/idempotency hardening); D5 split into D5a (apply+validate NetworkPolicies/token/anti-affinity while monolith is still default) + D5b (flip the default). Monolith remains the documented rollback until D5b. |
| Staff-engineer + PM review of this design | done | 2026-07-24 — both ran (`munger-workflow` governance). pm-reviewer: split D3 (too big), add the D1.5 cache-payoff gate, fix D0 "gate" wording — all applied. staff-engineer-reviewer: fixed the §5 run-lock TTL double-execution trap (fencing/renewal) and the Redis-down "slower but correct" contradiction (it's fail-closed), and noted the `:latest` rollback caveat. Implementation-time review of each D-milestone still required as it's built. |
| `warren-buffett` investment-thesis agent added | done | 2026-07-24 (user request) — `.claude/agents/warren-buffett.md`: reviews the *investment* logic (not code/scope) from a value-investing lens — moat/business quality vs. statistically-cheap "cigar butts", circle of competence, margin of safety, concentration, turnover. Read-only. Complements staff-engineer (code) + pm (scope) as the third reviewer; intended to gate changes to the screening thesis (DESIGN.md §1–3, `screener.py`, `config.py` thresholds). |

## M15 — Report UX overhaul (added 2026-07-25, user request)

User request, scoped into 3 independently-shippable slices (split from one
large spec at the user's direction, matching this project's small-
milestone discipline): visual/explainability polish, then interactive
tools, then a subscribable feed. `report.py` stays static/no-server/no-
build-step per DESIGN.md §3.7 — every addition here is either plain
HTML+CSS or vanilla JS reading files already on disk, nothing new to run
or maintain.

| Task | Status | Date / Notes |
|---|---|---|
| Slice 1 — visual badges, metric tooltips, methodology drawer, responsive metrics table | done | 2026-07-25 — `_render_badges()`: green "Zero debt" (0 <= debt/equity <= 0 — explicitly guarded against a negative debt/equity, i.e. negative book equity, wrongly qualifying; see the fix note below) and "High ROE" (>= 20%), blue "Dividend" (yield >= 3%) on each pick/candidate card; thresholds are display-only informational copy, deliberately not read from `config.py` so they can't be mistaken for tunable screening parameters (they mirror DESIGN.md 3.3's existing gate/score constants). `_METRIC_TOOLTIPS`: a plain-language, Munger/Graham-context explanation per metric, shown via `title` on both the pick metrics table and the `tickers.html` column headers. `_render_methodology_drawer()`: a collapsed-by-default `<details>` (same zero-JS pattern as a pick card) walking through the Stage 1 (7 Graham gates) / Stage 2 (Munger quality floor) split from DESIGN.md §3.3, grouped as Valuation/Financial health/Profitability/Cash flow per the user's spec. `_render_metrics_table` output wrapped in a new `.metrics-wrap { overflow-x: auto }` div (existing `.table-wrap` already covered `tickers.html`; this closes the one table that wasn't wrapped). Note: light/dark theming and the responsive `.table-wrap` scroll pattern already existed pre-M15 (M13's `light-dark()` CSS + `color-scheme`, and M14's `.table-wrap` on `tickers.html`) — not rebuilt, just extended to the one gap. staff-engineer-reviewer (round 2) caught two real issues in the first draft, both fixed: (1) the badge condition as first written (`debt_to_equity <= 0.0`) would award "Zero debt" to a *negative* debt/equity — negative book equity, a red flag, not "no debt" — the exact trap `screener.py`'s own gate/score logic already guards against; fixed to require `>= 0` too, plus a regression test. (2) the drawer's first draft said "7 Graham entry gates" but only listed 6 and blurred Stage 1 gates together with Stage 2's quality floor (ROE/gross margin/FCF, which are not gates); fixed to list all 7 (including the optional, off-by-default dividend gate) and explicitly separate the two stages. 9 new tests total (badge combinations incl. none/missing-row/negative-debt, an integration check that `_render_pick`/`_render_candidate` actually emit the badges, methodology drawer presence, tooltip titles); 215 passing, ruff/mypy clean. **Still open:** no one has looked at the rendered page yet — M13 (row 354) set the precedent of keeping "user confirmed it looks right" as its own checkpoint even after code review passed; same here (see the row below). |
| Engineering visual smoke-test of all 3 slices (agent-run, not user sign-off) | done | 2026-07-26 — rendered a fresh local `report.generate_report()` (existing `screen_results.csv`/archive from 2026-07-23, so this was a rendering check, not a fresh-data check) and opened it in-browser via a local static server (`file://` doesn't load in the browser tooling used). Badges render correctly inside each pick's `<details>` card (e.g. TROW showed "Dividend"; HIG/INSW showed "High ROE"), metric tooltips present as `title` attrs on both the pick metrics table and `tickers.html`'s column headers, the methodology drawer expands to correctly-separated Stage 1 (7 Graham gates, grouped Valuation/Financial health/Profitability) / Stage 2 (Munger quality floor) sections, SEC EDGAR/Finviz research links present per card, Copy JSON/Export CSV buttons render near the picks header, `feed.json` parses as valid JSON and `rss.xml` as valid XML (`xml.etree.ElementTree`). **Not exercised:** the metrics table's responsive/narrow-viewport behavior (the fourth named slice-1 deliverable) — only inspected at default desktop width. This is real, useful evidence the code works, but per pm-reviewer (below) it does **not** satisfy the user-confirmation checkpoint — that requires the actual user, not an agent, looking at the result. |
| **User visual confirmation of slice 1 still outstanding** | todo | 2026-07-25, reaffirmed 2026-07-26 (pm-reviewer, precedent: M13 row 354) — pm-reviewer explicitly rejected marking this `done` on the strength of the agent-run smoke-test above: M13 row 354 established this checkpoint precisely because code-review/test confidence is "a different bar from 'the user finds this usable'" — self-certification by the agent doing the work is the exact thing it guards against. **Doubly blocked right now**: even if the user opens `gramunger.com` today to do this, they won't see any of M15 — see the Cloud Run staleness row below. Real user sign-off on the live site can't meaningfully happen until that redeploy lands; a local render (see row above) can't substitute for it per this project's own precedent. |
| **Cloud Run image is stale — predates all of M15** | todo | 2026-07-26 — found while doing the smoke-test above: `gramunger.com`'s live `index.html` has no badges, no methodology drawer, no research links, no export buttons — just bare ticker + score. The `daily-screen`/`report-web` images were last rebuilt 2026-07-25 ~18:54 UTC for the two GCS bug fixes only; M15 slice 1 (`c96eb90`) and slices 2/3 (`b43f3de`) were committed after that rebuild, so the deployed image never picked them up, even though `daily-screen` has run successfully since (it's regenerating the old report code's output, not stale *data*). **Owner/trigger:** next engineering session, before claiming the user-confirmation row above is even attemptable — rebuild + redeploy both Cloud Run targets (`report-web`, `daily-screen`). Relates to the "no one-command Cloud Run deploy script" and "fully automated CI/CD" rows below — this is the concrete, immediate consequence of neither existing yet: nothing in this project's workflow currently checks whether a milestone marked `done` in this file has actually reached the deployed target, so "done" here has meant "code done," not "shipped," without that gap being visible at a glance until now. |
| Slice 2 — export buttons + outbound research links | done | 2026-07-26 — `_render_research_links()`: per-ticker "SEC EDGAR" (the generic company-search page — the user's spec gave that exact URL, which isn't itself ticker-parameterizable, unlike Finviz) and "Finviz" (`?t={TICKER}`) links on every pick/candidate card, `target="_blank" rel="noopener noreferrer"`. `_export_rows()` + `_render_export_controls()`: Copy-JSON/Export-CSV buttons near the picks header, backed by the *same* row data driving the visible cards (`_export_rows` mirrors `_render_index`'s own picks-vs-candidates branch, via a new shared `_buyable_sorted()` helper factored out of `_render_candidates_or_empty` so the two can't silently diverge) — vanilla JS only, `navigator.clipboard`/`Blob`+`<a download>`, embedded via `<script type="application/json">`. **Real bug caught by this module's own existing test** (`test_generate_report_escapes_html_in_reason_strings`, unmodified): the first draft embedded that JSON unescaped, so a `reason` string containing `</script>` would close the script tag early and let arbitrary following markup render as real HTML — fixed by escaping `<` to its JSON unicode form before embedding (valid JSON either way; `JSON.parse` reads it back correctly). staff-engineer-reviewer (round 2): confirmed the escaping fix is correct and sufficient, confirmed no other new embedding site needed it, confirmed `_export_rows` genuinely can't diverge from what's rendered (traced both code paths); found the CSV-export JS's quoting regex missed a bare `\r` (a lone-CR value could misalign a row for some CSV consumers) — fixed (`/[",\r\n]/`). 12 new tests; 230 passing, ruff/mypy clean. |
| Slice 3 — JSON/RSS feed | done | 2026-07-26 — `feed.json` (JSON Feed 1.1) and `rss.xml` (RSS 2.0), written by `generate_report()` alongside the HTML pages (no separate build step, per DESIGN.md §3.7). One item per recently-archived day (`_recent_daily_feed_entries()`, capped at the new `config.FEED_MAX_ITEMS = 60` so the per-generation cost and the feed itself don't grow unbounded against the archive's already-known unbounded-growth TODO), each listing that day's actual buyable ticker symbols (read from that day's archive CSV), not just a count — so subscribing surfaces *which* names newly qualified. New `config.REPORT_BASE_URL` (env `MUNGER_REPORT_BASE_URL`, default `""` — unchanged local/k3s behavior, since k3s's report has no stable public URL) builds absolute feed links; falls back to a relative `.` base rather than fabricating a domain when unset. **Not yet set on the Cloud Run deployment** — needs `MUNGER_REPORT_BASE_URL=https://gramunger.com` added to the `daily-screen` Job's env for the feed to carry absolute (spec-correct) URLs there; until then it'll generate with relative links. Per the earlier caveat: built and tested, but hold off announcing/promoting the feed publicly until `daily-screen` has more than the current 2 successful runs behind it — a feed reader caching a broken/empty early snapshot is a worse first impression than a visitor just finding one. **staff-engineer-reviewer (round 2) found and fixed 3 real issues**, all before push: (1) `_render_feed_rss` used the HTML5 entity `&mdash;` in a bare XML document with no DOCTYPE to declare it — invalid XML, rejected outright by any strict parser/feed validator (caught because the original test only substring-checked the output rather than parsing it; the test now round-trips through `xml.etree.ElementTree`); fixed to the numeric entity `&#8212;`. (2) `calendar.html`, `feed.json`, and `rss.xml` each independently re-globbed and re-read the entire (unbounded-growth) archive directory on every `generate_report()` call — `_load_daily_summaries()`/`_recent_daily_feed_entries()` are now computed once in `generate_report()` and shared via optional params. (3) a day whose archive CSV couldn't be re-read with the `score` column (legacy schema, deleted, torn write) was silently coerced to "0 buyable candidates," indistinguishable from a real empty-but-valid day — fixed to skip that day from the feed entirely rather than fabricate a misleading data point. |
| **`MUNGER_REPORT_BASE_URL` not yet set on Cloud Run** | todo | 2026-07-26 — set to `https://gramunger.com` on the `daily-screen` Job's env (mirrors how `MUNGER_PROGRESS_WRITE_MIN_INTERVAL_SECONDS` was added there) so `feed.json`/`rss.xml` carry absolute URLs on the one deployment that actually has a public domain. |
| `gramunger.com` registered + mapped to Cloud Run `report-web` | done | 2026-07-25 (user decision) — both the apex and `www` are now live: `https://gramunger.com/` and `https://www.gramunger.com/` both return HTTP 200 (`www`'s managed TLS cert finished provisioning after the initial check, which had it still pending). Consistent with the dev/prod decision below: Cloud Run is prod, so it's the target that gets the public domain. |

## Infra — Deploy to Google Cloud Run (added 2026-07-25)

**Working end-to-end as of 2026-07-26**, after the two real bugs below
were fixed, the images rebuilt, and a Cloud Scheduler trigger wired up:
two consecutive `daily-screen` executions have now succeeded
(`daily-screen-pdh67`, 2026-07-25, and `daily-screen-w9w7w`, the first
scheduled run, 2026-07-26 13:00 UTC), each archiving a real ~1503-ticker
screen and regenerating the report — `gramunger.com` currently serves
that real, current data (7 buyable candidates as of the latest run), not
stale or placeholder content. A second deployment target, stood up
alongside the (working) Pi k3s cluster: GCP project `munger-503515`
(region `us-central1`), same image (screen-only `daily_screen.py`, never
importing `execution`) and same `MUNGER_DATA_DIR`-relocation mechanism as
the k3s deploy, but writable state lives on a GCS bucket mounted via
`gcsfuse` instead of a k8s PVC. It was picked up already mid-flight this
session, before its role was decided — see the "relationship" row below,
now resolved: **k3s is dev, Cloud Run is prod**, promoted after a 24h soak.

| Task | Status | Date / Notes |
|---|---|---|
| **Relationship between the two deployments — decided (user, 2026-07-26)** | done | k3s (Pi cluster) is **dev**; Cloud Run is **prod**. Pipeline: ship to k3s first, let it run for **24 hours** as the dev soak, then promote to Cloud Run/`gramunger.com`. Resolves the pm-reviewer "which target is canonical" gate below — closes that open item. **Not yet built:** nothing currently enforces or automates this sequence (both targets today run independently on their own schedules/images, with no promotion mechanism between them) — see the new row below. |
| No promotion pipeline (dev k3s → 24h soak → prod Cloud Run) built yet | todo | 2026-07-26 — the decided pipeline above is not yet implemented: there's no shared image/tag between the two targets, no automated 24h gate, and no promotion script (mirroring `deploy/build-and-deploy.sh`'s CI-then-CD pattern, but promoting a k3s-verified image/build to Cloud Run instead of building each target independently by hand). Until this exists, keep treating each target's deploys as manual and independent — don't assume a Cloud Run deploy is "promoted from a soaked k3s build" yet, because nothing currently guarantees that. |
| **Fully automated CI/CD for the dev→prod pipeline (user request, 2026-07-26)** | todo | User wants the k3s(dev)→24h-soak→Cloud Run(prod) pipeline above to be fully automated end-to-end, not just a documented manual sequence. This is architecturally significant enough (automated promotion to a public, real-domain prod target) to warrant its own design pass before implementation — per this project's own governance, changes of this weight get a `staff-engineer-reviewer` pass on the design, not just the code, given the operational risk of a bad build auto-promoting to `gramunger.com`. `DESIGN_CD_PIPELINE.md` (2026-07-26) is a draft covering this — explicitly not decided yet, several open questions for the user. **2026-08-06 finding, fixed 2026-08-07:** `116f8e3` shipped `.github/workflows/ci-cd.yml` — a build+push+deploy workflow — without going through that design pass, the same process gap M18 hit earlier. A retroactive `staff-engineer-reviewer` pass (run alongside an unrelated M17 review) found it was non-functional (its one run ever failed at `setup-python`: no prebuilt arm64 Python 3.11 for the runner's Ubuntu 20.04 base) and, even if that were fixed, would have deployed nothing while reporting success — it pushed images to a registry (`10.0.20.10:30500`) that no manifest pulls from (`imagePullPolicy: Never`, side-loaded via `k3s ctr images import`, a completely different mechanism) and its final `kubectl rollout restart deployment/munger-report-web` targeted a deployment name that doesn't exist (the real one is `munger-report`). It also ran on `pull_request`, not just `push` to `main`, with every quality gate neutered (`|| true`) — auto-deploying an unreviewed PR branch to the shared dev cluster with no real gate. Investigating the fix surfaced a deeper gap than a YAML bug: the self-hosted runner (`actions-runner/munger-ci-runner`, a bare pod pinned to pi4) has no Docker daemon reachable and **no node in the cluster has Docker installed at all** — every build to date, including this session's, has run on the dev machine via `deploy/build-and-deploy.sh`, not in-cluster. Giving the runner real build/deploy capability means a genuine new privilege grant (host Docker socket, or a daemonless in-cluster builder like kaniko with node-level containerd-socket access for the import step) — exactly the kind of decision `DESIGN_CD_PIPELINE.md` §5 already flags as open and unresolved (runner privilege model, registry choice). Rather than grant that unilaterally, user chose (2026-08-07) the honest interim scope: renamed to `.github/workflows/ci.yml`, push-to-`main`-only, real (non-neutered) ruff/mypy/pytest gates, build/push/deploy steps dropped entirely. Deploys stay manual (`build-and-deploy.sh`) until the design doc's open questions get an actual decision. Full auto-deploy remains **todo**, blocked only on that decision. **Basic CI (lint/typecheck/test on push to main) is the one piece of this row actually working today, confirmed green (2026-08-07), not just reasoned about:** getting `ci.yml` to actually pass on the runner from a genuinely fresh environment (the old workflow never really ran these gates, so nothing had ever verified them clean end-to-end before) surfaced five more real, previously-invisible bugs across four `gh run watch` iterations — a stale E501 in `tests/test_daily_screen.py`; `tests/test_gcs_bridge.py` tripping mypy's `no_implicit_reexport` by reaching into `gcs_bridge.storage` instead of patching the shared `google.cloud.storage` module directly; a `list[dict[str,str]]`-vs-`list[dict[str,object]]` invariance error in `tests/test_report.py`; `pandas-stubs` installed ad hoc in the local dev venv but never declared in `requirements.txt` (and pinned to the wrong pandas major version, 3.0.x, on the first attempt — caught by `staff-engineer-reviewer` before push — corrected to `~=2.3.3` to match the actually-pinned `pandas~=2.3.3`), which was itself masking a real `no-any-return` bug in `screener.py`'s `fetched_fraction()` (fixed with the same `float()`-cast convention already used at every other pandas-scalar-return site in the codebase); and `.venv/bin/pytest` not adding the repo root to `sys.path` the way `-m pytest` does, causing 12 collection errors on top-level imports (fixed by invoking `python -m pytest`, matching `deploy/k8s/ci-test-job.yaml`'s existing convention). Also pinned `ruff`/`mypy`/`uv` to specific versions and added `timeout-minutes: 15`, both `staff-engineer-reviewer` findings on the first draft, so an unrelated tool release or a hung step can't silently degrade this gate's signal. Run `31224948797` (`gh run view`) is the first fully green run: checkout, uv/Python install, ruff, mypy, and the full 281-test pytest suite all passed on the actual self-hosted runner. The Docker-daemon/runner-privilege finding above is new information not yet captured in `DESIGN_CD_PIPELINE.md` itself; an addendum has been added there (§5) so whoever resolves the open questions doesn't have to re-discover it from this row's prose. |
| GCP cost / billing exposure not yet confirmed | done | 2026-07-25 (pm-reviewer) — this repo has previously treated third-party cost as an explicitly-decided question (M1: the $19/mo FMP API was evaluated and declined). The `gramunger.com` domain registration (Cloudflare, 2026-07-25) plus Cloud Run Job/service, Artifact Registry storage, and GCS storage+egress all have real (likely small, but unconfirmed) costs against whichever billing account project `munger-503515` is attached to. Now more pressing given `daily-screen` runs on an automated daily Cloud Scheduler trigger (see below), not just ad hoc — confirm usage is expected to stay within free-tier limits (or that the cost is accepted). **2026-07-27 update:** M16 added one more billed resource to this same unresolved ledger — the `munger-pnl-writer` service account's daily `pnl.json` writes to `gs://munger-503515-data` (Class A operations, negligible bytes) — folded into this same open item, not a new one. An earlier read-only cost investigation this session (before M16) estimated the pre-existing footprint at ~$0-1/month; this adds one small object write per day, immaterial to that estimate but worth re-confirming together when this item is finally closed. **Closed with real measured data (2026-07-30), not an estimate:** `report-web` (Cloud Run service) served 16,670 requests and 134 MB egress over the trailing 30 days (Monitoring API) — 0.8% and 12.5% of the Always Free tier's 2M requests/1 GiB egress respectively. `daily-screen` (Cloud Run Job, 1 vCPU/1 GiB) runs ~646s/day → ~19,400 vCPU-seconds and ~19,400 GiB-seconds/month, ~11% and ~5% of the 180,000/360,000 free-tier allotments. GCS bucket is 17.4 MiB total. Cloud Scheduler: 1 job (3 free). **One real, if trivial, cost found:** Artifact Registry holds 783.8 MB across 11 `daily-screen` image digests (only 1 tagged `latest`; the rest are undeleted history from past deploys) — this exceeds the 0.5 GB free tier by ~0.28 GB, roughly **$0.03/month**, and will keep growing unboundedly with no cleanup policy (same class of issue as the PVC-archive-growth row above, just on the Cloud Run side). Accepted as immaterial for now; revisit if it's ever worth a retention policy. **Also checked for M17 P3's specific gate:** the billing account (`01D472-04F73C-4085FF`) has three other projects (`gen-lang-client-0820964252`, `nullsector-500716`, `mathquest-500713`) — GCP's Always Free e2-micro allowance is per-*billing-account*, not per-project, so a second free-tier VM elsewhere on the same account would have silently consumed it. Confirmed: Compute Engine API has never been enabled on any of the four projects, so the one free e2-micro slot is fully unused — M17 P3's proposed `us-central1` e2-micro is a free-tier match (region-eligible, no competing instance). **Ongoing drift now covered too (2026-07-30, user decision):** enabled `billingbudgets.googleapis.com` and created a $5/month budget (`munger-cost-alert`) scoped to just the `munger-503515` project (not the whole billing account, which also covers three unrelated projects), with alert thresholds at 50/90/100% of spend. No custom notification channel was configured — GCP's default behavior emails all Billing Account Administrators, and `jimmyokUSA@gmail.com` already holds `roles/billing.admin` on this account (confirmed via `gcloud billing accounts get-iam-policy`), so the alert reaches the user with no extra setup. Same posture as the M18 analytics heartbeat check: notice and alert on drift, don't pre-build a fix for a problem that hasn't happened. |
| Public, unauthenticated report URL — M13's exposure question, now deliberately decided | done | 2026-07-25 — `report-web`, now also reachable at `https://gramunger.com/`, is public with no auth, unlike the k3s report (LAN-only NodePort). This crosses the line M13's row 355 flagged as an open decision ("nothing currently states it must stay local-only... never published... without deliberately deciding that's acceptable") — pm-reviewer caught that it had been crossed *by default* (no one had decided it); registering and mapping `gramunger.com` is the user explicitly deciding it, closing the question. Still true and worth restating: this must stay screen-only, paper-account data — M13's note explicitly said to revisit before any paper→live go/no-go (M12), since the same report would then show real position sizes. |
| GCP infra provisioned | done | 2026-07-25 — by hand via `gcloud` (no IaC / deploy script yet, unlike the k3s side's `build-and-deploy.sh`): GCS bucket `munger-503515-data` (writable state, `MUNGER_DATA_DIR=/mnt/data`), Artifact Registry Docker repo `munger` (us-central1), Cloud Run **service** `report-web` (nginx, GCS FUSE volume mount, port 8080), Cloud Run **Job** `daily-screen` (same screen-only entrypoint as the k3s CronJob). |
| `deploy/cloudrun/report-web/{Dockerfile,nginx.conf}` | done | 2026-07-25 — `nginx:alpine`, no content baked in; serves `/mnt/data/report` off the `gcsfuse.run.googleapis.com` CSI volume Cloud Run mounts at runtime (the same bucket `daily-screen` writes to). Hardcodes port 8080 (Cloud Run's injected `$PORT`) since nginx doesn't template env vars natively. **Verified live:** `https://report-web-828862293557.us-central1.run.app/` returns HTTP 200 (see the public-exposure row above). |
| Real bug: GCS per-object mutation rate limit starved fetch threads | done | 2026-07-25 — first `daily-screen` execution (`daily-screen-fhzkc`, 16:00–16:22 UTC) hit GCS's per-object mutation rate limit: the per-ticker live-progress write (`data._write_progress_file`, ~3000 calls for a 1503-ticker universe) is unthrottled on local disk/the k3s PVC (no per-object limit there) but isn't on GCS FUSE — the resulting 429/stale-handle retries starved the fetch threads badly enough that 489/1503 tickers timed out. Fixed: `config.PROGRESS_WRITE_MIN_INTERVAL_SECONDS` (env `MUNGER_PROGRESS_WRITE_MIN_INTERVAL_SECONDS`, set to `2.0` on the deployed Job) throttles only the high-volume per-ticker `_mark_ticker_started`/`_mark_ticker_done` writes; `_start_progress`/`_finish_progress` always force-write so a batch's start/final state is never lost. New test `test_progress_write_throttle_skips_rapid_per_ticker_writes`. staff-engineer-reviewer: confirmed the throttle check-and-update is race-free (both happen inside `_progress_lock`); flagged that the safe non-zero interval is only applied via the Cloud Run env var, with no code-level guard against a future redeploy silently dropping it back to the unthrottled default — worth a startup check if this recurs. |
| Real bug: GCS FUSE rejects `chmod`, crashing archival | done | 2026-07-25 — second execution (`daily-screen-dvngr`, 18:43–18:52 UTC, already running with the throttle fix above) got past fetching but crashed in `journal.archive_screen_results`: `shutil.copy` calls `os.chmod` to preserve the source file's permission bits, and GCS FUSE rejects that outright (`PermissionError: [Errno 1] Operation not permitted` — GCS objects have no POSIX permission bits to set). `report._copy_archives_into_report` had the identical latent bug via `shutil.copy2`. Both worked fine on local disk/the k3s PVC, so neither surfaced there. Fixed: both switched to `shutil.copyfile` (content-only, no `chmod` call). New tests `test_archive_screen_results_never_calls_chmod` / `test_copy_archives_into_report_never_calls_chmod` monkeypatch `os.chmod` to raise, proving neither path calls it. staff-engineer-reviewer: verified `copyfile` is a safe drop-in at both call sites, including that `report.py`'s "skip unchanged file" mtime-comparison optimization still holds without `copy2`'s mtime preservation. |
| Third execution (`daily-screen-br47n`, 16:02–16:03 UTC) | done | 2026-07-25 — user-cancelled manual rerun during the `fhzkc` investigation, not an independent failure signal. |
| Images rebuilt with both fixes and redeployed | done | 2026-07-25 — `daily-screen`/`report-web` images rebuilt (Cloud Build, ~18:54–18:55 UTC) and redeployed to Cloud Run. |
| **First successful end-to-end execution** | done | 2026-07-25 — `daily-screen-pdh67` (18:56:47–19:08:32 UTC) completed successfully: fetched the universe, archived `screen_results_2026-07-25.csv`, regenerated the report. First execution of this target to actually succeed. |
| Cloud Scheduler trigger wired up | done | 2026-07-25/26 — `munger-daily-screen` (HTTP target, `0 13 * * *` Etc/UTC — same cadence as the k3s CronJob), region `us-central1`, state `ENABLED`. Not previously listed as a task here because it was wired up alongside the redeploy above; recording it now since its first scheduled fire is the strongest validation signal so far. |
| First scheduled run succeeded | done | 2026-07-26 — `daily-screen-w9w7w` (13:00:13–13:11:23 UTC), the Scheduler's first automatic fire, completed successfully: `screen_results_2026-07-26.csv` archived (1503 tickers, 7 buyable), report regenerated, live at `gramunger.com`. Two consecutive successful executions is a real but still thin track record — worth more data points before treating this as equivalent in reliability to the k3s CronJob's longer history. |
| No one-command Cloud Run deploy script | todo | 2026-07-25 — unlike `deploy/build-and-deploy.sh` for k3s, every Cloud Run resource and image push so far has been driven by hand via individual `gcloud`/`docker` commands; nothing is scripted or reproducible yet. More valuable to write now that the target demonstrably works, but still gated on the canonical-target decision above — no point scripting a deploy path that might be abandoned. |

## M16 — P&L Tracking (added 2026-07-27, user request)

"New milestone, track profit and loss in a new part of the website, for
the moment just the paper trading, but extensible for real money in the
future." Full design in DESIGN.md §3.8. Three new pieces: `pnl.py`
(fetches a P&L snapshot from Alpaca — account equity/cash, per-position
unrealized P&L, portfolio history — and writes it to
`config.PNL_DATA_PATH`), a new `report.py` page (`pnl.html`, stat tiles +
a positions table, gains/losses colored distinctly, an explicit empty
state when no snapshot exists yet), and a cross-system bridge (a new
scoped GCP service account, `munger-pnl-writer`, `roles/storage.objectAdmin`
IAM-Condition-scoped to only the `pnl.json` object (see the 2026-07-27
row below for why plain `objectCreator` wasn't actually enough) — GitHub
Actions writes there
since `report.py`'s own deployment deliberately never has Alpaca
credentials). New tests: `tests/test_pnl.py` (8 tests, mocked Alpaca SDK,
same pattern as `test_execution.py`) and 9 new tests in
`tests/test_report.py` for `_load_pnl_snapshot`/`_render_pnl`. Full suite
260/260 passing, ruff/mypy clean. Visually smoke-tested locally
(screenshot) with realistic sample data before wiring up the live bridge.

| Task | Status | Date / Notes |
|---|---|---|
| `pnl.py` written and tested | done | 2026-07-27 — read-only against the broker, fails closed on an unexpected `get_account()`/`get_portfolio_history()`/`get_all_positions()` response type (same posture as `execution.py`'s account/holdings reads; the positions check was a staff-engineer-reviewer finding — an earlier draft silently filtered any non-`Position` entry instead of raising, the one place the module's own "fail closed, not silently empty" promise wasn't actually enforced). **Revised same day (pm-reviewer finding):** an earlier draft's `__main__` caught its own exceptions and always exited 0 so a snapshot failure wouldn't mark the run as failed the way a real trading-path error would — but that meant a broken bridge (rotated key, changed SDK response shape, GCS outage) failed completely silently, with nothing paging anyone. Fixed: `__main__` no longer catches anything; a failure now fails its own GitHub Actions step (and therefore the job), which is exactly this project's existing alert channel (the standard GH failure email), same as every other alert here — see DESIGN.md §3.8. |
| `report.py`'s `pnl.html` written and tested | done | 2026-07-27 — nav links added from `index.html`/`tickers.html`/`calendar.html`; new CSS (`.stat-tiles`, `.pnl-gain`/`.pnl-loss`/`.pnl-flat`, `.mode-badge`) reuses the existing badge-green color for gains rather than inventing a second green. **Real bug found in staff-engineer-reviewer pass and fixed same day:** `_pnl_staleness_note` used `datetime.fromisoformat`, which parses a string with no UTC offset into a *naive* datetime without raising — subtracting that from an aware `datetime.now(UTC)` then raised an uncaught `TypeError` that would have crashed the *entire* `generate_report()` run (not just `pnl.html`) over one bad field in a file bridged across two systems. Fixed: a naive result is now explicitly treated as unparseable. New regression test `test_render_pnl_treats_a_timezone_naive_generated_at_as_unparseable`. |
| GCP service account + bucket-scoped IAM binding provisioned | done | 2026-07-27 — `munger-pnl-writer@munger-503515.iam.gserviceaccount.com`. Key created, added as the `GCP_PNL_WRITER_KEY` GitHub Actions secret, local key file deleted immediately after (`gh secret list` confirms the secret exists; no key material was left on disk or shown in any log). **IAM scoping went through four real iterations, three of them caught by actual failures (a real GitHub Actions run or a real impersonation test), not just review:** (1) first granted `roles/storage.objectAdmin` on the whole bucket — correctly bucket-scoped, not project-wide, but staff-engineer-reviewer flagged that this also grants delete on every object in the bucket (`state.json`, `journal.db`, `screen_results*.csv`, the whole `report/` tree), a far larger blast radius than "create/overwrite one file" needs; (2) downgraded to `roles/storage.objectCreator` (create/overwrite, no delete) — passed review, but the **first real scheduled run** (`daily-trade.yml` run `30284674051`, 2026-07-27) hit a live `403: does not have storage.objects.get access` on the upload step, because `gcloud storage cp` needs read access too; (3) granted `objectViewer` alongside — fixed the `get` error, but a manual impersonation test of an *overwrite* (not just a fresh create) then hit `does not have storage.objects.delete access`, since overwriting an already-existing object needs delete, which no combination of `objectCreator`/`objectViewer` grants; switched to a single `roles/storage.objectAdmin` binding with an **IAM Condition** (`resource.name == "projects/_/buckets/munger-503515-data/objects/pnl.json"`) — create+get+delete, but only for that one object; (4) a **second real manual run** (`workflow_dispatch`, `screen_only=true`, 2026-07-27) still failed the upload step with `does not have storage.objects.list access` — `gcloud storage cp` also checks whether the destination path is directory-like before writing, a bucket-level `list` call an object-scoped condition structurally can't grant. Final fix: added a second, unconditional `roles/storage.objectViewer` binding (bucket-wide list+get, read-only) alongside the object-scoped `objectAdmin` condition. Verified for real this time by impersonating the service account through the *full* real sequence — create, then a second overwrite (matching the actual daily pattern), then a write attempt to a different object name (still correctly denied) — all three behaved as expected, not just a single create. This is the config actually deployed as of this row; a further real `daily-trade.yml` run is still needed to close the "First real run" row below since local impersonation, however thorough, is not the same as the actual CI environment. |
| `daily-trade.yml` steps added: generate + upload P&L snapshot | done | 2026-07-27 — two new steps after "Upload run artifacts", both `if: always()` (independent of whether "Run bot" succeeded, placed any orders, or ran screen-only — the account/positions exist regardless). The GCS upload step passes the service-account key via an env var, not templated directly into the shell script, since it's multi-line JSON (matches how `ALPACA_API_KEY` is already passed elsewhere in this file) — writes it to a `mktemp` file, `gcloud auth activate-service-account`, uploads, deletes the temp file. |
| **First real run exercising the P&L bridge end-to-end — succeeded** | done | 2026-07-27 — third attempt (`workflow_dispatch`, `screen_only=true`, run `30287588744`) after two real IAM failures (see the row above): `pnl.json` confirmed present in `gs://munger-503515-data/` (`gcloud storage ls -L`, real content: equity $99,893.02, cash $54,998.56, 3 open positions with real `unrealized_pl`). `daily-screen` execution `daily-screen-xkp9k` then regenerated the report, and `gramunger.com/pnl.html` now shows that real data live: Equity $99,893.02, Cash $54,998.56, Today's P&L **-$106.96** (correctly colored `pnl-loss`, red), `PAPER` mode badge, no staleness warning, no empty state. Confirmed directly via `curl` against the live page, not assumed. |
| Confirm P&L numbers against Alpaca's own dashboard | done | 2026-07-29 — verified. User read Alpaca's live paper equity ($101,495.23); it was compared against a fresh read-only `pnl.generate_snapshot()` pulled at the same moment (2026-07-29 15:30 UTC, local `.env` paper creds, no GCS write, no orders placed): equity **$101,517.02 vs $101,495.23 = +$21.79 (0.02%)**, identical cash ($29,727.04) and the same 11 positions — a match to within live intraday price-tick drift. (Aside: the GCS `pnl.json` at the time still showed $101,076.96 from a 2026-07-28 16:16 UTC snapshot; that 0.41% gap vs live was purely ~1-day staleness, not a calc error — the internal check `cash + Σ market_value = equity` held to the cent.) This confirms the *numbers themselves*, not just that the pipeline runs (row 550). Unblocks M17 Graph 1. |
| k3s dev report P&L (deferred, user decision) | todo | Scoped to Cloud Run/`gramunger.com` only for now — wiring this into the k3s dev report needs GitHub Actions to reach the home LAN, the same reachability gap `DESIGN_CD_PIPELINE.md` already names for a different reason. Revisit together with that design if/when it's actually built. |
| Confirmed `daily-screen.yml` (GitHub Actions) never publishes a stale `pnl.html` anywhere live | done | 2026-07-27 (staff-engineer-reviewer question, checked) — this separate workflow also calls `report.generate_report()`, and it never receives `pnl.json` (the GCS bridge only reaches Cloud Run's mounted bucket), so its own `pnl.html` always renders the empty state. Confirmed harmless: its output is only committed to the `daily-screen-archive` git branch (same archival pattern as `bot-state`), and `gh api repos/jimmyokusa/munger/pages` returns 404 — no GitHub Pages site serves that branch, so nobody ever sees this empty-state `pnl.html`. |
| Accepted tradeoff: `pnl.py`'s Alpaca calls have no retry/backoff | todo | 2026-07-27 (staff-engineer-reviewer finding, not fixed) — unlike `data.py`'s yfinance fetches (`DATA_FETCH_MAX_RETRIES`, exponential backoff, rate-limit cooldown), a single transient network blip or 5xx from Alpaca's REST API fails the whole step immediately, firing the GH-failure-email alert every time with no debounce. Left as-is for now: Alpaca's REST API doesn't have the same public rate-limiting history that justified the more patient yfinance path, and building retry logic for a display-only feature before it's shown to actually be needed would be premature. Revisit if this produces real alert noise once the bridge has run for a while. |

## M17 — Durable P&L history + embedded Grafana dashboards (added 2026-07-28, user request)

"Profit and loss should be exported for collection by prometheus... grafana in
the app as a secondary page... separate tabs," refined with the user to two
graphs: (1) **account P&L over time** (whole trading account), (2) **daily
close price per owned ticker**. Full design in `DESIGN_DASHBOARDS.md`, which
went through both review gates (staff-engineer + pm) before build.

**Prometheus was evaluated and dropped** (recorded like M1's FMP-API decline):
the two graphs are backfilled from JSON via Grafana's Infinity datasource, the
user declined alerts, and single-node Prometheus is a *worse* durability tier
than the GCS bucket for the long-term record (and can't run free on prod). The
durable long-term store is instead an **append-only series in GCS**
(`pnl_history.jsonl`), read directly by Grafana. Retires the calendar tab and
deletes the `feed.json`/`rss.xml` feed (reverses M15 slice 3), both per user.

Phasing: **P1** (dev, from existing data) — durable append-series + GCS→PVC
bridge (closes row 552) + dev Grafana + account-P&L-over-time dashboard/tab +
calendar→dashboards swap + feed deletion. **P2** — `prices.py` held-symbol
daily close + Prices dashboard. **P3** — prod (`gramunger.com`) Grafana on a
free-tier GCE e2-micro VM, its own milestone (cost confirmation gated).

| Task | Status | Date / Notes |
|---|---|---|
| Design + both review gates | done | 2026-07-28 — `DESIGN_DASHBOARDS.md` (supersedes the dropped `DESIGN_PROMETHEUS.md` draft). staff-engineer + pm gates ran on the draft; findings folded into the doc (§9 of the old draft; now integrated). |
| **P1 — durable P&L append-series in `pnl.py`** | done | 2026-07-28 — `config.PNL_HISTORY_PATH` (`pnl_history.jsonl`); `pnl.update_history_series(snapshot)` upserts one row/day keyed by date (idempotent re-run), seeds the past from Alpaca's `history` array, and **preserves days that scroll out of Alpaca's rolling ~1-month window** (the durability the file exists for) — resolving the staff-eng "30-day rolling window" finding. Today's row comes from the fresher account snapshot, not history's partial same-day point. Atomic write (temp+rename). `write_snapshot` now accepts a pre-fetched snapshot so `__main__` reads Alpaca once for both files. 8 new tests (seed/idempotent/preserve-old/account-wins/skip-None/atomic/default-path/prefetch); ruff+mypy clean, full suite 273 passing. **Two more real bugs found in staff-engineer-reviewer pass and fixed 2026-07-30:** (1) `mode` was being refreshed to the account's *current* mode on every already-recorded date, not just new ones — the first run after ever flipping `config.PAPER_TRADING` from paper to live would have silently relabeled the prior ~30 days of genuinely-paper history as live, permanently (once those days scroll out of Alpaca's window, the wrong label is "preserved" forever); fixed by only setting `mode` on a date the first time it's recorded. (2) `zip(timestamps, equities, profit_losses, strict=False)`'s own comment claimed it "pairs only the complete points" on a degraded (length-mismatched) Alpaca response, but `zip` just truncates to the shortest array — if `profit_loss` came back one entry short, the *most recent* day's perfectly-valid timestamp/equity point would be silently dropped instead of kept with a null `profit_loss`; fixed with explicit indexed iteration. 2 new regression tests; full suite now 276 passing. |
| P1 — `daily-trade.yml`: bridge `pnl_history.jsonl` through GCS | done | 2026-07-29 — implemented: a new "Download prior P&L history from GCS" step runs before `pnl.py` (restores the series so the read-modify-write upsert sees prior days), and the upload step now pushes `pnl_history.jsonl` back alongside `pnl.json`. YAML-validated. `GCP_PNL_WRITER_KEY`'s SA needs read (`storage.objects.get`) for the download — satisfied by its M16-row-548 bucket-wide `objectViewer` binding. **Real bug found in staff-engineer-reviewer pass and fixed 2026-07-30:** the download step originally treated *any* `gcloud storage cp` failure — a transient blip, an IAM regression, not just a genuine "object doesn't exist yet" — identically as "first run," and the always-run upload step would then have permanently overwritten the durable GCS history with a truncated ~30-day reseed, job green, nothing paging anyone. Fixed: the download step now greps the failure output for "matched no objects" to distinguish the expected first-run case from a real error, and on a real error sets a step output that the upload step checks to skip re-uploading `pnl_history.jsonl` rather than clobbering it. |
| **First real run exercising the `pnl_history.jsonl` bridge end-to-end — succeeded** | done | 2026-07-30 — **a fifth real IAM iteration, found live:** the first real manual run (`workflow_dispatch`, `screen_only=true`, run `30561177850`) got past the (now-fixed) download step cleanly and uploaded `pnl.json` fine, but failed uploading `pnl_history.jsonl` with `does not have storage.objects.create access` — the M16-era IAM Condition on `munger-pnl-writer`'s `objectAdmin` binding was scoped by literal object name to `pnl.json` only (see M16 row 548's four-iteration history), and nobody widened it when M17 introduced a second durable object. Fixed by replacing that binding with one whose condition covers both `pnl.json` and `pnl_history.jsonl` by name (same narrow by-name-only scoping principle, not broadened to the whole bucket). **Confirmed for real** on the very next run (`30562279629`, `screen_only=true`): job green end-to-end, and `gcloud storage cat gs://munger-503515-data/pnl_history.jsonl` shows a real 21-row durable series (dates through 2026-07-30, all `mode: "paper"`) — the round-trip (download prior series → `pnl.py` upsert → re-upload) is proven working live, not just YAML-validated. Both confirming runs were manual `workflow_dispatch` calls, not yet a naturally-scheduled cron trigger since the fix landed — low-risk given the P&L/GCS steps run `if: always()` completely independent of `screen_only`/`KILL_SWITCH` (the identical code path a real 14:00 UTC run takes), but noted for completeness. |
| P1 — read-only GCS→PVC bridge CronJob (k3s) | done | 2026-07-29 — implemented as `deploy/k8s/40-gcs-reader-cronjob.yaml`: new k8s SA `munger-gcs-reader` + standalone CronJob at **15:00 UTC** (after daily-trade's 14:00 run and its 45m timeout), pinned to **pi1** (the RWO PVC host — writers must run there), `google/cloud-sdk:slim`, atomic temp+rename. Writes `pnl.json`→`/data/pnl.json` (**closes M16 row 552**) and `pnl_history.jsonl`→`/data/report/` (nginx-served so off-node Grafana can HTTP-read it); also emits a JSON-array `pnl_history.json` because Infinity parses JSON, not NDJSON. GCP SA `munger-gcs-reader` (`objectViewer`, bucket-wide read) via Secret `munger-gcs-reader-key`, created out-of-band (commands in the manifest header). YAML + embedded shell/python validated locally; transform tested against a `pnl.py`-shaped sample. Wired into `deploy/build-and-deploy.sh`'s apply loop. **Relabeled from in-progress (pm-reviewer, 2026-07-30):** everything the coding agent can do here is done; what remains — SA/Secret creation + `kubectl apply` — is blocked purely on cluster reachability (pi4 SSH times out, the documented DHCP-drift gap), which needs the user, not more agent effort. **2026-08-07, cluster back up: a real bug found live** — `google/cloud-sdk:slim` failed every run with `exec /bin/bash: exec format error` (amd64-only, not multi-arch as assumed — confirmed via `docker manifest inspect`, no manifest list present). Replaced with `gcs_bridge.py`, a pure-Python rewrite (google-cloud-storage client) bundled into the already-arm64-verified `munger` image — no third-party image, no arch risk. Also fixed a data-integrity gap `staff-engineer-reviewer` caught: the JSONL→JSON transform ran *after* committing the jsonl rename, so a malformed line would leave jsonl updated while json (what Grafana reads) silently froze — now transforms from the temp download before either file commits. **Confirmed live:** `kubectl logs` on the last 3 scheduled runs (23h/47h/2d23h old) all show `Bridged pnl.json` + `Bridged pnl_history.jsonl`, and the PVC (`kubectl exec` into `munger-report`) holds a real bridged series through 2026-08-05. |
| P1 — dev Grafana (k3s) | done | 2026-07-29 — implemented as `deploy/k8s/50-grafana.yaml`: Deployment pinned **off pi1** (nodeAffinity NotIn pi1), provisioned-stateless (emptyDir + ConfigMap datasource/dashboard-provider/dashboard), locked down (anonymous Viewer-only; login-form/basic-auth/sign-up/Explore all disabled; embedding on for the iframe). Does **not** mount the RWO PVC — reads the bridged files over HTTP from the in-cluster nginx Service. Infinity via `GF_INSTALL_PLUGINS`. NodePort 30030. YAML + embedded provisioning YAML validated. **Relabeled from in-progress (pm-reviewer, 2026-07-30):** same cause as the row above — `kubectl apply` is blocked on cluster reachability, plus confirming the node pulls `grafana/grafana:11.3.0`/`google/cloud-sdk:slim` (arm64) and has outbound internet once for the plugin install, neither of which the agent can do without cluster access. **2026-08-07, cluster back up: a real, never-before-tested typo found** — the Infinity datasource plugin ID was `yesoreyeric-infinity-datasource` (typo) instead of the real `yesoreyeram-infinity-datasource`, which crash-looped the pod (404 from Grafana's plugin catalog); fixed across all 6 references (env var, datasource type, dashboard JSON). **Confirmed live:** `curl http://10.0.20.10:30030/api/health` returns `{"database":"ok","version":"11.3.0"}`; dashboard listing returns the real `munger-account-pnl` panel. |
| P1 — account-P&L-over-time dashboard + Dashboards tab | done | 2026-07-29 — dashboard JSON (uid `munger-account-pnl`, two timeseries panels — account equity + daily profit/loss) embedded as a ConfigMap in `50-grafana.yaml`, reading the durable series via Infinity at `http://munger-report.../pnl_history.json`. The `report.py` Dashboards nav/tab + loud staleness banner were already implemented in a prior session (verified present: `_render_dashboards`, `_history_nav_link`, gated on `config.GRAFANA_BASE_URL`). **Relabeled from in-progress (pm-reviewer, 2026-07-30):** transitively blocked on the same cluster-reachability gap as the two rows above — setting `MUNGER_GRAFANA_URL` needs a live Grafana URL to point at first. **2026-08-07: wired on the k3s `daily-screen` CronJob** (`http://10.0.20.10:30030/d/munger-account-pnl/account-pandl-over-time?kiosk` — pi4's node IP; any of the four works since NodePort exposes on all, pi4 chosen as control-plane/least likely to be relabeled). **Confirmed live** via a manually triggered run: `dashboards.html` (not `calendar.html`) is what the nav now serves, its `<iframe>` points at the real URL above, and it renders the actual bridged P&L series. |
| P1 — delete feed | done | 2026-07-29 — **split from a combined row (pm-reviewer finding, 2026-07-30): this row is genuinely done and shouldn't share ambiguous status with the still-incomplete calendar swap below.** Generation + `config` constants were already removed in a prior session; this session removed the 8 now-dead feed tests and trimmed 2 mixed tests (favicon, export-controls) in `tests/test_report.py`, clearing the 10 stale failures flagged in the feed-retirement-debt row (M17, moved below). Full suite **274 passing** at the time, ruff clean on the file; no dangling feed refs remain (grep-confirmed — only an explanatory comment in `report.py`). (One pre-existing mypy nit at `test_report.py:382` in the unrelated M15 export-controls test remains — not feed-related, left untouched.) |
| P1 — calendar→dashboards swap | done on k3s dev; prod deliberately held | 2026-07-29 — **split from the row above (pm-reviewer finding, 2026-07-30).** The config-gated swap *mechanism* is in place (`report.py` emits `dashboards.html` + drops the calendar nav where `GRAFANA_BASE_URL` is set, else keeps `calendar.html` — design §4) and is code-complete. **2026-08-07: retired on k3s dev, confirmed live** — the row above's manual run shows `dashboards.html` is genuinely what's served, not `calendar.html`. **`gramunger.com` (prod, Cloud Run) — checked in with the user before touching it (live public-site change, retires a page real visitors see); user approved. Turned out `MUNGER_GRAFANA_URL` was already set on the Job (from earlier this session) and prod had already regenerated with it** — confirmed live (2026-08-07): `curl https://gramunger.com/dashboards.html` → `200`, nav no longer links `calendar.html`, iframe points at `https://34-82-149-71.sslip.io/d/munger-account-pnl?kiosk` (`200`, correct `frame-ancestors https://gramunger.com` CSP), page timestamped from today's regular scheduled run. `calendar.html` itself still returns `200` (stale artifact left in the bucket, no longer linked from nav — matches this project's established pattern of not retroactively deleting superseded static files). `pnl.html` stays; `screen_results_archive/` data untouched. **M17 Phase 1 (P1) and Phase 3 (P3) are fully shipped on both dev and prod. Phase 2 (`prices.py`, rows below) is fully shipped on k3s dev as of 2026-08-08 — prod (the GCE VM) is not yet: see the P2 rows below for what's still open there.** |
| P1 prerequisite — verify P&L numbers vs Alpaca (M16 row 551) | done | 2026-07-29 — satisfied via M16 row 551: a fresh read-only snapshot matched Alpaca's live equity to 0.02% ($101,517.02 vs $101,495.23), same cash + same 11 positions. Graph 1 is no longer blocked on unverified data. Note the paper account is young — equity sat at exactly $100k for ~18 days before recent position P&L moved it — so the durable-series curve will still look short/near-flat initially even after the history-seed. |
| P2 — `prices.py` held-symbol daily close | **done — confirmed live end-to-end on k3s dev, see the row below for the deploy/verification detail** | 2026-07-28 — Alpaca `StockHistoricalDataClient` daily bars for **currently-held** symbols only; validation layer (reject zero/outlier/missing, handle "no bars"); "owned" = *currently* owned (a sold ticker's series drops off — named, not silent). Gated on confirming the account's data tier returns daily historical bars. **2026-08-07/08, gate confirmed live and module built:** a real read-only Alpaca call found the account's free-tier subscription 403s on the client's default (SIP) feed for historical bars ("subscription does not permit querying recent SIP data") — `config.PRICES_DATA_FEED` ("iex") fixes it, confirmed live. (Noted, not chased further: IEX reflects one exchange, not the SIP consolidated tape most quote sources show — a reasonable proxy for a read-only display chart, not a trading input, but a judgment call made to work around the access error rather than a deliberate data-source choice; pm-reviewer finding.) New `prices.py` (mirrors `pnl.py`'s structure): `held_symbols()` reads `pnl.json`'s `positions[]` (not a second Alpaca positions call, so `pnl.py`/`prices.py` can never disagree on "held"); `fetch_daily_closes()` validates each close (`config.PRICES_MAX_PLAUSIBLE_CLOSE` ceiling) and distinguishes a symbol with zero bars ("no_bars") from one whose bars all failed validation ("no_valid_bars") — never a silent bad point on a public chart. `prices.json` is **not** an append-series like `pnl_history.jsonl` (Alpaca's historical-bars endpoint has no rolling-window loss), so each run is a complete fresh fetch, no upsert/read-modify-write needed. **Real live end-to-end proof (not just mocks):** ran against the actual paper account's 14 real held symbols — all 14 returned ~251 daily bars (a year of trading days), plausible prices, zero validation failures. `gcs_bridge.py` extended to pull `prices.json` from GCS into `REPORT_DIR` (nginx-served for Grafana's Infinity datasource); refactored the pnl_history branch from an early `return` to if/else so a missing `prices.json` can't skip `pnl_history.jsonl` bridging or vice versa (a real control-flow bug caught while building it). `daily-trade.yml` runs `prices.py` after `pnl.py` and uploads `prices.json` alongside the existing P&L uploads. staff-engineer-reviewer pass found and fixed a real bug: `_validate_close`'s comparisons all evaluate `False` against `NaN` (compares unequal to everything), so an unvalidated NaN close would have serialized as the literal token `NaN` via `json.dumps`' default `allow_nan=True` — not standard JSON, breaking Grafana's Infinity parser on the *entire* file, not just one point; fixed with an explicit `math.isfinite` guard, regression test added. 17 new tests (`test_prices.py` 13, `test_gcs_bridge.py` +2 for the new independent-file guarantee); full suite **297 passing**, ruff+mypy clean repo-wide. **2026-08-08: deployed and confirmed live end-to-end** — see the row below (P2 — build the Grafana "Prices" dashboard panel) for the full deploy/verification detail (`daily-trade.yml` run, IAM fix, k3s bridge, live browser confirmation). |
| P2 — build the Grafana "Prices" dashboard panel | **done — confirmed live on k3s dev** | 2026-08-07 — split out as its own trackable row (pm-reviewer finding: the row above bundled this into a `done` row's prose, where a `todo`-only scan would never surface it). **2026-08-08, built and unusually thoroughly live-tested** (real local Grafana+Infinity Docker containers + real Chrome browser automation, not just written and assumed correct — nothing here touched the actual k3s cluster): `gcs_bridge.py` extended with `_flatten_prices()`, deriving a flat `prices_flat.json` (one row per `{symbol, date, close}`) from `prices.py`'s nested-by-symbol `prices.json` — the long-format shape Grafana's "Partition by values" transform needs, same "raw + derived pair" pattern `pnl_history.jsonl`/`.json` already uses. New, fully isolated ConfigMap/provider/mount in `deploy/k8s/50-grafana.yaml` for the Prices dashboard (deliberately not sharing the already-live account-pnl dashboard's mount path, to put zero risk on that proven-working setup). **Three real bugs found and fixed via live testing, not review alone:** (1) Infinity's query needs `"parser": "backend"` explicitly, or it silently returns "No data" in the browser despite looking correct on inspection. (2) The `partitionByValues` transform's `fields: ["symbol"]` (lowercase) silently didn't match the query's actual field name `"Symbol"` (capitalized) — Infinity derives DataFrame field names from each column's `text`, not `selector` — so the transform no-op'd and every symbol rendered as one interleaved zigzag line instead of separate series; fixed by lowercasing `text` to match. (3) **A live, currently-open upstream regression**: Infinity plugin v3.11.2 (released 2026-08-05) breaks its own frontend module entirely (`react/jsx-runtime` 404 via SystemJS) — confirmed both by reproducing it locally and cross-checking `grafana/grafana-infinity-datasource#1772` (opened 2026-08-06, still open, unfixed). Since `GF_INSTALL_PLUGINS` re-fetches "latest" on every pod restart (this Grafana deployment is provisioned-stateless by design), this was a live, currently-real risk to the **already-shipped account-pnl dashboard too** — it's only working right now because that pod hasn't restarted since before v3.11.2 shipped. Fixed by pinning `GF_INSTALL_PLUGINS: "yesoreyeram-infinity-datasource 3.11.1"` (space, not `@` — confirmed the exact syntax against the actual Grafana docker image's `run.sh`, since `@version` silently 404'd). **Final proof, not just config review:** a real local Grafana 11.3.0 + Infinity 3.11.1 container, fed a real 2-symbol (AAPL/MSFT) sample dataset, rendered as two visually distinct, correctly-colored, correctly-legended lines in an actual browser screenshot. staff-engineer-reviewer pass also caught that the Prices dashboard, once deployed, would have been unreachable from the site — `report.py`'s `dashboards.html` only ever embedded `config.GRAFANA_BASE_URL` (the P&L dashboard); fixed with a new `config.GRAFANA_PRICES_URL` constant (env `MUNGER_GRAFANA_PRICES_URL`) rendering a second, independently config-gated iframe section, wired on `30-daily-screen-cronjob.yaml` alongside the existing P&L URL. 3 new tests (`test_dashboards_omits/shows_prices_section...`); full suite **300 passing**, ruff+mypy clean repo-wide. **2026-08-08, fully deployed and confirmed live end-to-end on k3s dev** (user request: "get it working on k8s first, then ... deploy to prod"). Live cluster had diverged from this repo's checked-in `50-grafana.yaml` in the meantime — a separate, unrelated home-network monitoring stack (Prometheus/Alertmanager/node-exporter/UniFi-Poller dashboards, tracked in a different repo) had been added directly onto the same shared Grafana instance minutes earlier. Integrated non-destructively: applied the new `dashboard-prices` ConfigMap standalone (additive, new resource name), then a `kubectl patch --type strategic` (not a full `kubectl apply` of the stale manifest) to add just the new volume/volumeMount + the plugin-pin env var, confirmed via `kubectl get deployment -o jsonpath` that all 8 volumes (7 pre-existing + 1 new) survived; `50-grafana.yaml` itself was then reconciled from the live Deployment's true state (including referencing, not owning, the foreign ConfigMaps, so a future full apply from this file stays safe) rather than left stale. Real bug hit during the live-vs-local final check: `GF_INSTALL_PLUGINS` pin syntax uses a **space**, not `@` (`"yesoreyeram-infinity-datasource 3.11.1"`) — confirmed against the actual Grafana image's `run.sh`. A second real bug surfaced only once the pipeline ran for real: `munger-pnl-writer`'s GCS IAM condition (the same by-object-name scoping M16/M17 already iterated on four times, see rows 548/583) was still scoped to only `pnl.json`/`pnl_history.jsonl` — `daily-trade.yml`'s first real run with the new `prices.py` step failed uploading `prices.json` with `storage.objects.create` denied; fixed by widening that same condition (user-confirmed before applying, since it is a live IAM change) to include `prices.json`, same narrow scoping, not broadened to the bucket. **Confirmed for real, not just deployed:** `daily-trade.yml` run `31237969633` succeeded end-to-end (prices.py fetched 14 real symbols, uploaded to GCS); the k3s `gcs-reader` CronJob (rebuilt image, side-loaded to pi1) bridged `prices.json`/`prices_flat.json` onto the PVC (`kubectl logs` shows all three bridge lines); `curl` against the live nginx confirms 3,514 real rows across all 14 held symbols; and the Grafana Prices dashboard, opened in a real browser, renders all 14 symbols as distinct, correctly-colored, correctly-legended lines over the real trailing year. `dashboards.html` confirmed live (via a fast report-only regeneration Job, not a full re-screen) to embed both the P&L and Prices iframes correctly. **Bonus, unrelated fix made in the home-network repo (not this one) while waiting on CI:** Grafana's node-exporter/traefik dashboards were showing raw Pi IPs instead of hostnames (user report) — fixed via Prometheus relabel_configs there, not tracked further in this file. **Still open, see the two rows below:** the prod GCE VM's plugin pin, and standing up the Prices dashboard on prod at all (not yet started). |
| P2 — stand up the Prices dashboard on prod (GCE VM) | **done — confirmed live** | 2026-08-08 (pm-reviewer finding: this had no dedicated row, only a trailing clause inside the `done` row above — exactly the anti-pattern rows 587/588/635 were already split apart to avoid). Explicitly deferred, not forgotten — user's own stated sequencing was "get it working on k8s first, then ... deploy to prod." **2026-08-08, built and deployed:** `deploy/gce/pull-pnl-history.sh` extended to also pull `prices.json` and derive `prices_flat.json` (reimplements `gcs_bridge.py`'s `_flatten_prices()` in shell+Python, since this script runs in a bare `google/cloud-sdk:slim` container with no import path to the munger package); new `deploy/gce/grafana-dashboard-prices.json` (structurally matches the k3s version); `bootstrap.sh`/`README.md` updated to fetch/document the new file. `munger-grafana-vm`'s IAM was already bucket-wide `storage.objectViewer` with no per-object condition, so — unlike `munger-pnl-writer`'s write-side gap — no IAM change was needed here. staff-engineer-reviewer caught a real, high-confidence-not-hypothetical finding before deploy: the new dashboard initially omitted `"parser": "backend"` (to match the existing `account-pnl.json`'s convention in this same directory), but the reviewer correctly flagged that the *structural* match is actually the k3s Prices dashboard (same 3-column `partitionByValues` transform, same Grafana/Infinity version pin), which needed that field explicitly — added it, and live testing on the real VM confirmed the reviewer right (blank without it, working with it — see below). Deployed via the documented incremental pattern (`gcloud compute instances add-metadata` + re-run `bootstrap.sh` via `google_metadata_script_runner startup`, exit 0) rather than a full VM rebuild. **Confirmed live, not just deployed:** all three containers (`caddy`/`grafana`/`json-server`) came back up, the pull timer had already fetched real data at boot (`prices.json` 276,979 bytes / `prices_flat.json` 202,808 bytes), `docker exec json-server cat prices_flat.json` shows the real 3,514-row/14-symbol dataset, and the dashboard opened in an actual browser at `https://34-82-149-71.sslip.io/d/munger-prices/...` renders all 14 symbols as distinct, correctly-legended lines — visually identical to the k3s confirmation. `MUNGER_GRAFANA_PRICES_URL` was also set on the Cloud Run `daily-screen` Job (`gcloud run jobs update --update-env-vars`), pointing at this same dashboard — confirmed via `gcloud run jobs describe` that the write landed with the right key/value. **Not independently re-verified on `gramunger.com/dashboards.html` itself:** doing so needs a fresh `daily-screen` execution to regenerate the report with the new env var baked in, and Cloud Run Jobs have no fast report-only override (unlike the k3s one-off Job trick used earlier this session) — forcing another full-universe screen just to confirm one URL string felt disproportionate given the exact same `config.GRAFANA_PRICES_URL`/`_render_dashboards()` code path was already thoroughly verified on k3s and this session's earlier `daily-screen` redeploy already proved prod correctly picks up new `report.py` logic. Will show up on the next real (scheduled or manual) `daily-screen` run regardless. |
| Pin the Infinity plugin on the already-LIVE prod GCE Grafana VM, not just this repo's bootstrap script | **done — confirmed live** | 2026-08-08 (pm-reviewer finding on the row above) — `deploy/gce/bootstrap.sh`'s `grafana` container installs the *same* unpinned `GF_INSTALL_PLUGINS=yesoreyeram-infinity-datasource` as the k3s manifest did (comment there already said "mirrors deploy/k8s/50-grafana.yaml's container env exactly"), meaning `gramunger.com`'s live, public dashboards page (via `34-82-149-71.sslip.io`, row 591 M17-P3) carries the identical v3.11.2 exposure — and unlike the k3s dev manifest, this one is already running in production. **Fixed the script** (now pins `yesoreyeram-infinity-datasource 3.11.1`, matching the k3s fix), but that only protects the *next* rebuild/restart of that VM — the currently-running container was provisioned before this fix existed and is not retroactively patched by an edit to a script sitting in this repo. **2026-08-08: applied, user-confirmed before recreating the live container.** Checked first via `docker inspect` — the running container was already on `3.11.1` (provisioned before v3.11.2 shipped, so this wasn't an active incident, just an unpinned latent risk against the *next* restart). Recreated it with the exact same env/network/mounts (captured live via `docker inspect`, not retyped from the bootstrap script, to avoid the same kind of drift risk found on k3s) plus the pin. **Rollback path (not stated originally, pm-reviewer finding):** `docker rm -f` doesn't remove the `grafana/grafana:11.3.0` image, and the pre-recreate `docker inspect` output was captured before acting — reverting is re-running the prior `docker run` (unpinned) from that captured config, no different in kind from the recreate itself. Downtime window was the gap between `docker rm -f` and the new container passing its own startup — single-digit seconds by the time of the first post-recreate check, not independently instrumented. **Confirmed live (raw output, not just prose):** `curl .../api/plugins/yesoreyeram-infinity-datasource/settings` → `version: 3.11.1`; `curl .../api/health` → `{"database":"ok","version":"11.3.0","commit":"d9455ff7db73b694db7d412e49a68bec767f2b5a"}`; `curl .../api/search` → the real `munger-account-pnl` dashboard object (re-provisioned correctly from its bind mount, not an empty list); `curl -o /dev/null -w '%{http_code}' https://gramunger.com/dashboards.html` → `200` through the Caddy/sslip.io embed path. |
| Revisit the Infinity plugin version pin (`yesoreyeram-infinity-datasource 3.11.1`, in both `deploy/k8s/50-grafana.yaml` and `deploy/gce/bootstrap.sh`) once upstream ships a fix | todo | 2026-08-08 (staff-engineer-reviewer finding, sharpened by pm-reviewer) — the pin's own comment says "re-evaluate ... once upstream ships a fix," but a comment is not a mechanism, and pm-reviewer correctly noted this row itself has the same weakness one level up: no cadence, no owner, nothing that surfaces itself. Accepted as manual tracking for now (matches this file's existing pattern for other deferred external checks, e.g. the Search Console re-crawl confirmation row) rather than building real automation for one plugin pin. Check `grafana/grafana-infinity-datasource#1772` (or the plugin's release notes for a 3.11.3+ that documents a fix) periodically, or whenever next touching either Grafana deploy file. |
| Audit the rest of the stack for other unpinned "install latest on every restart" dependencies | todo | 2026-08-08 (pm-reviewer finding) — the Infinity plugin pin was only caught because building the Prices panel happened to restart a Grafana container at the exact wrong moment; the underlying pattern (fetch "latest" of a third-party dependency on every container start, no pin) has existed since the account-pnl dashboard first shipped (2026-07-29) and was never audited as a class of risk, only patched reactively for the one instance found. Not yet done: check whether any other `docker run`/CronJob/bootstrap step in `deploy/` installs anything unpinned this way (the Grafana Infinity plugin is the only one *known* to do this, but it was never specifically searched for elsewhere). |
| P3 — prod Grafana on GCE e2-micro | done | 2026-07-28 — free-tier e2-micro at `grafana.gramunger.com`, authenticated GCS pull (instance SA, nothing public), cross-origin iframe with graceful degradation to a link when the VM is down + a rebuild runbook. **Cost gate cleared (row 510, 2026-07-30):** confirmed no other project on the shared billing account has ever enabled the Compute Engine API, so the account-wide one-e2-micro-free-tier allowance is fully unused — that check is region-independent (it's about whether the one allowance is already spent anywhere on the account, not about which region). **Built and verified live (2026-08-07):** `deploy/gce/` (bootstrap.sh, Caddyfile, Grafana provisioning, pull-pnl-history.sh) — an e2-micro in `us-west1-b`, not the originally-planned `us-central1` (`ZONE_RESOURCE_POOL_EXHAUSTED` on every `us-central1` zone). `us-west1` is still a free-tier-eligible region for e2-micro — GCP's Always Free tier is limited to exactly three regions (`us-west1`, `us-central1`, `us-east1`), a fixed, documented product policy rather than something that needed a live measurement the way the account-usage check above did. Container-Optimized OS, 4 plain `docker run --restart=always` containers (caddy, grafana, json-server, a systemd-timer GCS pull). **DNS plan changed**: `grafana.gramunger.com` never publishes — new subdomains on the `gramunger.com` Cloudflare zone return authoritative NXDOMAIN despite the API/dashboard accepting the record (same root cause that blocked `stats.`/`analytics.` earlier; account/edge-side, unresolved). Sidestepped with `34-82-149-71.sslip.io` (resolves to the VM's static IP with zero DNS config, still gets a real Let's Encrypt cert via HTTP-01). Two real bugs found and fixed getting there: COS's read-only root filesystem rejected `/opt/munger` (moved all paths to `/var/lib/munger`), and the same Infinity-plugin-ID typo as the k3s row above. **Confirmed live:** `curl https://34-82-149-71.sslip.io/` → `200` with `content-security-policy: frame-ancestors https://gramunger.com` present (the Caddyfile's iframe-scoping header, confirmed on the wire not just in config), `/api/health` → `{"database":"ok","version":"11.3.0"}`, dashboard search returns the real `munger-account-pnl` panel — GCS → pull → json-server → Grafana → real query results, end-to-end. **Not live-tested:** the graceful-degradation-to-a-link fallback for when the VM itself is down (`report.py`'s `_render_dashboards`) — implemented and code-reviewed, but confirming it live would mean deliberately taking the VM offline; left as implemented-not-drilled rather than force that disruption for a TASKS.md checkbox. |
| Cloudflare `gramunger.com` zone silently refuses new subdomains (standing infra defect, no owner) | todo | 2026-08-07 (pm-reviewer) — the same failure has now hit **three separate subdomains across two milestones**, each rediscovered and worked around individually rather than tracked as one standing issue: `stats.`/`analytics.` (M18's "GoatCounter on Unraid + DNS" row above — worked around with `count.` instead) and `grafana.` (M17's "prod Grafana on GCE e2-micro" row above — worked around with `sslip.io` instead). Symptom is consistent every time: the Cloudflare API/dashboard accepts the DNS record, but the zone's authoritative nameservers return NXDOMAIN for it. **Suspected cause corrected 2026-08-10:** the M18 row's "leftover Zero-Trust/Access" theory is disproven — a working credential (see below) confirms **Access is not enabled on this account at all** (`access.api.error.not_enabled` on every Access API call), so it structurally cannot be the cause. Root cause remains unidentified; a conflicting SaaS custom-hostname rule is still plausible and untested. **Credential problem resolved 2026-08-10** (was: "the Cloudflare API token in `.env` fails `/user/tokens/verify`" — still true, that exact token is still dead): a working credential now exists, saved in `.env` as `CLOUDFLARE_EMAIL`/`CLOUDFLARE_API_KEY` (a Global API Key, X-Auth-Key scheme, Super Administrator — verified live against `/user`). **Custom-hostname theory also disproven 2026-08-10** — `/zones/{id}/custom_hostnames` returns "No quota has been allocated for this zone or for this account," i.e. SSL for SaaS isn't even enabled; zero Page Rules either. **The bug is confirmed broader than "new subdomains," and confirmed to reproduce live on the apex record itself**, not just new subdomain creation: flipping `gramunger.com`'s existing A/AAAA records to `proxied: true` (for the Cloudflare Access row below) succeeded via the API — `dns_records` reports `proxied: true`, zone `status: active`, correct Cloudflare nameservers delegated — but real resolution (checked directly against `1.1.1.1`, Cloudflare's own resolver, not a caching issue) still returns the origin's direct IPs, unproxied, as if the change were never applied. Two plausible zone-level causes ruled out; root cause still unidentified. **This now looks like a genuine platform-level anomaly on this specific zone** (Free plan — support options for a root-cause dig are limited) rather than a misconfiguration this session can find and fix by inspecting more settings. Until root-caused, treat this zone as unable to reliably apply *any* Cloudflare-side change that depends on proxying (Access, a proxied subdomain, etc.) — assume the API/dashboard accepting a change means nothing about whether it actually takes effect, and verify independently (as this session did) every time. |
| Confirm `gramunger.com/dashboards.html` itself embeds the new Prices iframe (not just the GCE dashboard directly) | **done — confirmed live** | 2026-08-08 (pm-reviewer finding) — resolved on its own: the scheduled `daily-screen` run fired naturally after the env var was set. `curl https://gramunger.com/dashboards.html` shows both iframes (`munger-account-pnl` and `munger-prices`). |
| User visual confirmation of the live dashboards | todo | 2026-07-28 — per M13 row 354 precedent (reaffirmed M15/M16): a user-facing display feature is not "done" until the **user** looks at the live dashboards and confirms the two graphs show the right data. An agent smoke-test does not satisfy this. **Explicitly includes the Prices dashboard (Graph 2) once deployed** — this row predates that panel's build (2026-08-08) but is written broadly enough ("the two graphs") to already cover it; noted here so a future skim doesn't read this as P&L-only. |
| Pre-existing feed-retirement test debt | done | 2026-07-29 — **moved here from the M18 table (pm-reviewer finding, 2026-07-30): this is an M17 item** (the ~10 failing `test_report.py` cases were feed code removed in M17, tests left behind), it was just recorded under M18 because that's the session it got cleaned up in. Cleared by the M17 subagent (trimmed 2 mixed tests, deleted 8 pure-feed tests, fixed an E501). Suite now **274 passed, 0 failed** (as of that point in the session). Remaining non-feed nit: one pre-existing mypy `list[dict]` variance at `test_report.py:382` in the separate M15 export-controls WIP (not M18/M17). |

## M18 — Visitor metrics + robots.txt + SEO (added 2026-07-28, user request)

"Design website metrics gathering for example number of visitors, add robots.txt
and general seo." Full design in `DESIGN_WEB_ANALYTICS_SEO.md`. Metrics tooling
chosen with the user: **self-hosted GoatCounter** (cookieless → no consent
banner, sqlite, one Unraid container), over GA4 (cookies/consent + third-party)
and Umami (Postgres overhead). Cloud Run `request_count` kept as a free
cross-check only, not the product.

Everything routes through the existing config-gating pattern (mirrors
`GRAFANA_BASE_URL`/`MUNGER_GRAFANA_URL`): two new constants, empty by default so
**dev/local stays noindex, un-sitemapped, and uncounted**. Because `report.py`
bakes `<head>` content at generation time, the env vars live on the **`daily-screen`
Job** (which runs `report.py`), not the `report-web` nginx service. `robots.txt`
and `sitemap.xml` are generated into `REPORT_DIR` (must be at the served web
root — the GCS FUSE mount shadows anything baked into the nginx image), served
with no image rebuild.

| Task | Status | Date / Notes |
|---|---|---|
| Design doc | done | 2026-07-28 — `DESIGN_WEB_ANALYTICS_SEO.md`. **Process gap (flagged by pm-reviewer, 2026-07-30): this milestone shipped to production — real DNS, a real public container, prod env vars — before its review gates ever ran, breaking the precedent M17 itself set** (M17's design doc explicitly went through both gates *before* build). Review gates ran retroactively 2026-07-30 instead; findings are the five new rows below plus the doc's own stale `stats.gramunger.com` references (now corrected in the doc). Note for future milestones: this is the one exception, not a new precedent. |
| M18.2 — `config.py` constants | done | 2026-07-28 — `SITE_BASE_URL` (`MUNGER_SITE_URL`, `.rstrip("/")`) drives canonical/OG/sitemap/robots; `ANALYTICS_URL` (`MUNGER_ANALYTICS_URL`) drives the GoatCounter snippet. Both empty by default. |
| M18.3 — SEO `<head>` + analytics snippet | done | 2026-07-28 — `report.py` `_seo_head(title, description, path)` + `_analytics_snippet()` (read from config live, not a module constant, so tests can monkeypatch — like `GRAFANA_BASE_URL`). All 5 page heads converted; added `<html lang="en">`, `viewport` (was missing everywhere), description, and on prod canonical + Open Graph + Twitter + `index,follow` (dev → `noindex,nofollow`). **Bug caught in review:** titles/descriptions already carry entities (`&mdash;`, `P&amp;L`), so they're embedded raw — `html.escape` double-encoded them into visible `&amp;mdash;`. og:image intentionally omitted (no rasterizer; a 404'd image is worse than none). |
| M18.4 — `robots.txt` + `sitemap.xml` | done | 2026-07-28 — generated in `generate_report()`. `robots.txt` always (dev = `Disallow: /`; prod = allow + disallow `progress.json`/`screen_results_archive/` + `Sitemap:` line). `sitemap.xml` only when `SITE_BASE_URL` set; respects the `GRAFANA_BASE_URL` calendar↔dashboards fork; `lastmod` = generation date. |
| Tests | done | 2026-07-28 — 9 new `tests/test_report.py` cases (viewport/lang/description; noindex+no-canonical unset; canonical+OG set; trailing-slash join; robots dev vs prod; sitemap XML validity + fork; analytics present-iff-configured). All pass; ruff+mypy clean on changed files. Verified end-to-end via real `python report.py` in both modes. |
| M18.1 — GoatCounter on Unraid + DNS | done | 2026-07-29 — **LIVE at https://count.gramunger.com.** `arp242/goatcounter` on Unraid `marquee-net`, sqlite at `/mnt/user/appdata/goatcounter` (dir chown 1000:1000 — image runs as uid 1000), `serve -listen=:8080 -tls=proxy`. Fronted by the box's **Caddy** (host :8443) with an LE `*.gramunger.com` wildcard origin cert issued via a throwaway `neilpang/acme.sh` container (dns_cf); DNS-only A record → home IP, router forwards 443→8443. **Hostname is `count`, not `stats`:** `stats.` AND `analytics.` are silently unpublishable in this Cloudflare zone (API accepts, authoritative NS returns NXDOMAIN — likely leftover Zero-Trust/Access or SaaS custom-hostnames); any other subdomain publishes in seconds. **Side-quest:** enabling this surfaced that Unraid `br0` is ipvlan, so uptime-kuma couldn't reach host `192.168.1.36` → fixed by enabling `DOCKER_ALLOW_ACCESS="yes"` (Host access to custom networks). Also learned `caddy reload` no-ops on this box — need `docker restart caddy`. |
| M18.5 — og:image asset | todo | 2026-07-28 — deferred. No SVG rasterizer (PIL/cairosvg) installed and not worth adding; `og:image` omitted per design §3.3 until a preview card is wanted. |
| M18.6 — wire prod | done | 2026-07-29 — `MUNGER_SITE_URL=https://gramunger.com` + `MUNGER_ANALYTICS_URL=https://count.gramunger.com/count` set on the `daily-screen` Cloud Run Job via `--update-env-vars` (merged; `MUNGER_DATA_DIR`/`MUNGER_REPORT_BASE_URL`/progress-interval preserved). Beacon + robots.txt/sitemap/SEO bake into gramunger.com on the **next daily run** (not force-executed — slow/costly full-universe run). Optional Search Console (DNS TXT) + submit sitemap still open. |
| User visual confirmation of live analytics | todo | 2026-07-28 — per the M13/M17 precedent: not "done" until the **user** sees a real visit land in the GoatCounter dashboard and confirms robots.txt/sitemap.xml serve correctly on `gramunger.com` after the next daily run. |
| **Home-infra hardening from the retroactive M18 review — moved out of this repo (2026-07-30, user decision)** | done | staff-engineer-reviewer's retroactive M18 review flagged three home-network infra items (DNS-only exposure of `count.gramunger.com` to a home IP with no DDNS, a Caddy cert-renewal-doesn't-restart gap that could take down every `*.gramunger.com` subdomain at once, and no network segmentation for the GoatCounter container from the rest of the Unraid media-stack). Per user decision, home-infra changes are tracked and fixed in a separate `workspace/home-network` project, not here — munger's own scope is limited to (1) the in-repo config/code for analytics+SEO and (2) noticing and alerting if the analytics endpoint goes unreachable, not fixing the underlying home infra. Marked `done` because the *decision and redirect* are complete, not because the infra itself is fixed — that work explicitly does not belong in this file going forward. See the heartbeat-check row below for the notification side. |
| `heartbeat.yml`: alert if `count.gramunger.com` becomes unreachable | done | 2026-07-30 — added a step to the existing dead-man's-switch workflow (same GH-failure-email alert channel already used for a stalled scheduler) that `curl`s `ANALYTICS_URL`'s origin and fails the job on a non-2xx/timeout, gated on the `MUNGER_ANALYTICS_URL` **GitHub Actions repo variable** (a separate value from the same-named Cloud Run env var in M18.6 — this one had to be created fresh for the workflow to read; confirmed set via `gh variable list` → `https://count.gramunger.com/count`, not just written into the YAML and left unverified). Skips cleanly if unset, matching the config-gated pattern everywhere else. Munger only *notices and alerts* here — it does not attempt to fix anything about the home-network side; that is out of scope per the row above. **Confirmed for real via a manual `workflow_dispatch` run (`30564638787`, 2026-07-30):** log shows `Checking https://count.gramunger.com/` → `Analytics endpoint reachable.` — the step ran in actual CI against the live endpoint, not just a local dry run. |
| **New (pm-reviewer, retroactive M18 review, 2026-07-30): Search Console verification — still open, was untracked** | todo | `DESIGN_WEB_ANALYTICS_SEO.md` §3.6/§7.3 flags this as needing a decision (DNS TXT record, preferred, vs. HTML-meta) but it had no dedicated row anywhere — the only mention was a trailing clause inside the already-`done` M18.6 row, which is exactly where an open decision goes to be forgotten. Given this row now exists: verify ownership via DNS TXT and submit `sitemap.xml` once `gramunger.com` has baked the SEO changes in on a daily run. |
| **New (pm-reviewer, retroactive M18 review, 2026-07-30): "not investment advice" disclaimer — still open, was untracked anywhere** | **done — confirmed live on gramunger.com** | `DESIGN_WEB_ANALYTICS_SEO.md` §3.5/§7.5 explicitly pushes this content decision out of M18's scope (reasonable — it's a content question, not a code one) but it appears nowhere in `TASKS.md`, with no owner and no trigger to revisit it. This is a public site making stock-screening claims; flagging so the deferral is a tracked decision, not a silently dropped one. **2026-08-07, user request, resolved:** `report.py`'s new `_disclaimer_banner()` renders an unconditional "not investment advice" notice on all 5 pages (index/tickers/pnl/calendar/dashboards) — a standing banner, not the conditional staleness-style warning `_pnl_staleness_note` uses. Regression test (`test_disclaimer_banner_appears_on_every_page`) locks it into all 5 `_render_*` functions so a future refactor can't silently drop it (staff-engineer-reviewer finding: nothing previously would have caught that). Verified via a real local `report.generate_report()` render, not just the unit test. **2026-08-08: deployed and confirmed live** — see the `report-web` deploy row below for the full detail. |
| Remove dead/leaked references to the private GitHub repo's markdown docs from the public site | **done — confirmed live on gramunger.com** | 2026-08-07, user request — `report.py`'s methodology drawer linked `https://github.com/jimmyokusa/munger/blob/main/DESIGN.md#33-screener`; the repo is **private** (confirmed via `gh repo view`), so this was a dead 404 link for every visitor, not just an internal-detail leak. Removed the link (the drawer already contains the full Stage 1/Stage 2 detail inline, so the "Full detail" pointer was redundant, not just broken). Also removed a plain-text `(DESIGN.md section 4)` parenthetical from `pnl.html`. Grep-confirmed zero remaining `DESIGN.md` references in any rendered HTML output (only Python docstrings/comments, never emitted to a page). **2026-08-08: deployed and confirmed live** — `curl` against `gramunger.com` shows zero `DESIGN.md` references on both `index.html` and `pnl.html` (down from 2/1). |
| Google Search Console: "duplicate without user-selected canonical" + "page with redirect" errors on gramunger.com | **done — confirmed live on gramunger.com** | 2026-08-07, user-reported. Investigated live: `http://` → `https://` redirects (both apex and www) are expected/correct and account for the "page with redirect" entries, not a bug. Root cause of the duplicate-content flag: `https://gramunger.com/` and `https://www.gramunger.com/` both serve identical content as independent `200`s — only a `<link rel=canonical>` *hint* distinguishes them, no actual HTTP redirect, which Google doesn't always fully trust. Fix: `deploy/cloudrun/report-web/nginx.conf` now 301-redirects `www.gramunger.com` → bare apex (matches the existing canonical/sitemap/`config.SITE_BASE_URL`, all already apex-only). **Real bug caught in local testing before it could ship:** nginx falls back to the *first* `server{}` block as its implicit default when no `Host` header matches either `server_name` — with the new `www` redirect block defined first, it would have silently become the fallback and 301'd every request, apex included, not just `www`. Fixed with an explicit `listen 8080 default_server;` on the pre-existing content-serving block. Validated via `docker run nginx:alpine nginx -t` (syntax) and curl with explicit `Host` headers (`www.gramunger.com` → 301 to apex with path preserved; `gramunger.com`/unrecognized hosts → fall through to content, not redirect). Full local content-serving verification hit an unrelated macOS Docker Desktop bind-mount permission quirk (confirmed by testing as root, still failed) — not reproducible on the real GCS-FUSE-backed Cloud Run environment, not chased further. **2026-08-08: deployed and confirmed live** — the required post-deploy smoke test passed: `curl -I https://www.gramunger.com/` shows `301` to apex with path preserved (checked on `/` and `/tickers.html`), `curl -I https://gramunger.com/` still shows `200`. The actual Search Console "duplicate content" flag clearing is separate and still open — see the dedicated row for that below. |
| Rebuild + redeploy `report-web` **and the `daily-screen` regen it depends on** (Cloud Run) with the disclaimer banner, dead-DESIGN.md-link removal, and www→apex redirect | **done — confirmed live on gramunger.com** | 2026-08-07 — split out as its own row (pm-reviewer finding: the three rows above each said "not yet deployed" only in prose, with no single `todo`-status row a future scan would surface). All three ship together in one `report-web` image rebuild + Cloud Run redeploy — see `deploy/cloudrun/report-web/`. **2026-08-08:** deployed via `deploy/cloudrun/deploy.sh report-web --verify` (revision `report-web-00002-4zt`, passed its health check) — but content stayed stale (nginx.conf takes effect immediately, report.py's output does not, since `report-web` only serves the GCS-FUSE-mounted `report/` dir that the *separate* `daily-screen` Cloud Run Job writes). Also rebuilt/redeployed `daily-screen` (`deploy/cloudrun/deploy.sh daily-screen --verify`, a real full-universe execution, not just an image swap) to actually regenerate the report content — that execution's own Cloud Run status reports `succeededCount=1 failedCount=0` (per-ticker fetch-fraction not separately pulled from its logs, but the run completed cleanly per the platform's own accounting). **Confirmed live on the real domain:** the disclaimer banner renders on `gramunger.com`, zero `DESIGN.md` references remain on `index.html`/`pnl.html` (down from 2/1), and `https://www.gramunger.com/` → `301` → apex with the path preserved (checked on both `/` and `/tickers.html`). `dashboards.html` correctly shows only the P&L iframe (pointing at the prod GCE VM's Grafana) — the Prices section stays hidden as designed, since `MUNGER_GRAFANA_PRICES_URL` is not yet set on prod (see the GCE Prices-dashboard row above). |
| Confirm the Search Console "duplicate without user-selected canonical" flag actually clears after the redirect deploys | todo | 2026-08-07 — the `curl -I` smoke test on the row above only proves the redirect *mechanism* works, not that the reported Search Console error itself resolves (Google's re-crawl can lag a deploy by days to weeks). Same category of deferred-external-confirmation as row 593/624 (user-visual-confirmation precedent) — check back in Search Console after the redeploy and after Google has had time to re-crawl, not just the curl check. |

## M19 — Intraday P&L refresh (added 2026-08-10, user request)

User noticed `pnl.html`'s Unrealized P&L ($660.96) didn't match Alpaca's own
site ($413.72) at the same moment. Root cause investigated, not assumed:
`pnl.html` is a **once/day static snapshot** — `pnl.py` runs once in
`daily-trade.yml` (14:00 UTC) and `report.py` bakes its numbers into HTML
once in the `daily-screen` job (13:00 UTC); neither re-runs intraday, so the
page just reflects whatever the account looked like at that morning's
snapshot, not the live account Alpaca's own site queries in real time.

User's first ask was 5-minute updates while the market is open, "unless
that generates cost." Investigated and reported back: GCS/Cloud Run cost
impact would be negligible either way (Cloud Run's `report-web` already
live-mounts the bucket via FUSE), but **GitHub Actions minutes are the real
cost driver** — `pnl.py` can only run where `ALPACA_API_KEY` lives, and by
design (§3.5/M14's screen-only boundary, re-confirmed in this milestone's
DESIGN.md note) that is deliberately *only* GitHub Actions, never the
public-facing Cloud Run/k3s report deployment. This repo is private, so
Actions minutes are metered; a 5-min cadence during market hours was
estimated at ~1,700–3,400 extra runner-minutes/month, likely exceeding the
free quota. User chose **30 minutes** instead as the cost/freshness
tradeoff, keeping the security boundary intact rather than relaxing it.
**The arithmetic for the chosen cadence, not just the rejected one (pm-reviewer
finding):** 30 min during market hours is 1/6th of the 5-min estimate above —
roughly **280–570 extra runner-minutes/month** (~13 runs/day × ~22 trading
days), on top of whatever the three existing daily workflows
(`daily-trade.yml`/`daily-screen.yml`/`heartbeat.yml`) already consume. Not
independently confirmed against the account's actual metered quota/plan (no
billing-API access from this session), but an order of magnitude more likely
to fit than the rejected 5-min estimate — worth a real post-launch check (see
the dedicated row below) rather than treating this estimate as final.

**k3s dev-parity note (pm-reviewer finding):** the reported symptom was
specifically on `gramunger.com` (prod, Cloud Run) — the k3s dev deployment
isn't what the user was looking at. Extending the same cadence to k3s anyway
matches this project's established pattern (M17/M18 both shipped to dev and
prod together, `gcs_bridge.py` already exists specifically to keep k3s from
silently diverging from what GCS holds) and is cheap to do in the same pass
as the Cloud Run bridge work, not because the dev page itself was reported
broken.

Three parts, none of which alone would have fixed the user-visible symptom:
1. **New cron cadence** — a standalone `pnl-snapshot.yml` workflow (not
   folded into `daily-trade.yml`, which stays once/day for the actual
   trading run) at 30-min intervals across a UTC envelope wide enough to
   cover market hours under both DST and standard time, weekdays only, and
   gated by a new `pnl.market_is_open()` check (Alpaca's own `/clock`) so a
   tick inside the envelope but outside real trading hours (pre-market,
   after-hours, a holiday) is a fast no-op, not a wasted Alpaca
   fetch + GCS upload.
2. **Live-servable `pnl.json`** — discovered mid-design that cadence alone
   wouldn't have helped: `config.PNL_DATA_PATH` lives at `DATA_DIR` root,
   outside `REPORT_DIR`, which is the only tree either nginx (Cloud Run's
   root, and k3s's `subPath: report` container mount) actually serves — so
   a browser could never fetch a fresher `pnl.json` no matter how often it
   changed in GCS. Fixed by exposing it specifically (not the whole
   `DATA_DIR`, which also holds `journal.db`/`state.json`): a Cloud Run
   nginx `alias` for exactly `/pnl.json`, and a second REPORT_DIR copy from
   `gcs_bridge.py` for k3s (whose nginx container can't see outside
   `report/` at all — no alias is possible there), with the k3s bridge
   CronJob's own schedule bumped to match.
3. **Client-side refresh** — `report.py` bakes `pnl.html` once/day same as
   before (no change to that cadence — regenerating the whole site more
   often was rejected as unnecessary cost for refreshing one page), but the
   page now polls the now-servable `pnl.json` client-side and updates the
   already-rendered tiles/table in place, mirroring the established M13
   `progress.json` live-poll pattern rather than inventing a new one.

| Task | Status | Date / Notes |
|---|---|---|
| Design note in DESIGN.md (§3.8.1 addendum) + staff-engineer-reviewer pass | done | 2026-08-10 — added §3.8.1; review pass caught the durable-history truncation risk (see the `PNL_SNAPSHOT_ONLY` row below), the clock-call retry gap, the missing concurrency group, the nginx prefix-alias footgun, the naive 2s poll-interval risk, and the k3s CronJob deadline mismatch — all folded into the doc before code was written, per this project's design-before-code gate. Marker updated (`.claude/review-markers/design-staff-engineer.sha256`). |
| `pnl.py`: `market_is_open()` + `config.PNL_MARKET_HOURS_ONLY`/`PNL_SNAPSHOT_ONLY` env-gated skips in a new `main()` (extracted from `__main__` for direct unit-testability) | done | 2026-08-10 — `main()` is a no-op fast path when `PNL_MARKET_HOURS_ONLY` is set and `market_is_open()` reports closed; `PNL_SNAPSHOT_ONLY` skips `update_history_series()` entirely (the actual safety property this milestone depends on — the durable series stays `daily-trade.yml`'s exclusive responsibility). Both flags default `False`, so `daily-trade.yml`'s existing once/day call is unaffected — confirmed by a dedicated regression test. A shared `_with_retry()` helper wraps every Alpaca read (`generate_snapshot()`'s three calls and `market_is_open()`'s clock call), single retry, still fails closed after a second failure. |
| `.github/workflows/pnl-snapshot.yml`: 30-min cadence, DST-safe UTC envelope, weekdays only | done | 2026-08-10 — `*/30 13-21 * * 1-5`, its own `concurrency` group (mirroring `daily-trade.yml`'s), sets `MUNGER_PNL_MARKET_HOURS_ONLY=1`/`MUNGER_PNL_SNAPSHOT_ONLY=1`, uploads only `pnl.json`. YAML-validated. Not yet actually run in GitHub Actions — see the deploy row below. |
| Cloud Run `nginx.conf`: alias `/pnl.json` to the live FUSE-mounted file | done | 2026-08-10 — exact-match `location = /pnl.json` (not a prefix alias). Verified live with a real local `nginx:alpine` container (a named Docker volume, not a bind mount, after hitting the same macOS Docker Desktop bind-mount quirk this file's `www→apex redirect` row already noted): `/pnl.json` serves the mounted file, `/journal.db` and `/pnl.json/` both 404, `/` still serves the normal site — the "expose exactly one file, nothing else under `DATA_DIR`" claim confirmed, not just asserted. |
| k3s: `gcs_bridge.py` copies `pnl.json` into `REPORT_DIR` too; `40-gcs-reader-cronjob.yaml` cadence bumped to match | done | 2026-08-10 — k3s's nginx container has no filesystem visibility outside `REPORT_DIR` (its volumeMount is scoped to the PVC's `report` subPath), so unlike Cloud Run's alias this is a genuine second download, not a symlink. CronJob schedule `*/30 * * * *`, `startingDeadlineSeconds` shrunk 3600→300 (the old value would let a missed 30-min tick fire nearly an hour late at the new cadence). Not gated to market hours (no Alpaca call, no cost reason to narrow it). |
| `report.py`: element IDs on `pnl.html`'s tiles/table/staleness note + a client-side poller | done | 2026-08-10 — `_pnl_polling_script()` mirrors the M13 `progress.json` live-poll pattern at a 5-min interval (not that pattern's 2s, which staff-engineer-reviewer flagged as a real, uncosted ~1,800-req/hr-per-tab risk against data that only changes every 30 min). **Verified live in a real browser** (not just unit tests): a page baked with one set of numbers, on load, correctly overwrote itself with a different `pnl.json`'s equity/cash/today's-P&L/unrealized-P&L/position count/colors/staleness banner, with zero console errors. Second pass fixed a real drift bug the code-push review caught: the JS staleness check didn't reject a timezone-naive `generated_at` the way `_pnl_staleness_note`'s Python-side check does (JS's `Date()` silently "succeeds" on a naive string instead of raising) — fixed with an explicit offset regex, currently dormant in practice since `generate_snapshot()` always writes an offset-aware timestamp, but locked in by a source-marker test since no `node` runtime exists in the arm64 CI image to execute the JS directly. |
| Tests (`market_is_open`, `_with_retry`, the `main()` branching, the new `REPORT_DIR` bridge copy, the new element ids/poller markers) + full suite green | done | 2026-08-10 — 20 new tests across `test_pnl.py`/`test_gcs_bridge.py`/`test_report.py`; full suite **316 passing**, ruff+mypy clean on every changed file. One pre-existing test (`shows_no_staleness_warning_for_a_fresh_snapshot`) had to be tightened to check the rendered wrapper's emptiness rather than a page-wide substring search, since the poller's own embedded JS now legitimately contains the same banner wording as dead-but-present script text. |
| staff-engineer-reviewer pass on the full `.py` diff | done | 2026-08-10 — confirmed the core safety property (durable history untouched when `PNL_SNAPSHOT_ONLY` is set, unchanged default-flags behavior) is correctly implemented and tested, not just designed. Found and fixed the JS naive-timestamp mirror gap (see the `report.py` row above) and flagged the client-poller health-signal gap now tracked as its own row below. Marker updated (would be `.claude/review-markers/code-push-staff-engineer.sha`, local/untracked — not committed). |
| **Alert-noise risk from M16 row 554 (pm-reviewer finding) — addressed, not silently inherited** | todo | M16 row 554 accepted "no retry/backoff, any transient Alpaca blip fails the whole job and fires the standard GitHub-failure-email alert" as a deliberate for-now tradeoff at ~1 run/day. This milestone takes that same code path to ~13 calls/day during market hours — ~13x the exposure to one-off network blips, which would otherwise read as false-positive failure emails, not real incidents. Decision: a single shared retry-with-backoff helper wraps **every** Alpaca read in `pnl.py` — `generate_snapshot()`'s three calls (`get_account`/`get_all_positions`/`get_portfolio_history`, not two as an earlier draft of this row said) *and* the new `market_is_open()`'s `/clock` call, which runs on every tick including out-of-hours no-ops and is just as exposed (staff-engineer-reviewer finding: scoping the retry to only `generate_snapshot()` would have left the clock check, called most often of all, uncovered by the same fix meant to protect it). Plain retry loop, no new dependency — cheap, doesn't hide a genuinely broken pipeline (still fails loud, still alerts, after the retry is exhausted), reduces rather than eliminates false-positive risk. **Also newly scoped in (staff-engineer-reviewer finding): `pnl-snapshot.yml` must never call `update_history_series()` / touch `pnl_history.jsonl` at all** — that file's read-modify-write upsert is `daily-trade.yml`'s exclusive responsibility (with its own GCS-restore-before-write dance); replicating that dance 13x/day, or worse, omitting it and upserting from an ephemeral-runner empty local file, would silently truncate months of durable history on every intraday tick. The intraday job calls only `generate_snapshot()` + `write_snapshot()`. |
| **`_pnl_staleness_note`'s 48h threshold — gap acknowledged, tightening deferred (pm-reviewer finding)** | todo | The existing banner (M16) was tuned for a once/day cadence; at 30-min cadence it would take up to 48h of silent cron failure before a viewer sees any warning. A market-hours-aware tightened threshold was considered but explicitly **not** attempted this milestone: `report.py` deliberately holds no Alpaca credentials (§3.5/M14), so it cannot ask Alpaca's own clock whether the market is "supposed to be open right now" the way `pnl.py`'s new `market_is_open()` can — any tightened check here would have to approximate market hours with a bare UTC-day/time heuristic, and getting weekends/holidays right without false-positiving every Monday morning is meaningfully more work than this milestone's actual goal (make the common-case number fresh). Left as a named, deferred gap, not a silent one. |
| Deploy (GH Actions workflow live, Cloud Run `report-web`/`daily-screen` + k3s manifests redeployed) and user visual confirmation the page now tracks Alpaca intraday | todo | Per the M13/M17/M18 precedent — not done until the user actually sees it. **Concrete bar (pm-reviewer finding, modeled on M16 row 551):** a same-moment comparison between `pnl.html`'s Unrealized P&L and Alpaca's own site/app, expected to match within ordinary intraday price-tick drift plus up to ~30 minutes of snapshot lag (not exact-to-the-second) — not just "it looks like it's updating." |
| **The client-side live-refresh mechanism itself has no health signal (staff-engineer-reviewer finding, code-push review)** | todo | If the Cloud Run nginx alias, the k3s `REPORT_DIR` bridge copy, or `pnl-snapshot.yml`'s upload silently breaks, `pnl.html` simply falls back to showing the once/day static bake — which itself keeps refreshing normally via the unaffected `daily-trade.yml` job, so `_pnl_staleness_note`'s 48h banner never fires (the daily bake's own `generated_at` always stays fresh). The whole M19 feature could be dead for weeks with nothing on the page indicating it, discoverable only by eyeballing the number against Alpaca's own dashboard mid-market. Distinct from, and in addition to, the already-accepted "48h threshold too loose for 30-min cadence" gap above. Deferred, not fixed, for the same reason that gap was: `report.py` has no Alpaca access to build a real freshness check, and this milestone doesn't add browser-side monitoring/alerting infrastructure. Named here so it isn't rediscovered from scratch. |
| Post-launch: check actual GitHub Actions minutes consumed against the ~280–570/month estimate above | todo | pm-reviewer finding — the estimate driving the whole 5-min-vs-30-min negotiation was never checked against this account's real metered quota (no billing-API scope from this session). Confirm after a week or so of the new cadence running for real, not just trust the estimate. |

## M20 — Real-money trading page (added 2026-08-10, user request)

Full design in `DESIGN_REAL_MONEY.md`, which went through both review gates
(pm-reviewer + staff-engineer-reviewer) before build, per the M17/M19
precedent. User request: "a separate page, with separate graphs similar to
what we do for paper trading," on top of the user's existing KYC'd/funded
live Alpaca account.

**M20b (donations) and a subsequent broader reframing — DROPPED, per
legal advice (2026-08-10).** Sequence: a "pure gift, no strings attached"
Stripe donation button was fully designed as M20b, passed both engineering
review gates, and the user's legal review of that specific model passed.
The user then described a broader plan in the same session — donations
plus Google AdSense ad revenue plus affiliate-link revenue, all
accumulating toward one publicly-stated **$100,000 target**, with live
trading contingent on reaching it. That reframing was flagged as a
materially different legal fact pattern from individual undirected gifts
(a stated collective target multiple revenue streams count toward reads
closer to the "common enterprise" element of the Howey test) and had not
itself been reviewed. A background memo (informational only, drafted for
the user to bring to their attorney, not legal advice) was prepared on
that specific delta. **After that follow-up consultation, the user's
attorney advised against crowdfunding this account in any of these forms
— donations, ads, or affiliate revenue.** The user decided to fund the
live account entirely with their own capital instead. This drops M20b and
the entire funding-pipeline reframing outright, not deferred — no
donation button, no ads, no affiliate links, and no stated funding target
will ship anywhere on the site. `DESIGN_REAL_MONEY.md` §5 is kept as the
historical design record (it was sound, fully-reviewed engineering, killed
for a legal reason unrelated to its architecture) but nothing in it is
being built; see §0.1 there for the full account.

**M20 is now M20a only: live account + `real-money.html`, self-funded.**
Not legally gated — the design doc's phasing (§0/§7) always treated the
standalone real-money page as separate from the donation question, and
the user separately confirmed the legal review that did pass covered the
standalone page as well as the (now-dropped) donation model.

The staff-engineer-reviewer pass on the design doc found the first draft's
account-isolation claim didn't actually hold (git-branch/GCS-prefix
separation alone, without also isolating local `DATA_DIR`, would have let
paper and live share `state.json`/`journal.db` on a single-job
implementation) and a concrete reuse bug in the P&L-page plan (a hardcoded
poll target would have shown the wrong account's numbers on the live
page). Both fixed in the design before any code was written — see
`DESIGN_REAL_MONEY.md` §3–§4 for the corrected architecture this
milestone builds against.

| Task | Status | Date / Notes |
|---|---|---|
| Design doc + both review gates | done | 2026-08-10 — `DESIGN_REAL_MONEY.md`. pm-reviewer findings: explicit M20a/M20b phasing, verification-before-live bars (mirroring M16 row 551/M17 row 599), a technical off-by-default gate on the donate section rather than relying on human discipline alone (this project has a documented instance of a review gate being skipped under pressure — M18's design-doc-after-ship gap), refund/chargeback-aware donation totals kept separate from account equity, named owners/deadlines for every open decision. staff-engineer-reviewer findings: corrected account-isolation mechanism (separate GH Actions workflow file, not a second step in one job), a code-level global kill switch (escalated from a runbook note), an `account` column added to the journal schema plus an account-tagged `client_order_id` as independent defense-in-depth, reconciliation-mismatch handling decided as abort-not-warn for live, the P&L-page reuse bug (hardcoded fetch target/mode strings) fixed via parameterization, plus named GCS IAM and nginx gaps consistent with this project's own documented IAM-scoping history (M16/M17). |
| Legal review of the donation model, then the funding-pipeline reframing | done — reframing declined, donations dropped | 2026-08-10 — first review (donation model + standalone page) passed. Follow-up review (donations + ads + affiliate revenue toward a stated $100k target) came back negative — attorney advised against crowdfunding this account in any of these forms. Recorded as user-reported in both cases, no attorney/firm name or consultation notes on file in this repo. |
| M20b (donations) and the AdSense/affiliate/$100k-target reframing | **dropped** | 2026-08-10 — see the milestone intro above and `DESIGN_REAL_MONEY.md` §0.1. Not being built. |
| M20a — `journal.py`: `account` column + account-tagged `client_order_id` | done | 2026-08-10 — §3.4/§3.5. New `account` column with an explicit `PRAGMA table_info`-gated `ALTER TABLE` migration (real, not just `CREATE TABLE IF NOT EXISTS`, which is a no-op against the already-persisted production `journal.db` predating this column — verified by a test that builds a genuine pre-M20 schema by hand, not a self-fulfilling fixture) backfilling existing rows as `'paper'`. `get_expected_holdings`/`get_holdings_detail`/`check_reconciliation` all filter by account, not just tag on write — confirmed via a dedicated test that a live-only mismatch isn't masked by (or blamed on) paper's unrelated, legitimate holdings. `client_order_id` gets a matching `"{mode}-{run_date}-{symbol}-{side}"` prefix in `execution.py` — verified not a broker-level idempotency risk (paper/live are separate Alpaca endpoints via separate credentials) but real independent defense-in-depth if the journal-column defense is ever imperfect. |
| M20a — `config.py`: env-driven `PAPER_TRADING`, `LIVE_TRADING_ENABLED` gate, global kill-switch path, live credential/GCS-path constants | done | 2026-08-10 — §3.1/§3.2/§1.3. `MUNGER_PAPER_TRADING` defaults to today's exact hardcoded-`True` behavior when unset. **Fail-safe direction fixed in code-push review (staff-engineer-reviewer finding):** the first draft parsed as "anything other than exactly `'true'` means live" — a typo, stray whitespace, or an empty value from a future repo-variable/UI-set source would have silently *enabled* live trading. Inverted to "anything other than exactly `'false'` (trimmed, case-insensitive) stays paper," matching `DESIGN.md` §4's own fail-safe precedent; 6 new parametrized tests lock the safe direction for garbage input, plus one confirming incidental whitespace around a genuine `'false'` still works. `LIVE_TRADING_ENABLED` corrected from the design doc's original "derived from Alpaca credential presence" to a plain manually-set flag (`MUNGER_LIVE_TRADING_ENABLED`), since `report.py`'s deployment must never even reference an Alpaca credential name — a dedicated test scans `config.py`'s actual source (not just today's value) for `ALPACA_LIVE` to lock this in. |
| M20a — global kill switch + live reconciliation-abort posture | done | 2026-08-10 — §3.2 (`bot._global_kill_switch_active()`, checked before the per-account switch — confirmed the "checked first, unconditionally" ordering against the real control flow, not just a test) + §3.3 (mismatch aborts, not warns, for the live account — confirmed via a test asserting sell/buy evaluation never ran, not just that the exit code happened to be 1, which a mere warning also produces). Paper's existing warn-and-continue behavior (M10) is unchanged and separately tested to prove it still continues past a mismatch. |
| M20a — `report.py`: parameterize `_render_pnl`/`_pnl_polling_script` on mode; new `real-money.html` | done | 2026-08-10 — §4. Fixes the reviewed-and-caught reuse bug (hardcoded "paper" heading/disclaimer/SEO slug, hardcoded `fetch('pnl.json', ...)` poll target) via a new `__PNL_SNAPSHOT_URL__` template token and mode-aware keyword arguments. Paper's own call site passes today's exact values, so `pnl.html` is unchanged pre-`LIVE_TRADING_ENABLED`. **Two more real gaps found in code-push review, both fixed:** (1) nothing cross-checked a snapshot's own self-reported `mode` field against which page was rendering it — a misrouted upload could have silently shown the wrong mode badge; added an `expected_mode` parameter and a visible mismatch banner, mirroring `journal.py`'s account-column defense at the display layer instead of the trade-data layer. (2) the new nav link only connected `pnl.html`⇄`real-money.html`; `index.html`/`tickers.html`/`calendar.html`/`dashboards.html` had no path to the live page at all, regressing the M16 precedent (`DESIGN.md` §3.8: link added to *every* existing page, not one) — fixed, with a test asserting the link's presence on every page. Verified live in a real browser (both the initial paper↔live cross-navigation and, separately, the mismatch banner and its absence when modes agree). |
| M20a — serve `real_money.json` live (Cloud Run nginx alias + k3s `gcs_bridge.py` copy) | done | 2026-08-10 — mirrors M19's `pnl.json` treatment exactly (§3.8.1): a second, independent exact-match `location` block and bridge copy, not a generalization of the existing one. **Verified live with a real local `nginx:alpine` container** (named Docker volume, matching M19's own workaround for the macOS Docker Desktop bind-mount quirk): `/real_money.json` and `/pnl.json` both serve their own independent content, `/journal.db` and both files' trailing-slash variants still 404, `/` still serves the normal site. `gcs_bridge.py`'s real_money.json download is tolerant of `NotFound` (unlike `pnl.json`, which stays required/fail-loud) — a missing live snapshot is the expected state until the pipeline is actually provisioned, not a broken bridge. **GCS IAM scoping needs its own iteration, not an assumed extension (pm-reviewer finding, restoring a design-doc §4 warning an earlier draft of this row had dropped):** `live/pnl.json`/`live/pnl_history.jsonl` are new object names, not covered by the existing `munger-pnl-writer` IAM condition — this project's own history (M16/M17, four iterations total to scope `pnl.json`/`pnl_history.jsonl`/`prices.json` correctly) says expect an IAM iteration here too, not "same bridge pattern" implying "already permissioned." |
| M20a — `daily-trade-live.yml` workflow | done | 2026-08-10 — standalone workflow file (not a second job/step in `daily-trade.yml`), own `bot-state-live` branch, own `live/pnl.json`+`live/pnl_history.jsonl` GCS paths with its own download-before-upsert step (not `MUNGER_PNL_SNAPSHOT_ONLY`, which is M19's intraday-only flag). Cron staggered to 14:30 UTC (30 min after `daily-trade.yml`'s 14:00, itself an hour after `daily-screen.yml`'s 13:00) so none of the three collide on yfinance/Wikipedia rate limits. **Resolved the "TBD at implementation time" screening-mechanism question (pm-reviewer finding on the design doc):** runs its own fully independent `bot.py` invocation (own `screener.run_screen()` universe fetch), not a cross-workflow read of `daily-trade.yml`'s `screen_results.csv` — mirrors the precedent `daily-trade.yml` itself already set relative to `daily-screen.yml` ("runs its own independent screen... not a shared fetch"), accepting a second full fetch's worth of load for the sake of zero new cross-workflow coupling/artifact-passing failure modes. YAML-validated. **Inert without `ALPACA_LIVE_API_KEY`/`ALPACA_LIVE_SECRET_KEY`**, and its first real upload is expected to 403 until `munger-pnl-writer`'s IAM condition is widened to cover the new `live/`-prefixed object names (flagged explicitly in the workflow's own comments, matching the already-documented M16/M17 IAM-scoping precedent) — both user-side/live-infra steps this session cannot do, gating the workflow's first real (non-mocked, non-screen-only) run, not its being written. |
| M20a — tests + staff-engineer-reviewer pass on the `.py` diff | done | 2026-08-10 — full suite **354 passing**, ruff+mypy clean on every changed file. staff-engineer-reviewer pass confirmed the headline claim (state isolation between paper and live genuinely holds — separate workflow file, separate `bot-state-live` branch, disjoint GCS paths, a real tested journal.db migration) and found four real gaps, all fixed: (1) **high** — `daily-trade-live.yml`'s GCS upload step had no `set -e` and never checked `gcloud storage cp`'s exit code, so the *already-predicted* first-run 403 (IAM not yet widened, see the row above) would have shown green instead of failing loud, directly undermining the "verify before live" bar; fixed with `set -e`. (2) `config.PAPER_TRADING`'s fail-safe direction (see the config.py row above). (3)/(4) `report.py`'s mode-mismatch and nav-link gaps (see the report.py row above). Marker updated (`.claude/review-markers/code-push-staff-engineer.sha`, local/untracked). |
| **M20a — dry-run the safety mechanisms themselves before trusting them unattended (pm-reviewer finding — not previously a tracked task)** | todo | Every other verification bar in this milestone checks that the *displayed numbers* are right; nothing checks that the mechanisms escalated as mandatory in design review (§3.2's global kill switch, §3.3's reconciliation-abort) actually behave correctly against the real live workflow before real money is left running unattended on them. Concrete bar: trigger the global kill switch mid-run and confirm both `daily-trade.yml` and `daily-trade-live.yml` actually stop; force a reconciliation mismatch against the live account and confirm it aborts rather than warns-and-continues. Both before, not after, the first unattended live run. |
| **Real bug found and fixed: `config.REAL_MONEY_DATA_PATH`/`gcs_bridge.py` never matched what `daily-trade-live.yml` actually uploads** | done | 2026-08-10 — caught live during the k3s dev deploy (per the user's own "test on k3s before GCE" question), not by review: `gcs_bridge.py` looked for a GCS object literally named `real_money.json` at the bucket root, but the live workflow (already confirmed running for real, twice) uploads to `gs://munger-503515-data/live/pnl.json` — a completely different object. Every gcs-reader run since M20a's own deploy had been silently finding nothing to bridge; `real-money.html` would have rendered its "no snapshot yet" empty state forever, never the actual live account. Fixed by moving `REAL_MONEY_DATA_PATH` to `DATA_DIR/live/pnl.json` (mirroring the real GCS path exactly, matching `PNL_DATA_PATH`'s own "local path equals GCS object path" pattern that Cloud Run's FUSE mount depends on) and pointing `gcs_bridge.py` and the Cloud Run nginx alias at the corrected path; the public URL/poller target (`/real_money.json`) is unchanged, so no Cloudflare Access reconfiguration was needed. **The existing test for this (`test_bridge_downloads_real_money_json...`) would not have caught the bug** — its mock used the same wrong key the buggy code did; noted explicitly in the test now as a reminder that a test mirroring an implementation's own wrong assumption proves nothing. Full suite re-verified (354 passing) after the fix. |
| **M20a — deploy + user visual confirmation on k3s dev — done for k3s; Cloud Run prod still pending** | k3s: done — prod: todo | 2026-08-10 — k3s dev: full `build-and-deploy.sh` run (image rebuilt with the bug fix above, in-cluster CI passed, manifests applied including `MUNGER_LIVE_TRADING_ENABLED=1` on the daily-screen CronJob), `gcs-reader` bridge triggered manually and confirmed pulling `live/pnl.json` correctly, a report-only regen confirmed `real-money.html` renders with the real live account's data end-to-end (`$97.14` equity, `LIVE` badge, correct nav, `/real_money.json` independently fetchable) — verified via `curl` against the live NodePort, not assumed. **Concrete bar for Cloud Run prod (mirrors M16 row 551), still open:** live snapshot checked against Alpaca's own dashboard at the same moment, within ordinary intraday drift, on gramunger.com specifically — not yet done; needs `MUNGER_LIVE_TRADING_ENABLED=1` set on the Cloud Run `daily-screen` Job and a redeploy, same promotion step every prior milestone (M17/M18/M19) has followed after k3s dev confirmation. Still blocked on the same open items as before (live secrets/account-size decisions, the pre-existing `G` holding/reconciliation-abort question) for the account to actually *trade*, independent of the display page itself now working. |
| **Live secrets provisioned + IAM widened — confirmed live, not just written** | done | 2026-08-10 — `ALPACA_LIVE_API_KEY`/`ALPACA_LIVE_SECRET_KEY` added as repo secrets. **Two real manual `workflow_dispatch` runs (`screen_only=true`) against the actual live account, not mocks:** first run (`31433643108`) confirmed real credentials authenticate, the full 1,506-ticker screen runs cleanly, `KILL_SWITCH` correctly blocks all orders, and `pnl.py` generates a real snapshot from the live account ($97.14 equity) — but the GCS upload failed with the exact predicted 403 (`munger-pnl-writer`'s IAM condition didn't cover `live/`-prefixed objects), and thanks to the code-push review's `set -e` fix, failed the job loudly instead of passing silently. Fixed via a read-modify-write on the bucket's IAM policy (fetched current policy + etag, widened only the `pnl-objects-only` condition's OR expression to add `live/pnl.json`/`live/pnl_history.jsonl`, wrote it back — same narrow by-object-name scoping this project has used every time, not broadened further) — user-confirmed before applying, since it's a live IAM change. **Second run (`31434939497`) confirmed the fix for real:** upload succeeded, `gs://munger-503515-data/live/pnl.json` verified readable with real account data. |
| **New finding from the live dry-run: a pre-existing, un-journaled holding will block every future live run** | todo | The live account already holds a real position (`G`, 1 share, ~$34) from before this bot ever touched it — confirmed via `gcloud storage cat` on the real uploaded snapshot. `bot-state-live`'s `journal.db` doesn't exist yet at all (screen-only mode returns before the reconciliation check ever runs, so nothing's been persisted) — meaning the *first* real (non-screen-only) live run will find `G` on the broker with zero journal history for it, and the new M20a abort-on-mismatch logic (§3.3) will correctly, safely abort — repeatedly, every day, until this is resolved. Not itself a bug (the safety mechanism is working exactly as designed), but an open operational question only the user can decide: sell `G` in Alpaca directly first (cleanest), or something else. Nothing trades either way until it's addressed. |
| **`real-money.html` gated behind Cloudflare Access, not left fully public — design decision reversed** | todo | 2026-08-10, user decision, after the page was already live and verified working — supersedes `DESIGN_REAL_MONEY.md` §6's original "yes, real numbers go public, deliberately" call (see that section for the full reasoning; it made sense when public donations were still the funding model, less so now that M20b is dropped and the account is self-funded). Chosen over HTTP Basic Auth (nginx) and a real app-level login page specifically because Access needs **zero code changes** to this app (edge-level, intercepts before Cloud Run) and sidesteps the standing Cloudflare subdomain-publishing bug (path-based gating on the existing `gramunger.com` hostname, no new subdomain needed). **Real gotcha found before it could ship as a half-fix:** `real-money.html` and `real_money.json` don't share a path prefix (hyphen vs. underscore) — the Access Application must explicitly cover both, or the raw JSON stays fetchable even with the HTML page locked down. **API credential blocker resolved 2026-08-10** — a working Cloudflare credential now exists (see the standing-subdomain-defect row above). **User enabled Zero Trust Access in the dashboard (the one first-time-enable step the API can't do); both Access Applications (`gramunger.com/real-money.html`, `gramunger.com/real_money.json`) and an allow-only-account-owner policy on each were then created via API and confirmed present** (`GET /access/apps` shows both, each with one `decision: allow` policy scoped to the user's email). **Blocked on the same standing zone defect, not on Access config itself:** Access can only intercept *proxied* traffic, and `gramunger.com`'s DNS records were `proxied: false` (direct to Cloud Run's IPs) — flipped to `true` via API (user-confirmed first, given SSL mode is already `full` so the classic redirect-loop risk doesn't apply), confirmed accepted by the API (`proxied: true`, zone `status: active`, correct nameservers), but **real-world DNS resolution never changed** (checked directly against Cloudflare's own `1.1.1.1` resolver) — the exact same "accepted but not actually applied" symptom as the standing subdomain bug, now confirmed to also hit an existing record's proxy toggle, not just new record creation. **The site itself was never at risk** (since the change never actually took effect, `gramunger.com` kept resolving exactly as before throughout — verified 200s on every existing page immediately after). Access is fully configured and will start working the moment the underlying zone bug is resolved (see the row above) — nothing further to build here, this is now purely blocked on that platform-level issue. **Left `proxied: true` in place** (not reverted) since it's currently inert either way and would just need re-doing if the zone bug resolves itself. |
| **Munger's k3s Grafana moved from `grafana.k8s.blocksmashers.com` to `grafana.k8s.gramunger.com` — done, confirmed live** | done | 2026-08-10, user request. The old hostname was a DNS-only (LAN IP, `10.0.20.10`) convenience record on `blocksmashers.com`'s zone — unrelated to `gramunger.com`, and (discovered live, not assumed) had just been reassigned 8 minutes earlier to a completely different, unrelated home-monitoring Grafana instance (`home-grafana-ingress`, `monitoring` namespace) — munger's `munger-grafana-ingress` (`munger` namespace) had *already* been hand-edited to request `grafana.k8s.gramunger.com` as its host 2 days prior, waiting only on DNS. Added the missing DNS-only A record (`grafana.k8s.gramunger.com` → `10.0.20.10`) on the `gramunger.com` zone via the now-working Cloudflare credential — confirmed resolving correctly via three independent resolvers immediately (this specific record type/zone-change combination is unaffected by the standing proxied-record bug above). **Second real bug found and fixed along the way:** the cluster's shared `cert-manager` `ClusterIssuer` (`letsencrypt-prod`) failed to issue a cert for the new hostname with `10102: Max auth failures reached` — its `cloudflare-api-key-secret` (used for ACME DNS-01 challenges) held a stale, already-invalid Cloudflare credential, unrelated to anything built this session. This wasn't just blocking the new cert — it would have equally broken any future *renewal* of the cluster's existing `*.k8s.blocksmashers.com` wildcard cert too. Fixed by updating that one Secret's `api-key` field to the same working credential now in use elsewhere this session (explicit user confirmation obtained first — this was blocked once by an automated safety check on the initial attempt, re-run only after the user confirmed). A scoped `Certificate` (`grafana.k8s.gramunger.com` only, not a wildcard, matching this project's narrow-scoping convention) was issued successfully after the fix and wired into `munger-grafana-ingress` via an explicit `spec.tls` block — deliberately not relying on the cluster's shared default TLS store (which still correctly serves the *old* `*.k8s.blocksmashers.com` wildcard for other apps, untouched). `deploy/k8s/30-daily-screen-cronjob.yaml`'s `MUNGER_GRAFANA_URL`/`MUNGER_GRAFANA_PRICES_URL` updated from the bare NodePort IP (`http://10.0.20.10:30030/...`) to the real HTTPS hostname. **Confirmed live end-to-end, not just deployed:** `curl` shows a valid Let's Encrypt cert on `https://grafana.k8s.gramunger.com` and a correct `/api/health` response; the *old* hostname now correctly answers with the *other* (home-monitoring) Grafana's health check, confirming no cross-talk; a report-only regen on k3s confirms `dashboards.html`'s two iframes now embed the new hostname, not the old one. |

## M21 — Discord loss alerts (added 2026-08-10, user request)

"Notify in discord when a stock we own is down > 10%." Considered and
**dropped real Prometheus + Alertmanager** — this project already evaluated
and dropped Prometheus once (M17, `DESIGN_DASHBOARDS.md`: "can't run free on
prod," "the user declined alerts") for cost/complexity reasons on the
4-node, ~900MB-RAM-each Pi cluster; standing up Alertmanager now would
reverse that decision for a problem a few lines in `pnl.py` already solves
with zero new infrastructure. User confirmed this tradeoff explicitly and
chose the lightweight path.

`pnl.py` already computes each position's `unrealized_plpc` on every run;
`check_position_loss_alerts()` flags anything at or below
`config.POSITION_LOSS_ALERT_THRESHOLD_PCT` (-10%) and POSTs a formatted
message to `config.DISCORD_WEBHOOK_URL` (stdlib `urllib.request`, no new
dependency). Applies to **both accounts** (paper and live), per user
choice — distinguished in the message by the account's own `mode` label.

**Spam avoidance without new dedup state:** gated on `not
config.PNL_SNAPSHOT_ONLY` — the exact same condition that already
distinguishes `pnl-snapshot.yml`'s 30-min intraday paper cadence from the
two canonical once-daily runs (`daily-trade.yml`/`daily-trade-live.yml`).
A position sitting below -10% for a week gets one alert/day, not up to 13,
without building any new persisted "have we already alerted" state.

| Task | Status | Date / Notes |
|---|---|---|
| Webhook saved | done | 2026-08-10 — as a GitHub Actions repo secret (`DISCORD_WEBHOOK_URL`) and locally in `.env`, not committed anywhere; never echoed back in any response. |
| `pnl.py`: `_breached_positions`/`_format_loss_alert`/`_send_discord_alert`/`check_position_loss_alerts` | done | 2026-08-10 — config-gated (empty `DISCORD_WEBHOOK_URL` = no-op, same shape as `GRAFANA_BASE_URL`/`ANALYTICS_URL`). A failed POST (network blip, bad webhook, Discord down) is logged and swallowed, not raised — unlike this module's core snapshot generation, which fails closed by design, a missed notification isn't worth failing the whole P&L pipeline over. A position with a missing/non-numeric field still gets flagged (it already passed the breach check) rather than silently dropped from the message. `config.POSITION_LOSS_ALERT_THRESHOLD_PCT = -0.10` is a named constant, not a hardcoded literal. |
| `main()` wiring: gated on `not PNL_SNAPSHOT_ONLY` | done | 2026-08-10 — placed inside the same `else` branch as `update_history_series()`, so it's structurally impossible for this to run from the intraday-only path without also running the durable-history upsert (both gated on the identical condition). |
| Workflow wiring | done | 2026-08-10 — `DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}` added to both `daily-trade.yml`'s and `daily-trade-live.yml`'s "Generate P&L snapshot" steps. YAML-validated. Not yet exercised by a real scheduled/dispatched run with an actual breached position — the unit tests mock the POST; a real end-to-end Discord message has not been sent (offered to the user, pending their go-ahead given it would post a real message to their real channel). |
| Tests | done | 2026-08-10 — 15 new tests (threshold boundary — at vs. past, not just past; missing/non-numeric data; message formatting incl. sign-before-`$` convention matching `report.py`'s own `_format_dollars`; sort order; real POST behavior via a mocked `urlopen`; swallowed-failure behavior; config-gated no-op; the `PNL_SNAPSHOT_ONLY` gating on `main()` itself, both directions). Full suite **365 passing**, ruff+mypy clean. |
| User visual/live confirmation | todo | Per this project's own precedent (M13/M16/M17/M18/M19/M20) — not done until the user sees a real Discord message land, or explicitly confirms the mocked test coverage is sufficient without one. |
| **This webhook was completely non-functional from day one — found and fixed under M22, not here** | done | 2026-08-12 — see M22's "Real, critical bug found on first end-to-end run" row below for the full story: `_send_discord_alert` never actually delivered a message, on any account, since this milestone shipped (a Cloudflare bot-fingerprint block on urllib's default User-Agent, silently swallowed by this function's own soft-fail `except` clause). Fixed there; recorded here too so an audit of this milestone in isolation doesn't miss it. Still open, independent of the fix: this webhook has still never been exercised through the *actual* `daily-trade.yml`/`daily-trade-live.yml` path with a real breached position (only a direct function call, both before and after the fix) — see M22's own open item on this. |

## M22 — Daily Claude news/performance digest (added 2026-08-10, user request)

"The next webhook will be used for Claude AI to give daily updates on our
owned stocks and their performance based on news and events." This is the
project's **first-ever call to a paid LLM API** — a genuinely new kind of
recurring cost, unlike everything else here, which runs entirely on
free-tier infra or already-provisioned services (M17's own Prometheus
rejection, M21 above). Investigated feasibility before designing anything:
confirmed live that Alpaca's News API (`alpaca.data.historical.news.
NewsClient`) works on this account's existing free tier with zero new
dependency or feed restriction (unlike `prices.py`'s historical-bars
IEX/SIP tier gap), and confirmed the new `ANTHROPIC_API_KEY` authenticates
and returns real completions once billing credits were added (first
attempt hit "credit balance too low" — a real Console billing gap, not a
key problem; resolved by the user, then re-verified with a live call).

**Mid-design pivot:** the user asked for the newly-added `buffett` subagent
(`.claude/agents/buffett.md` — a genuine Claude Code subagent using
WebSearch/WebFetch for deep-dive "is this stock a good buy" research) to be
used for this feature. Flagged the real constraint: a GitHub Actions cron
job has no Claude Code harness present, so it cannot invoke a `.claude/
agents/*.md` subagent definition directly — that only exists inside a live
Claude Code session. Resolved (user's explicit choice, over "run the real
subagent manually/periodically instead") by baking the `buffett` agent's
analytical framework (moat, management, financial health, margin-of-safety
discipline — reduced to a brief daily-digest scale, not a full 10-K
research pass) into `news_update.py`'s system prompt for one lightweight,
unattended, direct Anthropic API call per day. The real subagent remains
available for on-demand, interactive deep-dive analysis separately.

Model: Claude Opus 5, the user's explicit choice over Sonnet/Haiku after
being shown the cost tradeoff (trivial either way at this once-daily,
short-output volume).

**Cost estimate (not yet measured against real usage — pm-reviewer
finding):** at `NEWS_MAX_OUTPUT_TOKENS` = 4,096 output tokens and a
generously-estimated ~3,000 input tokens (system prompt + per-symbol
headlines/summaries at `TARGET_POSITION_COUNT` = 15 positions), Claude
Opus 5's $5/$25-per-million pricing puts one run at roughly $0.12,
worst-case (real usage should run well under the output ceiling most
days). Two runs/day (paper + live) is roughly **$0.24/day, ~$7/month** —
trivial relative to the account itself, but the project's first-ever
per-call metered cost, unlike everything else here. **Follow-up task
below:** check the Anthropic Console's actual metered spend after the
first week of real runs against this estimate, the same
estimate-then-reconcile discipline this file already applies to GitHub
Actions minutes.

**Explicit non-goal (pm-reviewer finding):** this digest's output never
feeds back into screening, buy/sell, or any other trading decision — no
code path connects `news_update.py` back into `screener.py`/`portfolio.py`/
`execution.py`, and none should ever be added. It is display-only
commentary, the same boundary `report.py`'s own dashboards already draw,
now stated explicitly rather than left as an implicit consequence of the
module graph. Guards against the `warren-buffett` review gate's own
standing concern — sentiment/narrative-driven logic creeping into a
long-term weighing-machine system — from entering through a new door.

**Human-checkpoint disclaimer (pm-reviewer finding, addressed in code):**
this posts fully autonomous, unmoderated, AI-generated investment-toned
commentary about a real-money live account with no human review step —
the digest message itself now opens with "_(AI-generated summary, not
reviewed or investment advice)_" so nobody who sees the Discord channel
mistakes it for vetted analysis. No other human-in-the-loop gate is
planned; a spot-check task is tracked in the table below instead of a
blocking review step, since this is a display feature, not a trading
action.

**Recorded, deliberate tradeoffs (pm-reviewer finding — "record it even
when deferring it," this file's own M8 precedent):** no retry/backoff on
the Anthropic call, unlike `pnl.py`'s `_with_retry` — a once-daily miss
is tolerable for a bonus display feature the way it wasn't for M19's
13x/day P&L bridge. No alerting on repeated consecutive soft-fails
either (e.g., a rotated/expired key silently degrading this feature for
days) — accepted the same way every other soft-fail step in these
workflows already is, on the same "bonus feature, not core pipeline"
reasoning; if this proves to matter in practice, add one, don't build it
speculatively now.

**New data-exposure surface (staff-engineer-reviewer finding, noted not
gated):** this is the first time real position/P&L data — including, via
`daily-trade-live.yml`, the *live* account's actual holdings and
unrealized P&L — leaves this system's boundary to a third-party API
(Anthropic), subject to that provider's own data-handling/retention
policy rather than this project's. Accepted as the necessary cost of the
feature (the whole point is Claude seeing the positions), not something
to design around, but worth being deliberate about rather than an
unnoticed side effect — headlines/summaries only (never full article
text) and short-lived per-call context, not a persisted store of
position history on the provider side.

Runs where `ALPACA_API_KEY`/`ANTHROPIC_API_KEY` live (`daily-trade.yml`/
`daily-trade-live.yml`, GitHub Actions) — never in `report.py`'s own
deployment (DESIGN.md 3.5/M14's screen-only boundary; `news_update.py`
follows `pnl.py`/`prices.py`'s existing precedent exactly). Applies to
**both accounts** (paper and live, user's recommended default — not
explicitly re-confirmed after the `buffett` pivot interrupted that
question, so worth a quick user sanity-check on the first real run), same
Discord channel, distinguished by a `PAPER`/`LIVE` label like M21's alert.

**Deliberately soft-fail, unlike `pnl.py`'s core snapshot generation:**
this is a bonus display feature riding alongside the trading run, not
something that should ever fail the job. A broken Alpaca News call, a
refused/rate-limited/errored Anthropic call, or a Discord outage is logged
and swallowed inside `news_update.run()`, never raised.

| Task | Status | Date / Notes |
|---|---|---|
| Feasibility investigation: Alpaca News API + Anthropic API key | done | 2026-08-10 — both confirmed with real, safe calls against real credentials before any code was written (this project's own "verify a credential works before relying on it" standard, applied here as "verify the whole approach works before designing around it"). |
| Webhook + `ANTHROPIC_API_KEY` saved | done | 2026-08-10 — `DISCORD_NEWS_WEBHOOK_URL` and `ANTHROPIC_API_KEY` both saved as GitHub Actions repo secrets, neither echoed back in any response. The Anthropic key was pasted directly into chat by the user — flagged the transcript-exposure risk explicitly (same as every prior live credential this session), consistent with, not a one-off exception to, this project's established handling. |
| `config.py`: `DISCORD_NEWS_WEBHOOK_URL`, `ANTHROPIC_API_KEY`, `NEWS_UPDATE_MODEL`, `NEWS_LOOKBACK_HOURS`, `NEWS_PER_SYMBOL_LIMIT`, `NEWS_MAX_OUTPUT_TOKENS` | done | 2026-08-10 — every threshold/toggle a named constant, matching this codebase's own convention (module docstring, section 5 of the workflow skill). Webhook/API-key empty-by-default and config-gated (same shape as `GRAFANA_BASE_URL`/`DISCORD_WEBHOOK_URL`) — unset either one and the whole digest no-ops. |
| `news_update.py`: `fetch_recent_news`/`generate_digest`/`run` | done | 2026-08-10 — new module, deliberately mirroring `prices.py`'s existing shape closely (reads `pnl.json` for the held-symbol list rather than re-fetching positions itself, so the two can never disagree about "held today"). One Alpaca News call for all held symbols at once (not N calls), `include_content=False` (headlines/summaries only — cheaper, and avoids paywalled full-article text), capped per-symbol via `NEWS_PER_SYMBOL_LIMIT`. Raises on a malformed/unexpected Alpaca response shape (same fail-closed posture as `pnl.py`/`prices.py`'s own response-type checks) — but that raise is caught by `run()`'s soft-fail wrapper, not propagated to fail the job. `stop_reason == "refusal"` from Claude is explicitly checked before reading response content (this skill's own guidance) and treated as a soft-fail case, not a crash. Discord message truncated (not multi-message-split) at ~1900 chars if a digest runs long. |
| Workflow wiring | done | 2026-08-10 — new "Generate daily news digest" step added to both `daily-trade.yml` (after `prices.py`) and `daily-trade-live.yml` (after `pnl.py`, which has no `prices.py` step to follow — see that workflow's own comment on why), each with its own account's Alpaca credentials plus the shared `ANTHROPIC_API_KEY`/`DISCORD_NEWS_WEBHOOK_URL`. `if: always()`, matching every other display-concern step in these workflows. YAML-validated. |
| Tests | done | 2026-08-10 — 20 new tests (symbol extraction/sort/skip-malformed; per-symbol news grouping and limit; headlines-only request shape; malformed-response rejection; digest text extraction and refusal handling; Discord post + truncation; every `run()` no-op/gating path; soft-fail swallowing across every exception class the pipeline can actually raise). Full suite **385 passing**, ruff+mypy clean on every changed file (two pre-existing, unrelated mypy findings in `test_config.py`/`test_report.py`/`test_pnl.py` confirmed via `git stash` comparison to predate this change). |
| `pm-reviewer` pass on this TASKS.md section | done | 2026-08-10 — findings: no quantified cost ceiling/reconciliation task for the project's first paid API (**high**); no human-checkpoint/disclaimer on autonomous investment-toned commentary against a real-money account (**high**); no stated non-goal against feeding trading decisions; no content-quality (vs. delivery-only) success criterion; retry/backoff and failure-visibility tradeoffs not explicitly recorded as deliberate. All folded in above/below: cost estimate + reconciliation task added, disclaimer line added to the actual Discord message (code change, not just documentation), non-goal stated explicitly, tradeoffs recorded as deliberate, content spot-check task added below. Marker updated (`.claude/review-markers/tasks-pm-reviewer.sha256`). |
| `staff-engineer-reviewer` pass on the `.py` diff | done | 2026-08-10 — found and fixed one real **high**-severity bug and two mediums, all confirmed by tracing actual SDK source, not speculative. **High:** `run()`'s soft-fail except tuple didn't catch `alpaca.common.exceptions.APIError` (a plain `Exception` subclass, not an `OSError`) — a rate-limited/entitlement-restricted Alpaca News call (this account has already hit exactly this shape of error once, on a sibling data client — see `PRICES_DATA_FEED`'s own comment) would have crashed the step outright, defeating the module's entire soft-fail purpose; fixed by adding it to the tuple. **Medium:** `_held_symbol_positions()`'s `assert isinstance(positions, list)` was called ahead of `run()`'s `try:` block, so a malformed `pnl.json` shape would raise an uncaught `AssertionError` at the one call site most exposed to another process's output; fixed by moving the call inside `try` and adding `AssertionError` to the caught tuple. **Medium:** the combined Alpaca News request's fixed `limit=50` could let a couple of unusually newsy held symbols crowd out the whole recency-sorted feed, silently starving other held symbols of real news that existed — indistinguishable downstream from those symbols genuinely having none; fixed by scaling the request limit with `len(symbols) * NEWS_PER_SYMBOL_LIMIT * 2`. **Low, also fixed:** no de-dupe of a duplicate symbol in `positions[]` (would have rendered a ticker's section twice — `prices.py`'s own `held_symbols()` already de-dupes, this now matches); no detection of `stop_reason == "max_tokens"` (a truncated digest would post mid-sentence with no indication — now appends an explicit truncation note). **Noted, not code — folded into the milestone intro above:** this is the first time real position/P&L data, including the live account's actual holdings, leaves the system boundary to a third-party API, a genuinely new data-exposure class worth being deliberate about. 5 new tests added for the fixes (dedupe, malformed-positions soft-fail, limit scaling, truncation note) — `test_news_update.py` goes from 20 to 25, bringing the full suite from **385 to 390 passing**; ruff+mypy clean. Marker updated (`.claude/review-markers/code-push-staff-engineer.sha`, local/untracked). |
| Reconcile actual Anthropic spend against the ~$7/month estimate | todo | Check the Anthropic Console's real metered usage after the first week of live runs (both accounts). **Note:** the ~$7/month estimate above was computed before this row's request-limit-scaling fix (which deliberately pulls in more news per newsy symbol than the flat `limit=50` it replaced), so real input-token usage may run somewhat above the original ~3,000-token assumption — treat this as the first live data point against a moving-target estimate, not a fixed one. If actual cost is a small multiple of the estimate, investigate why (prompt larger than expected, more thinking tokens than assumed) before letting it run unattended indefinitely. **M23 update (pm-reviewer finding):** this estimate is now doubly stale — cadence dropped from ~2 calls/day to ~1 call/month per account (~30x fewer calls), while `NEWS_PER_SYMBOL_LIMIT` (5→15) and `NEWS_LOOKBACK_HOURS` (24→744) both push per-call input tokens up. Net effect should be a large *decrease* in monthly spend despite the larger per-call prompt, but "~$7/month" no longer means anything concrete post-M23 — don't reconcile against it; wait for the first real monthly-cadence invoice line instead and treat that as the new baseline. |
| Spot-check digest content quality on the first several real runs | todo | Beyond "did a message land" (the existing user-visual-confirmation row below) — read the actual commentary against the real news it was given and sanity-check for hallucination or a claim not supported by the fetched headlines, at least a few times before treating this as fire-and-forget. **Concrete bar (pm-reviewer finding):** check the first 3 real digests (both accounts) against their fetched headlines; if any contains an unsupported claim, pause the feature (unset `ANTHROPIC_API_KEY` or `DISCORD_NEWS_WEBHOOK_URL` to no-op it) and tighten `_SYSTEM_PROMPT` before re-enabling — don't just log it and let an autonomous, unmoderated feed of a real-money account keep running past a caught hallucination. |
| Account scope confirmed: both paper and live | done | 2026-08-12 — user explicitly confirmed "both" (the recommended default this section had shipped ahead of confirmation, per the row above) once asked cleanly, separate from the `buffett`-agent pivot that interrupted the original question. |
| CI green: `mypy . --config-file mypy.ini` | done | 2026-08-12 — the real command Munger CI runs (not the bare `mypy <file>` calls used earlier while iterating) caught errors an unqualified local mypy call missed, including a **pre-existing break on `main` since 2026-08-10** (M19/M20a's own commit) that had gone unnoticed for two days: a `yield`-fixture typed `-> None` instead of `Iterator[None]` (`test_config.py`), four dict literals mypy inferred narrower than `dict[str, object]` due to invariance (`test_report.py`), and reaching through `pnl.py`/`news_update.py` to monkeypatch a submodule (`pnl.urllib.request`, `news_update.anthropic`), which mypy strict's `no_implicit_reexport` rejects — fixed by importing those names directly in the test files instead (behavior-identical: Python modules are singletons in `sys.modules`, confirmed by a dedicated staff-engineer-reviewer pass). Full suite **390 passing**, `mypy . --config-file mypy.ini` clean — confirmed against CI itself, not just claimed. |
| **Real, critical bug found on first end-to-end run: both Discord webhooks were silently non-functional — root cause fixed, code path confirmed live** | done | 2026-08-12 — triggered real `workflow_dispatch` runs of both `daily-trade.yml` and `daily-trade-live.yml` (screen-only, per this project's live-verification standard) to see an actual digest land. Both jobs reported green, including the "Generate daily news digest" steps — but green was misleading: this module's own soft-fail design (see the milestone intro above) swallows exactly this kind of failure by construction, so a passing step proves nothing about delivery on its own. Reading the actual step logs (not just the pass/fail status) showed the Anthropic call succeeding (`200 OK`) immediately followed by `WARNING:__main__:Daily news digest failed; skipping this run: HTTP Error 403: Forbidden` — a real digest was generated and never delivered, on both accounts. **Root cause, confirmed by direct reproduction:** Discord's webhook API sits behind Cloudflare, and Python `urllib`'s default `User-Agent` header (`Python-urllib/3.x`) trips Cloudflare's bot-fingerprint rule (error code 1010) — reproduced locally with a bare `urllib.request` call (`error code: 1010`), while an identical `curl` request to the same URL succeeded (`204`), isolating the cause to the User-Agent rather than the webhook URL, network, or credentials. **This is not new — it affects `pnl.py`'s M21 loss-alert webhook identically, and has silently affected it since the day M21 shipped** (`_send_discord_alert`'s own `except (urllib.error.URLError, ...)` swallows `HTTPError`, a `URLError` subclass, the same way; cross-referenced in M21's own section above). Neither webhook had ever actually been confirmed live before this run — M21's own TASKS.md row already noted "not yet exercised... a real Discord message has not been sent." Fixed by adding a new `config.DISCORD_USER_AGENT` constant (shared by both senders, so the fix and its value live in one place) and passing it as an explicit `User-Agent` header in both `pnl._send_discord_alert` and `news_update._post_discord_message`. **Verified three ways, not assumed:** (1) a raw `urllib` reproduction confirmed the exact failure and root cause; (2) a fixed reproduction with a custom `User-Agent` confirmed the theory (`204`); (3) the actual `pnl._send_discord_alert`/`news_update._post_discord_message` functions were called directly against both real webhook URLs after the code fix, and both succeeded — the code path is confirmed working end to end. **Not yet confirmed:** whether the user has actually looked at the Discord channel and seen the messages land and read acceptably — that is a separate, still-open step (see below); "confirmed live" here means the function calls succeeded, not that a human has verified the result. |
| pm-/staff-engineer-reviewer findings on the bug-fix diff, both addressed | done | 2026-08-12 — both reviewers converged on the same gap: the fix corrects today's specific trigger but leaves the *same silent-swallow structure* in place for any future Discord/Cloudflare rejection (a revoked webhook, Discord-side rate limiting, a new Cloudflare rule) — and this is exactly the risk this section's own earlier "no alerting on repeated soft-fails... if this proves to matter in practice, add one" tradeoff (see above) explicitly flagged as a future possibility, now confirmed as an actual occurrence rather than a hypothetical. **Fixed, not just noted:** both `_send_discord_alert`'s and `run()`'s except clauses now also emit a `::warning::` GitHub Actions workflow command alongside the existing `logger.warning` call — this surfaces any future swallowed Discord-send failure directly in the run's Annotations (visible without opening the full log, which is what let today's bug go unnoticed), for *any* cause, not just today's. Deliberately the smallest fix, not a persisted consecutive-failure counter with its own alert threshold — matches this project's established preference (M17's Prometheus rejection, M20's Alertmanager rejection) for the simplest mechanism over unrequested infrastructure; escalate to something heavier only if silent GH Actions annotations themselves prove insufficient in practice. **Second finding, also fixed:** the two new regression tests asserted the outgoing header equals `config.DISCORD_USER_AGENT` itself, which can't catch that constant degrading to empty/default alongside a future refactor — changed to assert a hardcoded literal independent of the config value, plus an explicit check that it isn't urllib's own default UA string. Full suite still **390 passing**, ruff+mypy clean. |
| Unifying lesson from both bugs found today (pm-reviewer finding) | noted | 2026-08-12 — the mypy CI break (above) and this Discord bug are the same underlying failure class: a check that looked fine against a convenient proxy (`mypy <file>` locally; a green workflow step) diverged from what actually matters (`mypy . --config-file mypy.ini`, the exact CI command; an actual message landing in Discord) — both sat unnoticed for days because nobody checked the real thing, only its stand-in. No new process change beyond what's already recorded above (the `::warning::` annotations, running mypy against the exact CI invocation going forward) — recorded here so the pattern itself is named once instead of left as two disconnected incident notes. |
| M21's loss-alert webhook: still never exercised through the real dispatched workflow with an actual breach | todo | Per the pm-reviewer finding — M22's digest got two real `workflow_dispatch` runs of the actual scheduled workflows (the run that surfaced the bug) plus a direct post-fix function call; M21's loss-alert path has only ever been exercised via a direct function call, never through `daily-trade.yml`/`daily-trade-live.yml` with a real position actually past the -10% threshold. Low urgency (the shared `_send_discord_alert`/User-Agent code path is now proven working via M22's real dispatched runs), but the account-specific trigger condition (`_breached_positions`, `check_position_loss_alerts`'s wiring into `main()`) is still unexercised outside unit tests with mocked positions. |
| User visual/live confirmation | done | 2026-08-12 — user confirmed directly: the digest message landed in Discord. Closes M22's last blocking verification step; the digest generation and Discord-post code paths are now confirmed working end-to-end both by direct function call and by the user's own eyes on the real channel. |

## M23 — News digest cadence: daily -> monthly (added 2026-08-12, user request)

User feedback right after confirming M22's digest landed live: daily updates
are too much noise. Asked for either weekly-on-Friday or monthly; presented
both as an explicit choice and the user picked **monthly**.

**Where the cadence lives:** `daily-trade.yml`/`daily-trade-live.yml` still
run every calendar day, unconditionally (`0 14 * * *`/`30 14 * * *`
unchanged) — the trading/`pnl.py`/`prices.py` steps are still daily. The
monthly cadence is enforced entirely inside `news_update.py`'s own `run()`,
via a new `_should_run_today()` helper gating on
`config.NEWS_UPDATE_DAY_OF_MONTH` (default `1`). Same "gate from Python,
not a second cron schedule" shape `config.PNL_SNAPSHOT_ONLY` already uses
for the intraday-vs-canonical distinction. Day 1 always occurs exactly once
per calendar month regardless of weekends/holidays, since the workflow
itself is not trading-day-gated -- no "last Friday of the month"-style edge
case to handle, unlike the weekly-Friday option that was on the table.

**Escape hatch for verification:** a new `force_news_digest`
`workflow_dispatch` boolean input (default `false`) on both workflows,
wired to `config.NEWS_UPDATE_FORCE_RUN`, lets a manual dispatch bypass the
day-of-month gate. Added specifically so the still-open "spot-check the
first 3 real digests" task (M22, above) doesn't require waiting up to 3
months under the new cadence -- without this, verifying content quality at
all before trusting the feature unattended would have taken until November.

**Other config changes to match the new cadence:** `NEWS_LOOKBACK_HOURS`
24 -> `24 * 31` (744, a full month with headroom); `NEWS_PER_SYMBOL_LIMIT`
5 -> 15 (a month accumulates meaningfully more news per symbol than a day
did -- scaled up, not precisely derived). `_SYSTEM_PROMPT` and
`_format_user_message` wording changed from "daily"/"today's news" to
"monthly"/"this month's news" throughout, including the Discord header
("... daily update" -> "... monthly update") and the no-news fallback line,
which now states the actual lookback period
(`config.NEWS_LOOKBACK_HOURS // 24` days) rather than a hardcoded "day".

| Task | Status | Date / Notes |
|---|---|---|
| Cadence decision: weekly-Friday vs. monthly | done | 2026-08-12 — presented as an explicit choice (weekly cuts calls ~5x vs. daily, monthly ~20-30x) rather than picked unilaterally, since it's a real product tradeoff (cost/noise vs. timeliness) with no objectively-correct answer. User chose monthly. |
| `config.py`: `NEWS_UPDATE_DAY_OF_MONTH`, `NEWS_UPDATE_FORCE_RUN`, `NEWS_LOOKBACK_HOURS`/`NEWS_PER_SYMBOL_LIMIT` updated | done | 2026-08-12 — every threshold/toggle a named constant, matching this codebase's own convention; both new constants documented with why (see module comments). |
| `news_update.py`: `_should_run_today()` + `run()` gating, wording updated to monthly | done | 2026-08-12 — `run()` no-ops before doing any work (webhook/key check, then the day-of-month/force-run check, then the positions check -- cheapest checks first, matching this module's existing order). `_should_run_today(today: datetime.date \| None = None)` takes the date explicitly rather than reading the clock internally, specifically so it's unit-testable without monkeypatching `datetime.datetime.now`. |
| Workflow wiring: `force_news_digest` input on both workflows | done | 2026-08-12 — identical (byte-for-byte) expression on both `daily-trade.yml` and `daily-trade-live.yml`, confirmed by staff-engineer-reviewer to have no copy-paste drift. YAML-validated (`yaml.safe_load`). |
| Tests | done | 2026-08-12 — 4 new tests: `_should_run_today` true/false on a fixed calendar date (via a real-function reference captured before the autouse fixture patches it, not a mock); `run()` no-op when not the trigger day and not forced; `run()` proceeds when forced despite not being the trigger day. Existing `_isolate_config` autouse fixture updated to default `_should_run_today` to always-True and `NEWS_UPDATE_FORCE_RUN` to `False`, so every pre-existing test stays date-independent. `test_format_user_message_...`/`test_run_posts_digest_with_mode_header` updated for the new wording/period text. Full suite **394 passing** (was 390), `ruff check`/`ruff format --check`/`mypy . --config-file mypy.ini` (the exact CI invocation) all clean. |
| `staff-engineer-reviewer` pass on the `.py`/workflow diff | done | 2026-08-12 — traced the GitHub Actions boolean-input expression by hand and confirmed it's correct (the `inputs.` context coerces a declared `type: boolean` to a real boolean, avoiding the truthy-string-on-scheduled-runs footgun other repos hit with `github.event.inputs`); confirmed no drift between the two workflow files; confirmed `NewsClient.get_news()` internally paginates (`page_limit=50` x `page_size=50` = up to 2,500 articles) so the new `limit` up to 450 from the `NEWS_PER_SYMBOL_LIMIT` bump is not silently truncated by a single-page cap (checked by reading `alpaca-py`'s actual source, not assumed) -- that flagged concern doesn't hold up. Two real findings below were initially filed as accepted tradeoffs; a pm-reviewer pass on that framing (below) pushed back, and the missed-month one was escalated to the user rather than decided unilaterally. |
| **pm-reviewer pushback: the missed-month risk shouldn't have been unilaterally "accepted" — escalated to the user** | done | 2026-08-12 — pm-reviewer's finding: unlike M22's daily cadence, where a missed run self-healed the next day for free, a missed trigger-day run under monthly cadence silently drops that whole month's news with no recovery path — a materially different risk than what the user was shown when choosing monthly over weekly (they saw a cost/noise tradeoff, not a "misses are now silent and unrecoverable" one). Filing it as "accepted" without asking was scope-creep-avoidance dressed up as a decision. Taken back to the user directly with three concrete options (widen the trigger window; accept as-is; build a real staleness check); user chose to widen the window. |
| **Fixed (not accepted): trigger window widened to catch a missed/late day-1 run, made safe by a small idempotency marker** | done | 2026-08-12 — `config.NEWS_UPDATE_GRACE_DAYS = 1` widens `_should_run_today()`'s window to `[NEWS_UPDATE_DAY_OF_MONTH, NEWS_UPDATE_DAY_OF_MONTH + NEWS_UPDATE_GRACE_DAYS]` (days 1-2), catching a GitHub `schedule` trigger that ran late or didn't fire on day 1. **Correction on the original framing of this fix:** a plain date-range gate with no memory would have fired on *both* days in the common case (day 1 succeeds, day 2 has no way to know that), turning into a guaranteed near-monthly duplicate post — reintroducing the exact noise complaint that started M23. Caught before implementing, not after — the fix instead adds `config.NEWS_DIGEST_STATE_PATH` (`news_digest_state.json`), a small "last_posted_month" marker written atomically (temp file + rename, matching `portfolio.py`'s `StateTracker.save()` precedent) by the new `_record_posted_month()` only after a real digest actually posts successfully (never on a soft-fail, so a genuinely failed day-1 run still leaves day 2 a real second chance). As a side effect, this also resolves the "duplicate post on manual re-run" risk below without any separate mechanism -- a same-day re-run without `force_news_digest` now correctly no-ops once a month has already posted. 4 new test functions (grace-window boundary true/false, already-posted-this-month suppresses the window and a later month is unaffected, malformed/missing state handled) plus 3 existing tests extended with new assertions (`run()` records only on success and not on soft-fail) — `test_news_update.py` goes from 29 to 33 test functions, full suite **394 -> 398 passing**. |
| **Critical bug caught by staff-engineer-reviewer before push: the new state marker was never actually being persisted** | done | 2026-08-12 — first implementation added `news_digest_state.json` to the *same* file list in both the restore step ("Prepare persisted bot-state[-live] branch") and the existing "Persist bot state[-live]" step, reusing that step's git-branch commit/push exactly like `state.json`/`journal.db`. Restoring there is correct. Persisting there is not: that step runs (and commits/pushes) *before* `news_update.py` ever executes later in the same job -- so every run would push whatever stale marker was restored at job start, while `_record_posted_month()`'s actual write only ever reached the ephemeral runner's local disk and was discarded when it was torn down. Concretely: day 1 would post and "record" the month locally, but day 2 would restore the *pre-day-1* state (still "not posted"), see `in_window=True, already_posted=False`, and post again -- reintroducing, in production, the exact near-guaranteed monthly duplicate the whole state-marker design exists to prevent. This is precisely the class of gap this project's review culture keeps catching (green step, wrong underlying behavior) -- caught here specifically because staff-engineer-reviewer was asked to trace persistence *across* runs, not just the Python logic within one. **Fixed:** reverted `news_digest_state.json` out of the original "Persist bot state[-live]" step's file list (back to `state.json`/`journal.db`/`munger.log` only, unchanged from before M23); added a new, dedicated "Persist news digest state" step immediately after "Generate monthly news digest" in both workflows, mirroring the original step's own git commands (same branch, same `--allow-empty` idiom) but scoped to just this one file and correctly ordered *after* the code that actually writes it. Full suite **398 passing**, `ruff check`/`ruff format --check`/`mypy . --config-file mypy.ini` (the exact CI invocation) all clean, both workflow YAMLs re-validated (`yaml.safe_load`). |
| **Accepted tradeoff: an unforced duplicate post is now structurally prevented; a `force_news_digest` re-run can still intentionally post twice** | noted | 2026-08-12 — the state marker above means a normal (non-forced) re-run on the same day now correctly no-ops. The only way to get two real digests in one month is deliberately: `force_news_digest` bypasses both the window and the already-posted check by design (that's the whole point of the escape hatch — spot-checking shouldn't be blocked by "already posted this month"). Accepted as intentional, not a gap. |
| **Live-verify the persist-timing fix itself, not just the unit tests** | done | 2026-08-12 — dispatched `daily-trade.yml` (paper, `screen_only=true`, `force_news_digest=true`; run `31658584996`). All steps green, no `::warning::` annotation anywhere in the run (the swallowed-failure signal `run()`'s soft-fail wrapper would have emitted), and the Anthropic call logged a real `200 OK`. **The load-bearing check, not just "step succeeded":** fetched `origin/bot-state` directly and read `news_digest_state.json` off it with `git show` — `{"last_posted_month": "2026-08"}`, exactly what `_record_posted_month()` should have written and pushed via the new "Persist news digest state" step (commit `51c94fb`, correctly ordered after "Generate monthly news digest" this time, unlike the buggy first draft). This is the concrete, direct proof the persist-timing bug is actually fixed in production, not just in the diff. **Deliberately not run a second dispatch to exercise the "already-posted suppresses a natural trigger" half** — that logic is pure Python with no YAML/persistence risk and is already covered by real (non-mocked) unit tests (`test_should_run_today_false_when_already_posted_this_month`); the one thing unit tests structurally couldn't catch (YAML step ordering) is exactly what this live dispatch just proved. Not run against `daily-trade-live.yml`/`bot-state-live` separately — identical code path and identical YAML fix in both workflow files, so the paper-account confirmation is the load-bearing evidence; re-verify live-side only if something about that workflow's own persistence ever looks different in practice. **Still open:** user's own visual confirmation that the actual Discord message landed and reads correctly (this run's own digest, not a future one) — the run's logs prove generation and the persist step's success, not that a human has seen the result. |
| Spot-check that a digest actually lands each month, not just the first few | todo | Extends M22's original "first 3 real digests" content-quality check (still open, now easier to run via `force_news_digest` without waiting a month). **Made concrete per pm-reviewer finding (was too passive to reliably catch a miss):** check within the first week of each calendar month that a digest landed around the 1st-2nd (both accounts); if it didn't, don't wait for next month's automatic trigger to "fix" it (it only covers its own ~31-day window, it won't backfill the gap) — run `force_news_digest` immediately instead. The grace window now makes an actual miss rarer, but doesn't make this check unnecessary — it only helps once, and this project has no backstop if the check itself gets skipped for a stretch. |

## M24 — Live-trading bug fixes: buy/sell gate integrity (added 2026-09-02)

Reimplemented from a review doc ("munger — Live-Trading Bug Fixes", authored
2026-08-27, found in Google Drive) written against a prior session's
`fix/live-trading-safety` patch that isn't reachable from this session (not
committed, not attached here) -- the doc's description of each fix is
precise enough (function names, exact logic, reasoning, test names) to
rebuild faithfully against current `main` (`dda9554`) rather than wait for
the original patch file to resurface. A temporary `GLOBAL_KILL_SWITCH` file
was committed straight to `main` first (commit `6b8edb1`), ahead of any of
this milestone's own review gates, specifically because the two bugs below
were confirmed to be live and unprotected -- `daily-trade-live.yml` runs on
a real daily cron with no kill switch present and `LIVE_TRADING_ENABLED`
gating nothing in `bot.py` (see Fix 4). That stopgap trades off against this
project's normal "don't commit ahead of review" discipline deliberately;
removing it is this milestone's own last step, once the real fix is
reviewed, tested, and merged -- not before.

**This is a rebuild from a description, not a recovered-and-verified
patch.** The original `.patch` never resurfaced. If it does later, it
should be diffed against what actually shipped here rather than assumed
identical -- not expected to matter (the doc was detailed enough to
reconstruct faithfully, and this section will be updated if a real
divergence turns up), but named so it isn't silently assumed away
(pm-reviewer finding).

**Retroactive check on the live account, before assuming "unprotected"
meant "already harmed": pulled the real `bot-state-live` git branch
(`origin/bot-state-live`) and read both `journal.db` (0 rows, ever) and
`munger.log` directly** (pm-reviewer finding -- this milestone was
otherwise purely forward-looking). Every single live run since M20 went
live (2026-08-16 through 2026-09-01, 13 runs) logged
`Run complete: 0 liquidations, 0 buys planned` -- the live account has
never held a position and never placed an order. **Neither bug actually
fired in production**; both were live-but-dormant, not live-and-harmful.
This doesn't reduce the urgency of fixing them before the account
actually gets funded/positioned, but it does mean there is no bad trade
to unwind or investigate as part of this milestone.

**Fix 1 -- top-ups bypassed the Graham entry gates (`portfolio.py::generate_buy_queue`).**
The top-up loop iterated `current_holdings` without consulting
`screen_results`, so it could add to a position the current screen
explicitly refuses to open -- averaging down into a deteriorating position
at any price, the exact thing the margin-of-safety gates exist to prevent.
Fix: build a `buyable_now` set from `screen_results["buyable"]` and skip
any holding not in it (logged, not silently dropped). A holding absent
from the screen entirely (delisting, symbol change) is conservatively
treated the same way -- not toppable, since buyability can't be confirmed
for a ticker the screen can't see. **Disposition (pm-reviewer finding):**
a skipped holding only logs at `INFO`, not an alert -- deliberate, since a
holding drifting out of buyability while still owned is the system's
normal, expected DESIGN.md §3.4 behavior ("growing out of cheap is
success"), not an anomaly; alerting on it daily would be alert fatigue
for routine operation, unlike Fix 2's `unresolved` case below, which is
genuinely anomalous. The position isn't silently stuck forever either --
it stays a normal HOLD and is topped up again automatically the next run
it re-qualifies as buyable.

**Fix 2 -- a data gap on a held position triggered false liquidation
(`portfolio.py::process_sells`, `bot.py`).** `fetch_metrics` returning
`None` was struck identically whether the cause was a genuine quality
failure, a delisting, a symbol change, an acquisition close, or a
transient yfinance failure -- and the existing fetch-fraction guard
doesn't catch a single bad ticker among many holdings (one miss among 15
is 93.3%, comfortably past the 90% floor). Fix: unreadable holdings are
collected separately as `unresolved`, hold their existing strike streak
steady (neither incremented nor reset -- absence of data is not evidence
either way), and surface via alert/non-zero exit rather than being struck.
Contract change: `process_sells` now returns `(to_liquidate, unresolved)`;
`_process_sells_if_data_is_healthy` propagates the tuple on all exit paths.
**Disposition path for `unresolved` (pm-reviewer finding -- not stated
originally):** the alert triggers `bot.run`'s existing non-zero exit,
which fails the GitHub Actions run and surfaces the failure notification
-- the same mechanism M20 already established for a reconciliation
mismatch (see that milestone's row above). No automatic action beyond
the alert: a ticker recurs as `unresolved` every run until either
yfinance data resolves on its own (transient failure, self-heals) or a
human investigates the named ticker and acts manually (confirms a
delisting/acquisition and closes the position by hand, or determines it
was transient and no action is needed). Its strike streak stays frozen
throughout, so it can't be auto-liquidated by attrition while stuck. This
matches the project's existing fail-loud/act-manually posture rather than
inventing a new auto-resolution path -- deliberately, since auto-acting
on ambiguous data is the exact failure mode this fix removes.

**Fix 3 -- strike cadence was calibrated for the wrong trading cadence
(`config.py`).** `STRIKES_TO_LIQUIDATE = 2` was sized for the quarterly
cadence `DESIGN.md` originally assumed; at the actual daily cadence it
means two consecutive daily yfinance reads is enough to force a real
liquidation, against yfinance's own acknowledged field-level flakiness.
`REBALANCE_DRIFT_BAND_PCT` already solves the equivalent buy-side noise
problem; nothing solved it sell-side. Fix per the review doc: retune to
`STRIKES_TO_LIQUIDATE = 10` (~2 trading weeks) as the smallest change that
stops the hair-trigger -- explicitly flagged by the doc's own author as a
judgment call, not a clean bug fix, with the more structurally faithful
fix (decoupling sell-evaluation cadence from the daily screen entirely,
onto its own weekly/monthly tick) left as an open design question for the
user rather than decided unilaterally here. Not deciding that larger
question is this milestone's own scope boundary, not an oversight.
**Explicit bundling decision (pm-reviewer finding -- was implicit):**
shipping Fix 3 in the same commit/review/merge cycle as the three
confirmed bugs, rather than splitting it into its own milestone, on the
grounds that the kill switch already gates *all four* fixes identically
until a human explicitly removes it and re-enables `LIVE_TRADING_ENABLED`
(Fix 4) -- bundling doesn't let a judgment call slip into production any
faster or any less visibly than shipping it alone would, since nothing
here auto-resumes live trading regardless. Splitting it out would mean a
second full review/test/push cycle for one three-line config change with
no offsetting safety benefit.

**Fix 4 -- `LIVE_TRADING_ENABLED` gated a webpage, not live trading
(`bot.py`).** `config.LIVE_TRADING_ENABLED` was referenced only in
`report.py`, to decide whether to render `real-money.html`. It reads like
a trading safety gate; it wasn't one -- the live workflow could place real
orders whenever Alpaca secrets were populated and no kill switch was set,
regardless of this flag. Fix: enforced in `bot.run`, after both kill-switch
checks and before `ExecutionModule` is constructed -- if
`not config.PAPER_TRADING and not config.LIVE_TRADING_ENABLED`, alert and
return screen-only, broker client never even constructed. Paper is
unaffected (the check only fires when `PAPER_TRADING` is false). Note:
`daily-trade-live.yml` does not currently set `MUNGER_LIVE_TRADING_ENABLED`
-- after this merges, the live workflow is screen-only until that env var
is added deliberately, on top of removing the kill switch. Both steps are
required to resume live trading, not just one.

**Deliberately not fixed this milestone** (per the review doc, carried
forward rather than silently dropped): `MAX_SINGLE_POSITION_WEIGHT` is
dead code (equal-weight 6.67% never reaches the 12% cap); no sector
constraint exists anywhere in `screener.py`/`portfolio.py`
(`EXCLUDED_SECTORS = ()`); the filing-analysis agent (EDGAR/XBRL) was
never implemented, so the single-product/litigation failure mode it would
catch is fully unmitigated; ROE at 30% score weight rewards buybacks
mechanically (shrinking equity raises ROE) with no ROIC/ROA cross-check.
Changing any of these is a position-sizing/scoring-thesis decision, not a
drive-by in a bug-fix milestone -- warrants its own milestone with a
`warren-buffett` pass if pursued.

**Still open per the review doc, carried forward as follow-up items, not
in this milestone's scope:** whether Alpaca actually accepts notional on
LIMIT orders is unverified against a real order (`execution.market_buy`'s
own docstring notes Alpaca's docs contradict themselves; if rejected,
`_submit_and_check` returns `None` and the run reports success with zero
fills -- a silent no-op); the live P&L upload's IAM condition doesn't
cover the `live/` prefix and 403s every run; the score's benchmark and
evaluation horizon are undefined, so nothing establishes the composite
score's 40%-ROE/8%-FCF-yield weighting predicts anything.

**Success criteria beyond tests+review+CI (pm-reviewer finding, against
M20's own row-784 precedent of dry-running safety mechanisms before
trusting them unattended):** unit tests prove the logic in isolation but
not that the real run wires it correctly end to end. Before the kill
switch comes off, this milestone also needs a screen-only `workflow_dispatch`
of `daily-trade-live.yml` (kill switch still on, so no orders regardless)
confirming from the real logs that: (1) a currently-non-buyable holding
(if any exist once the account is funded) logs the new "not topping up"
line rather than silently vanishing from consideration; (2) the run
completes with the new `LIVE_TRADING_ENABLED` check visibly evaluated
(log line or alert, not just "no crash"). A genuinely live-fire test of
Fix 2's `unresolved` path (a real held ticker returning no data) isn't
practical to force on demand -- covered by the real (non-mocked) unit
tests instead, same reasoning M23 used for its own unforceable half.

| Task | Status | Date / Notes |
|---|---|---|
| Temporary `GLOBAL_KILL_SWITCH` committed to `main`, both accounts halted | done | 2026-09-02 -- commit `6b8edb1`, ahead of this milestone's own review gates, given confirmed live exposure (see milestone intro). User explicitly confirmed adding it before it was pushed. |
| `pm-reviewer` pass on this section | done | 2026-09-02 -- findings folded in directly above (rebuild-vs-recovered-patch caveat, retroactive live-journal check, disposition paths for Fix 1/Fix 2, explicit Fix 3 bundling decision, live-verification success criteria). |
| Retroactive check: did either bug already cause a real bad trade on the live account? | done | 2026-09-02 -- checked directly, not assumed: `origin/bot-state-live`'s `journal.db` has 0 rows ever, and `munger.log` shows `0 liquidations, 0 buys planned` on all 13 live runs since 2026-08-16. Both bugs were live but dormant -- no bad trade occurred. |
| Fix 1: buy queue respects the current screen's `buyable` gate | done | 2026-09-02 -- `portfolio.generate_buy_queue`; 3 new tests. |
| Fix 2: `process_sells` splits liquidation from unresolved data gaps | done | 2026-09-02 -- contract change to `(to_liquidate, unresolved)`, propagated through `bot._process_sells_if_data_is_healthy` and `bot.run`; 2 tests inverted (not deleted), 2 new tests. |
| Fix 3: `STRIKES_TO_LIQUIDATE` retuned 2 -> 10 | done | 2026-09-02 -- config-driven test assertions (`config.STRIKES_TO_LIQUIDATE`, not hardcoded) so they survive the next retune. |
| Fix 4: `LIVE_TRADING_ENABLED` actually gates live order placement | done | 2026-09-02 -- gate placed in `bot.run` after both kill-switch checks, before `ExecutionModule` construction; 2 new tests (refuses without the flag, proceeds with it). |
| Tests (new + two inverted from asserting the old buggy behavior) | done | 2026-09-02 -- suite went 398 -> 406 passing. |
| `staff-engineer-reviewer` pass on the full diff | done | 2026-09-02 -- all four fixes confirmed correct against the real `screener.py` schema and every call/return path; two real findings, both addressed below rather than filed as accepted tradeoffs. |
| **staff-engineer-reviewer finding, fixed: the degraded-holdings-fetch abort silently exited 0** | done | 2026-09-02 -- `_process_sells_if_data_is_healthy`'s abort (>=10% of held tickers unreadable) only `logger.error()`'d, never called `_alert()` -- backwards relative to Fix 2's own goal, since a systemically bad fetch (arguably worse signal than one `unresolved` ticker) produced *weaker* operator-facing signal than a single unresolved ticker does. Fixed: now takes `alerts` and calls `_alert()`, exit 1. `test_run_skips_sell_evaluation_when_holdings_data_mostly_missing` updated from asserting `exit_code == 0` to `== 1`. |
| **staff-engineer-reviewer finding, noted not fixed: crash-recovery window between strike-reset and actual liquidation order** | noted | 2026-09-02 -- `process_sells` resets a liquidated ticker's strike streak to 0 and saves state *before* `bot.run` ever calls `exec_module.liquidate()`; a crash/kill in that narrow window loses all memory that a liquidation was decided, and the position's strike streak restarts from zero. Pre-existing (predates M24 -- the "reset now" logic is original M6/M7 design), but Fix 3 makes the consequence materially worse: re-detection of a genuinely quality-failing position now costs up to ~2 trading weeks (`STRIKES_TO_LIQUIDATE=10`) instead of ~2 days. Not fixed here: closing it properly means moving the strike-reset/save to *after* `exec_module.liquidate()` confirms, which changes the `portfolio.py`/`bot.py` boundary this milestone didn't otherwise touch -- a real fix, not a bug-fix-milestone drive-by. Fails conservatively (toward inaction, not double-selling), and `execution.py`'s own `client_order_id` idempotency check already prevents the more dangerous duplicate-order failure mode in the same crash window. Follow-up, not blocking this milestone. |
| **staff-engineer-reviewer finding, fixed: `DESIGN.md` §3.1 described the old contract** | done | 2026-09-02 -- still said `process_sells(current_holdings, new_market_data)` returns "a list of tickers to liquidate if strikes reach 2." Updated to the real `(to_liquidate, unresolved)` tuple, the `state` parameter, and `STRIKES_TO_LIQUIDATE=10`. `portfolio.py`'s own docstring was already accurate; this was doc-only drift. |
| Full suite green, `ruff check`/`ruff format --check`/`mypy . --config-file mypy.ini` clean | done | 2026-09-02 -- 406 passing, ruff check clean, mypy clean. `ruff format --check` flags 3 pre-existing files (`report.py`, `tests/test_config.py`, `tests/test_report.py`), none touched by this milestone -- matches the review doc's own note that this predates M24 and isn't in CI today. |
| Push, PR opened, merged | done | 2026-09-02 -- branch `fix/live-trading-safety` (commit `66ff1c8`, then a small TASKS.md follow-up `3bebe87`), PR [#2](https://github.com/jimmyokusa/munger/pull/2), squash-merged to `main` as `b48340d`. Local `main` fast-forwarded, feature branch deleted. |
| Live-verification `workflow_dispatch` of `daily-trade-live.yml` (kill switch still on) before it comes off | done, with a caveat | 2026-09-02 -- run [33641705798](https://github.com/jimmyokusa/munger/actions/runs/33641705798), all steps green, 9m58s, `screen_only=true` (belt-and-suspenders on top of `GLOBAL_KILL_SWITCH`). **Correction on what this actually proved:** the run correctly stopped at `GLOBAL_KILL_SWITCH active` (logged `2026-09-02T14:32:55Z WARNING ... GLOBAL_KILL_SWITCH active`) -- which fires *before* `_kill_switch_active()` and Fix 4's own `LIVE_TRADING_ENABLED` gate in `bot.run`'s check order. So this confirmed the merged M24 code runs end-to-end without errors (screen completes, state persists, no crash/import break), but did **not** actually exercise Fix 4's new gate or Fix 1's buyable-skip log line -- both are structurally unreachable while `GLOBAL_KILL_SWITCH` is present, which is every dispatch made "before the kill switch comes off" by construction. The "Success criteria" row above overclaimed what was achievable here; Fix 4 is verified by its two dedicated unit tests (`test_run_refuses_to_trade_live_without_the_live_trading_flag`, `test_run_trades_live_when_the_live_trading_flag_is_set`) instead, same reasoning M23 used for its own unforceable half (a live-fire test of Fix 4 structurally requires removing the kill switch first, which defeats the point of testing it beforehand). |
| Add `MUNGER_LIVE_TRADING_ENABLED` to `daily-trade-live.yml` and remove `GLOBAL_KILL_SWITCH` to actually resume live trading | todo | Two separate deliberate steps, not one -- see Fix 4. User decision, not automatic on merge -- not done as part of this session. |
| **GCP prod deploy: `daily-screen` Cloud Run Job rebuilt and repinned to the merged M24 code, verified live** | done | 2026-09-02 -- user request ("keep going until the full, new, fixed version is deployed and running on GCP"), independent of the GitHub Actions trading path (M24's actual fixes live in `bot.py`/`portfolio.py`, which `daily_screen.py` never imports for trading -- it's screen-only by construction, per DESIGN.md's split), but the shared image ships the whole repo, so this keeps GCP prod current with `main`. Used the existing `deploy/cloudrun/deploy.sh daily-screen --verify` (built via Cloud Build with `DOCKER_BUILDKIT=1`, pinned to digest `sha256:0ee20a04...`, not a bare `:latest` tag -- see that script's own header for why both matter). Verification execution `daily-screen-gvfv8` succeeded (`succeededCount=1`, ~14.7 min). Confirmed live, not just "job succeeded": `gramunger.com` returns `HTTP 200` and its rendered page shows today's date (2026-09-02), proving the new image's output actually reached the public site through the GCS FUSE mount, not just that the Job ran. `report-web` itself was not rebuilt -- correctly so, per its own Dockerfile's header comment: it bakes in no report content, only nginx config, which M24 didn't touch. |

## Ops — Paper-account Alpaca credential rotation + full reset (2026-09-02)

Two related account-hygiene actions taken mid-session, adjacent to M24
but not part of that milestone's own scope -- recorded separately so
they're not lost between M24's fix work and the Design v2.2 doc below.

| Task | Status | Date / Notes |
|---|---|---|
| Paper-account Alpaca key/secret rotated in GitHub Actions secrets | done, with gaps noted (pm-reviewer finding) | 2026-09-02 -- user provided the new key/secret pair directly in chat; `ALPACA_API_KEY`/`ALPACA_SECRET_KEY` updated via `gh secret set` (piped via stdin, never written to a file or echoed back in full). **Verified live, not assumed:** a direct `TradingClient.get_account()` call against the new credentials returned `status: ACTIVE`. Live-account secrets (`ALPACA_LIVE_API_KEY`/`ALPACA_LIVE_SECRET_KEY`) confirmed untouched -- only the two paper-account secret names were written. **Not confirmed:** why the rotation happened (routine replacement vs. a suspected leak was never stated, and wasn't asked), and whether the *old* key/secret pair was revoked on Alpaca's own side -- only that the new pair authenticates. If the old key leaked, it may still be live at Alpaca regardless of what's in GitHub secrets. No stated rollback path either, beyond re-requesting credentials from the user, if the new pair later turns out wrong. Low-stakes for a paper account specifically, but this touches the same secrets machinery the live-account path uses -- worth asking the user directly next time this comes up, not assuming routine. |
| User manually liquidated all paper-account positions | done | 2026-09-02 -- user's own action, taken directly on Alpaca, not through this codebase. Confirmed via the Alpaca API: paper account shows 0 open positions, $102,334.49 cash. |
| `state.json` on `bot-state` reset to `{}` | done, verified live | 2026-09-02 -- the persisted strike counters (`{"FOX": 1, "LPG": 1}`) were stale against the now-empty account (FOX/LPG were closed by hand, outside `process_sells`, so nothing reset their counters the normal way). Reset directly via a `git worktree` against `bot-state`, committed as `d218720`, pushed. **Verified live, not just committed (pm-reviewer finding: the other two rows in this table confirm live via a direct API check; this one initially only cited the commit):** `git fetch origin bot-state` + `git show origin/bot-state:state.json` read back `{}` directly off `origin`, same session, after the push. `journal.db` deliberately left untouched (out of this action's stated scope) -- the next paper run may log a harmless M10-standard reconciliation warning (expected holdings vs. an emptied broker account), which is existing, correct, non-fatal behavior, not a new bug. **Note for Design v2.2's own M25** ("reconcile the FOX/LPG divergence by hand"): this action likely already *is* that reconciliation for the `state.json` half of it -- before scheduling M25 as a fresh task, check this row and `DESIGN_V2.md`'s own M25 caveat first; both already flag the overlap, so treat "M25 not yet started" as the default assumption to verify, not the default fact. |

## Design v2.2 — authored, reviewed, and merged to `main` (added 2026-09-02, user request)

Not a code milestone -- a design document landing on `main`
(`DESIGN_V2.md`, 1152 lines), meant to supersede `DESIGN.md` once its
own forward milestone plan (M24-M49, continuing this file's own
numbering) is actually executed. Recorded here as its own section,
distinct from M24 and from the Ops section above, since it's a
substantial, independently-reviewable unit of work in its own right.

**Origin.** User pointed at a Google Doc ("munger — Design v2.2") that
wasn't written in this session -- it's a post-mortem written against a
real production trade journal from four weeks of paper trading
(2026-07-27 to 2026-08-10), diagnosing seven structural root causes
(one shared cadence for three jobs, collapsed no-data/bad-data/
bad-business states, open-loop execution, an unvalidated data source, no
falsification criteria, no durability/moat measure, no between-filings
event monitoring) and proposing a revised architecture, a moat-
persistence scoring component, a validation framework, and a tiered
material-event monitor. Pulled via the Google Drive connector, cleaned
from the Docs export into proper GFM (de-escaped, tables and the JSON
event-record example reconstructed), and committed to a new branch,
`design/v2.2`, off `main` post-M24.

**Full review round.** User asked for "all our agents" -- ran
`staff-engineer-reviewer`, `pm-reviewer`, and `warren-buffett` in
parallel against the imported doc, each explicitly instructed to give a
fresh independent read rather than defer to the doc's own embedded §4
review. All three surfaced real findings; results synthesized into an
Artifact
([design/v2.2 review round](https://claude.ai/code/artifact/22ae843e-db26-4a37-8661-bf2eec1a9075))
before any fix was applied, so the findings were visible before being
acted on. Highest-confidence convergent findings (independently reached
by 2+ reviewers): the doc was already stale against `main` (M24 listed
as pending, already merged; a stale `ruff format` count); the settlement
pass's "idempotent and blocking" language was policy, not a spec; Tier-2
qualitative flags auto-halved a live position's weight on a model's
judgment with no per-instance human sign-off, cutting against the doc's
own "veto or flag, never boost" framing (pm-reviewer and warren-buffett
converged on this independently, from process and investment-thesis
angles respectively); acceptance gates (XBRL/yfinance tolerance,
filing-agent precision/recall) were named with no threshold; M26 and
M29 were each several milestones bundled into one, contradicting the
doc's own "urgent and small" framing; the paper-restart baseline was
sequenced to have its process changed out from under it mid-validation;
and several new external dependencies (EDGAR/XBRL volume, a corporate-
action data source, the Tier-2 event-retrieval mechanism) were
unconfirmed, unlike this project's own D0-gate precedent for exactly
this kind of dependency.

**Fixes integrated.** Every concrete finding folded directly into
`DESIGN_V2.md` on the `design/v2.2` branch: currency corrections;
the settlement pass specified into five concrete rules (partial-fill
handling, query-failure-vs-genuinely-pending, reuse of the existing
`KILL_SWITCH` mechanism instead of new blocking behavior, a stale-block
escalation alert, idempotent re-run semantics); `state.json`'s lossy
integer-to-period migration given an explicit policy (reset in-flight
streaks to empty, don't synthesize a fabricated period) and a versioned
schema; the "evaluate touches broker: No" self-contradiction fixed to
"read-only"; two previously-unconfirmed dependencies given a concrete
candidate mechanism using infrastructure this project already has
(Alpaca's Assets API for corporate-action detection, Alpaca's News
API -- already integrated in `news_update.py` -- for Tier-2 event
retrieval). **Correction (pm-reviewer finding, this TASKS.md entry
originally said these were "closed" -- overstated):** reusing an
existing integration point is not the same as confirming it meets the
new use case. `DESIGN_V2.md`'s own milestone table already reflects
this correctly -- M29b tests corporate-action detection only against a
synthetic fixture, and M36 still requires EDGAR volume to be "measured
directly" before switchover -- so the design doc itself doesn't
overclaim here; this summary did, and is fixed above. Numeric thresholds
added everywhere a gate was named without one; and the Tier-2 split
that closes the auto-halving gap -- routine flags still auto-halve,
structural-threat flags (the GNTX class: a disclosed, dated, specific
threat to the core product category) now hold the position at its last
decision and route to a logged human call instead.

**PM planning pass.** A dedicated `pm-reviewer` planning pass (not a
review -- asked to produce an actual revised plan) split M26 into 5
independently-shippable milestones and M29 into 3, each sized against
this document's own M44-M47 granularity for calibration. It also caught
a real internal inconsistency: §3.7 gates the paper restart on §3.1-3.3
landing, but the milestone table sequenced the restart (Epic B) in
Tranche 1, *ahead of* Epic C (§3.1) -- contradicting the design's own
stated gate. Fixed by resequencing Epic D (data integrity) to run in
parallel with Epic A (the two touch disjoint modules -- `screener.py`/
`data.py` vs. `portfolio.py`/`execution.py`/`journal.py` -- with no real
dependency between them) and moving the restart itself into a new
Tranche 3, gated on Epic A + Epic C + Epic D all complete, so the paper
record takes one clean discontinuity, not two. Tranche count went from
3 to 4 as a mechanical result (the qualitative layer, unchanged in
content, is now Tranche 4). The original "roughly six months" estimate
was also re-derived from this project's own demonstrated milestone
velocity (`TASKS.md`'s own M20-M23 history) rather than re-asserted --
lands at the same rough magnitude (~5-6 months to the live-trading gate)
but for a stated, checkable reason: not because the work is large, but
because M33's own exit criterion requires a full fiscal quarter of
hand-verified data after the restart, a structural calendar floor no
amount of engineering speed shortens.

**Merged.** `design/v2.2` merged into `main` via a `--no-ff` merge
commit (`2147830`), preserving the branch's own commit history rather
than squashing it, since each commit on the branch (import, content
fixes, planning-pass restructure) documents a distinct stage of review
that's worth being able to `git log` back to individually. Branch
deleted locally and on `origin` after merge. `DESIGN_V2.md` is on `main`
now; its own M24-M49 are a forward plan, not started -- no corresponding
`TASKS.md` milestone rows exist yet for M25 onward (Design v2.2's own),
since none of that work has actually begun this session.

**Process gap, named plainly rather than smoothed over (pm-reviewer
finding):** the entire review -- three subagents, findings synthesis,
fix integration, the planning-pass restructure -- was agent-driven, and
the merge to `main` went directly (`git merge`), not through a PR the
way M24 went through PR #2. For a code change that's a minor process
variance; for a 1152-line document that will govern real judgment calls
later (moat-scoring weights, which Tier-2 flags count as
"structural-threat," a ~5-6 month sequencing commitment), it means
there's no PR thread for a future reader to audit, and nothing in this
record confirms the user read the final merged content end to end
before it landed, only that the user directed each step (review,
integrate, plan, merge) and saw the interim summaries along the way.
Mitigating factor: merging the doc changed no code and no running
behavior -- `DESIGN_V2.md` only starts to matter once M25 onward
actually begins, so there's a natural checkpoint before any of this
becomes consequential. Not fixed retroactively (redoing the merge
through a PR after the fact wouldn't add real review, just paperwork);
recorded so a future session doesn't assume this went through the same
human-reviewed-PR gate M24 did. If it matters, a read-through before
starting M25 closes the gap for real.

**No stated re-review trigger (pm-reviewer finding).** The doc has a
success bar for itself as a plan (authored, reviewed, merged) but no
stated condition for when it should be revisited mid-execution rather
than only at the ~5-6 month live-trading gate -- e.g. if M27's
regression coverage reveals the settlement-pass design in §3.3 doesn't
hold up, or if M40's GNTX-outranks-HRMY acceptance test fails. Not
resolved here; worth deciding explicitly once M25 actually starts,
rather than assuming the plan is right until the very end.

| Task | Status | Date / Notes |
|---|---|---|
| Import + clean the source Google Doc into `DESIGN_V2.md` | done | 2026-09-02 -- branch `design/v2.2`, commit `9182816`. |
| Full review round: `staff-engineer-reviewer`, `pm-reviewer`, `warren-buffett` | done | 2026-09-02 -- run in parallel, each an independent fresh read; synthesized into an Artifact before any fix applied. |
| Integrate the review round's fixes into `DESIGN_V2.md` | done | 2026-09-02 -- commit `404b3ba`. |
| `pm-reviewer` planning pass: split M26/M29, resequence Epic D, fix the restart-gate inconsistency, re-derive the time estimate | done | 2026-09-02 -- commit `06e1cfb`. |
| Merge `design/v2.2` into `main` | done | 2026-09-02 -- `--no-ff` merge commit `2147830`; branch deleted. |

## Design v2.2 execution: M26a-e, M27, M28 (added 2026-09-02, user request -- "implement all milestones and push to GCP")

First real implementation slice of `DESIGN_V2.md`'s §5 forward plan.
Epic A (Tranche 1) is being worked in the sequence the plan lays out;
recorded here as one section covering the milestones actually landed
this session rather than one row per milestone, since they shipped as a
single reviewed, tested unit and splitting the write-up after the fact
would just duplicate the design doc's own per-milestone descriptions.

**M26a-e -- closed-loop execution (Design v2.2 §3.3).** `journal.py`'s
single `journal` table split into `orders` (submitted) and `fills`
(settled), via a real `ALTER TABLE RENAME` migration -- tested against
`tests/fixtures/real_bot_state_journal.db`, a byte-for-byte copy of the
actual persisted paper-account journal, not only a synthetic fixture.
New `settlement.py` module polls order status, classifies into
pending/partially_filled/filled/expired/canceled, retries a query
failure with backoff, and journals the outcome idempotently
(`fills.client_order_id` as primary key, upserted). `portfolio.process_sells`
no longer resets a strike streak the moment liquidation is *decided* --
the real FOX/LPG root cause -- `bot.run` now resets only after
settlement confirms `"filled"`. A settlement query failure sets the
existing `KILL_SWITCH_FLAG_FILE_PATH` (reused, not reinvented) plus a
new `SETTLEMENT_BLOCKED_FLAG_FILE_PATH` marker so a later run can tell
a stuck settlement apart from a deliberate pause and escalate to a
Critical alert if the block outlives one cycle. `execution.py`'s
limit-price band widened 2% -> 5%, plus a new average-daily-volume
ceiling (`config.MAX_ORDER_PCT_OF_ADV`) on both buys and liquidations,
fetched from real Alpaca bars.

**M27 -- reconciliation gets teeth.** A broker/journal mismatch now
aborts for *both* accounts, not just live -- M20's original paper
carve-out (warn-and-continue) didn't anticipate this being the exact
check that would have caught the real FOX/LPG bug if it had been
allowed to actually stop the run. New regression test reproduces the
real divergence directly against the real fixture journal.
**Not fully closed** (`pm-reviewer` finding): `DESIGN_V2.md`'s own exit
criteria for M27 carries forward M24's open finding (b) -- "the
`bot-state-live` reconciliation path gets a dispatched-workflow
regression test, not only direct-call coverage." What actually shipped
is a direct-call unit-level regression test against the real fixture
journal; a `workflow_dispatch`-level test (the same live-verification
pattern M24 itself used) has not been built. Recorded here as
outstanding, not silently marked complete -- do before treating M27 as
fully meeting its own stated bar.

**M28 -- journal reason strings derived, not hardcoded.** Every buy
used to journal as `NEW_POSITION` regardless of whether the symbol was
already held -- the real bug that mislabeled six top-ups (HIG x2, ASO,
LPG, HRMY x2) as new positions. `bot.run` now checks membership in
`remaining_holdings` and journals `TOP_UP` vs `NEW_POSITION` correctly.

**`staff-engineer-reviewer` pass on the M26a-e diff, before continuing
to M27+ (real findings, all fixed, not just noted):**

| Finding | Fixed |
|---|---|
| Setting the kill-switch flag on a query failure only took effect on the *next* run -- the rest of *this* run (remaining liquidations, the entire buy queue) still placed orders on the same now-unverifiable position picture | `bot.run`'s liquidation and buy loops now both stop immediately on a genuine query failure, via a shared `_settle_and_react` helper's return value |
| Buy-side settlement failures were silently discarded -- no alert, no kill switch, `fills` left incomplete forever with zero signal | Same fail-closed treatment as the liquidation side now |
| A ticker whose liquidation fills *after* this run's synchronous settlement check (a real gap until M34's deferred settlement pass exists) left a stale nonzero strike count in `state.json` forever -- poisoning a future rebuy's first bad check into instant re-liquidation | New `StateTracker.tracked_tickers()` + `bot._reset_stale_strikes_for_tickers_no_longer_held`: reconciles tracked tickers against the broker's own current holdings directly every run: absence from `current_holdings` is real, broker-confirmed evidence the position is gone, independent of whether settlement ever resolved that specific order |
| `settle_order`'s `float()` conversion of Alpaca's response fields ran outside the retry/error handling -- a malformed numeric field crashed the whole run instead of going through the structured kill-switch path | Guarded individually; a bad field is recorded as `None` (unknown), not crashed on or silently coerced |
| ADV ceiling fails open (doesn't block) when volume data is missing, applied to liquidations too -- flagged as a real, disclosed tradeoff rather than a bug: a data gap on a thin, deteriorating name is exactly where the one control meant to prevent an oversized forced sale is most likely to fail open | Not changed -- accepted as-is per the code's own stated reasoning (secondary control, not primary defense; blocking every order on a data gap is judged worse than occasionally missing this one check). Recorded here as a live, not a resolved, tradeoff -- revisit if a real oversized-liquidation-on-thin-volume incident ever occurs |

| Task | Status | Date / Notes |
|---|---|---|
| M26a: orders/fills schema split + real migration | done | 2026-09-02 -- tested against the real fixture journal, not only synthetic. |
| M26b: settlement.py polling/classification/idempotent upsert | done | 2026-09-02 |
| M26c: strikes reset only on confirmed fill | done | 2026-09-02 -- regression test reproduces the exact FOX/LPG shape. |
| M26d: kill-switch + SETTLEMENT_BLOCKED + stale-block escalation | done | 2026-09-02 |
| M26e: wider limit-price band + ADV ceiling | done | 2026-09-02 |
| M27: reconciliation aborts both accounts | partially done | 2026-09-02 -- both-accounts abort + fixture-journal regression test done; dispatched-workflow-level test (M24's carried-forward finding (b)) still outstanding, see write-up above. |
| M28: TOP_UP vs NEW_POSITION derived correctly | done | 2026-09-02 |
| `staff-engineer-reviewer` pass + all findings fixed | done | 2026-09-02 -- 5 findings, 4 fixed, 1 accepted tradeoff (see table above). |
| Full suite green, `ruff check`/`ruff format`/`mypy . --config-file mypy.ini` clean | done | 2026-09-02 -- 439 passing (was 406 pre-M26). |
| Push to `main` and GCP redeploy | todo | Landing together with M29-M30/M36 below in one push; see that section's push/redeploy row. |

## Design v2.2 execution: M29a-c, M30 (added 2026-09-03, continuing the same `/goal` -- "all milestones are implemented and pushed into GCP")

Holding-state machine + corporate-action detection (Design v2.2 §3.2),
and the CI gap that let an unformatted diff slip through despite a
clean `ruff check`.

**M29a -- `HoldingState` enum + classifier.** New `portfolio.HoldingState`
(`HEALTHY`, `DETERIORATING`, `UNREADABLE`, `CORPORATE_ACTION`) and
`classify_holding_state(metrics, is_corporate_action)`. Corporate-action
status is checked *before* the metrics-quality checks, per §3.2 -- a
ticker mid corporate-action (halt, delisting, symbol change) is a
distinct failure mode from merely-bad or merely-missing fundamentals,
and conflating them would let the ordinary "unreadable data" strike
machinery run against a name where strikes are the wrong response.

**M29b -- corporate-action detection.** New
`execution.is_corporate_action(symbol)`, using Alpaca's own Assets API
(`asset.status != ACTIVE or not asset.tradable`) rather than a
yfinance-side signal -- the point of the check is to catch exactly the
case where the broker's view of a symbol has diverged from the market
data source's. Fails open on a query exception, consistent with the
project's existing fail-open convention for secondary risk signals
(documented in the function's own docstring, alongside the ADV ceiling).

**M29c -- wire detection into the sell/buy loop.**
`portfolio.process_sells` gains a `corporate_action_check` callback
parameter and now returns a 3-tuple (`to_liquidate, unresolved,
corporate_action`) instead of 2 -- a corporate-action ticker is neither
liquidated (the position may not even be sellable) nor topped up, it is
alerted on and left alone. `bot.run` excludes corporate-action tickers
from `remaining_holdings` (so they can't accidentally top up) and now
also passes them to `generate_buy_queue`'s new `exclude` parameter (see
the review finding below) so they can't be picked up as a *new*
position either.

**Not fully closed** (`pm-reviewer` finding, correcting my own
misreading of the design note): the safety-critical half of M29c's exit
criteria -- a corporate-action holding is never liquidated or topped
up -- is done and tested. The other half of the same exit criteria
("all four states are distinguishable in the persisted evaluate-run
artifact and on the corresponding site page") is **not** done. I had
written this off as deferred to M34; re-reading `DESIGN_V2.md`'s own
note (§5 Epic A table) says the opposite -- the display belongs in the
*existing* single daily loop's output now, and M34 only relocates its
cadence later, it does not create the display. `to_liquidate`/
`unresolved`/`corporate_action` are local lists inside `bot.run`,
surfaced only via Discord alert text, not persisted anywhere
`report.py` reads. Closing this needs: persisting a per-ticker holding
state somewhere `report.py` can read (state.json via `StateTracker` is
the natural place, alongside strikes) and a site-page rendering pass --
real scope, correctly its own reviewed increment, not a same-session
bolt-on. Tracked here as the concrete next step on M29c specifically,
ahead of general Epic A/D continuation.

**M30 -- CI format-check gap.** CI ran `ruff check` but never `ruff
format --check`, so a correctly-linted-but-inconsistently-formatted
diff could still merge. Added a "Ruff format" step to
`.github/workflows/ci.yml` between the existing "Ruff" and "Mypy"
steps.

**Second `staff-engineer-reviewer` pass, on the M29a-c diff (real
finding, fixed before proceeding):**

| Finding | Fixed |
|---|---|
| `generate_buy_queue`'s new-position loop could select a `CORPORATE_ACTION` ticker as a fresh `NEW_POSITION` buy. Removing it from `remaining_holdings` (passed in as `current_holdings`) makes it indistinguishable from "never held" to the loop's own `if ticker in current_holdings: continue` guard -- and because `is_corporate_action` is Alpaca-Assets-API-driven while `buyable` is yfinance-driven, a ticker can genuinely be both flagged as a corporate action *and* pass the ordinary buyability screen | Added `exclude: set[str] \| None = None` to `generate_buy_queue`, checked in both the top-up and the new-position loops; `bot.run` now calls it with `exclude=set(corporate_action)`. Also fixed a stale docstring on `journal.check_reconciliation` still describing M20's live-only abort instead of M27's both-accounts behavior. |
| No "outlived one cycle" escalation for a stuck *reconciliation* mismatch, unlike the parity mechanism `SETTLEMENT_BLOCKED_FLAG_FILE_PATH` already gives settlement query failures | Not fixed -- recorded as a known, live gap. A reconciliation abort already halts the run every time it recurs (fail-closed), so the gap is escalation-to-Critical-alert on persistence, not silent risk; revisit alongside M34's workflow split rather than as a standalone patch. |

**Third `staff-engineer-reviewer` pass, on the full M26-M30/M36 diff
before the combined push (real findings, all fixed):**

| Finding | Fixed |
|---|---|
| `journal.record_order` was a plain `INSERT`, not an upsert, unlike `record_fill`. `execution.py`'s `market_buy`/`liquidate` recover from a crash-and-restart by returning the *existing* broker order (not `None`), and `bot.py` unconditionally journals any non-`None` order -- a restart on the same run-date re-inserted a second `orders` audit row for the one broker order actually placed, silently duplicating the audit trail the docstring calls append-only-but-accurate | Added an idempotency guard: `record_order` now skips the insert (logs and returns) when a row for that `client_order_id` already exists; a `None` client_order_id (no idempotency key) always inserts, unchanged from every pre-M25 row's behavior |
| `market_buy`'s ADV-ceiling estimate divided notional by the buy's own (higher) limit price, not last trade price -- the inline comment claimed this made the estimate more conservative, but the arithmetic runs the opposite direction: dividing by a higher ceiling price yields *fewer* implied shares, understating what a same- or lower-price fill (which a DAY limit order routinely produces) would actually buy | Split `_last_trade_price` out of `_limit_price`; the ADV estimate now uses last trade price, reserving the wider limit price only for the order request itself. Comment corrected to state the real direction of the bias. Pinned regression test (`test_market_buy_adv_check_uses_last_trade_price_not_the_wider_limit_price`) proves a specific order that would wrongly pass the ceiling under the old (limit-price) estimate now correctly fails it |
| `xbrl.py`'s module docstring stated in the present tense that shadow-mode "runs alongside data.py" -- true of the design intent, not of the code: nothing calls `shadow_compare`/`fetch_company_facts` outside `tests/test_xbrl.py` yet, so a reader planning M37's hand-review step could wrongly believe there's already production log data to review | Reworded to state plainly that M36 builds and fixture-tests the module only; wiring it into a real run and accumulating a disagreement log to review is explicitly M37's job |

| Task | Status | Date / Notes |
|---|---|---|
| M29a: `HoldingState` enum + `classify_holding_state` | done | 2026-09-03 |
| M29b: `execution.is_corporate_action` (Alpaca Assets API, fails open) | done | 2026-09-03 |
| M29c: wired into `process_sells` / `bot.run`, buy-queue exclusion | partially done | 2026-09-03 -- exclusion + alerting done and tested; persisted-artifact + site-page display of holding state still outstanding, see write-up above (corrected from an earlier, wrong "deferred to M34" read of the design note). |
| M30: `ruff format --check` added to CI | done | 2026-09-03 |
| 2nd `staff-engineer-reviewer` pass + real finding fixed | done | 2026-09-03 -- corporate-action buy-queue leak closed via `generate_buy_queue(exclude=...)`; reconciliation-escalation-parity gap recorded, not fixed (see table above). |
| 3rd `staff-engineer-reviewer` pass (full M26-M30/M36 diff) + all 3 findings fixed | done | 2026-09-03 -- `record_order` idempotency, ADV last-trade-price estimate, `xbrl.py` docstring overstatement (see table above). |
| `pm-reviewer` pass on this TASKS.md write-up + findings corrected | done | 2026-09-03 -- M27/M29c/M36 "done" rows corrected to accurately reflect the design doc's own exit criteria; see each section's write-up. |
| Full suite green, `ruff check`/`ruff format --check`/`mypy . --config-file mypy.ini` clean | done | 2026-09-03 -- 479 passing (was 474 pre-3rd-review). |
| Push M26-M30 to `main` and GCP redeploy | todo | Next step this session. Landing all three sections (M26-M30, M36) as one combined push -- see the note below M36's table for why. |

## Design v2.2 execution: M36 -- XBRL shadow mode (added 2026-09-03, Epic D start)

First slice of Epic D (data integrity, §3.4). Builds the SEC EDGAR
client and the shadow-comparison mechanism; does not yet switch XBRL
to be the primary data source anywhere (that's M37).

**M36 -- `xbrl.py`: CIK lookup, companyfacts fetch, annual-value
extraction, gross-margin shadow comparison.** New module:
`load_cik_lookup` (disk-cached `company_tickers.json` ->
ticker-to-CIK), `fetch_company_facts` (fails soft on 404/network/JSON
errors -- a filer with no XBRL data, or a transient SEC outage, is not
a reason to abort a screen), `annual_values` (extracts 10-K annual
values for a concept), `gross_margin_from_xbrl`, and `shadow_compare`
(logs a `FieldDisagreement` when XBRL and yfinance disagree by more
than `config.XBRL_DISAGREEMENT_RELATIVE_TOLERANCE` /
`_ABSOLUTE_TOLERANCE_PP`, whichever bound is larger). Rate-limited to
`config.SEC_EDGAR_MAX_REQUESTS_PER_SECOND` (empirically verified live
against the real SEC EDGAR API as sustainable for a full ~1500-ticker
universe pass before this was hardcoded). Tested against real SEC
fixture data (`tests/fixtures/sec_company_tickers_sample.json`,
`sec_companyfacts_aapl_sample.json` -- trimmed real API responses, not
synthetic), following the project's existing precedent of testing
against real production/external data snapshots
(`tests/fixtures/real_bot_state_journal.db`).

**Two real data-correctness bugs found and fixed against the real AAPL
fixture, not merely noted:**

| Bug | Fixed |
|---|---|
| `annual_values` treated XBRL's `fp == "FY"` tag alone as proof of an annual period. Real 10-Ks embed quarterly sub-disclosures also tagged `fp: "FY"` -- extracted 55 "years" for AAPL instead of the real 19 | Added a date-span check: the concept's `start`-to-`end` interval must be 350-380 days |
| `gross_margin_from_xbrl` computed AAPL's margin as ~73.5% instead of the real ~46%, because it took each concept's independently-most-recent value. Apple stopped tagging `Revenues` after FY2018 (switched to `RevenueFromContractWithCustomerExcludingAssessedTax` under ASC 606), so a real FY2025 `GrossProfit` got divided by a stale FY2018 `Revenues` | Rewrote to match the same fiscal year across both concepts (with a fallback concept chain for revenue), not each concept's own latest value |

**Not fully closed against `DESIGN_V2.md`'s own M36 exit criteria**
(`pm-reviewer` finding): the design doc's bar for M36 is "both sources
then run a full cycle in parallel; per-field disagreement report ...
reviewed by hand before switchover" (M37). What actually shipped is the
`xbrl.py` module itself, live-verified against the real EDGAR rate
limit, and unit-tested against real fixture data for two names --
nothing in `bot.py`/`daily_screen.py`/`screener.py` calls
`shadow_compare` yet, so no full-universe parallel cycle has run and no
disagreement report exists to review. The module's own docstring
previously overstated this in the present tense ("runs alongside
data.py") -- corrected (see the review finding above). Marking M36
`partially done`: the client/extraction/comparison mechanism is real,
correct (two genuine bugs caught and fixed against real data), and
tested; wiring it into an actual run and producing the hand-reviewed
disagreement report is real remaining work, not administrative
follow-up, and is M37's actual prerequisite, not merely its neighbor.

| Task | Status | Date / Notes |
|---|---|---|
| M36: `xbrl.py` (CIK lookup, companyfacts fetch, annual-value extraction, gross-margin shadow compare) | partially done | 2026-09-03 -- module built, correct, and fixture-tested; not yet wired into any real run (see write-up above). |
| Live rate-limit verification against real SEC EDGAR API | done | 2026-09-03 -- confirmed sustainable for a full universe pass at the configured rate. |
| Real-fixture regression tests for both data-correctness bugs found | done | 2026-09-03 -- `tests/test_xbrl.py`, 21 tests. |
| `ruff check`/`ruff format --check`/`mypy . --config-file mypy.ini` clean | done | 2026-09-03 |
| Wire `shadow_compare` into a real run, accumulate + hand-review a full-universe disagreement report | todo | Owner: whoever continues Design v2.2 execution next (this session, if the `/goal` directive is still standing when work resumes) -- scheduled as the immediate next Epic D item after this push, not an indefinitely-deferred gap. The "hand-review" step specifically needs the user, not an agent, since it's the human checkpoint the design doc puts in front of making XBRL authoritative. |
| Push to `main` and GCP redeploy | todo | Next step this session -- landing together with M26-M30 (see note below). |
| M37: switch XBRL to primary + resolve GNTX margin discrepancy | todo | Blocked on the disagreement-report row above, not just sequentially next. |

**Why M26-M30 and M36 land as one combined push** (`pm-reviewer`
finding: three independently-reviewed slices -- Epic A execution
safety, Epic A cleanup, and the start of an unrelated Epic D module --
collapsed into one push makes isolating a post-deploy regression to one
slice harder than it needs to be, since everything upstream of this
point was otherwise kept separate: separate reviews, separate diffs,
separate write-ups). Judgment call, made explicitly rather than
silently: `GLOBAL_KILL_SWITCH_FLAG_FILE_PATH` remains active through
this entire push, so none of it can place a live order regardless of
deploy state -- the real safety net a staged rollout would otherwise
exist to provide is already in place independent of how these commits
are grouped. Weighed against that, splitting into three separate
branches/PRs/redeploys is solo-operator overhead (no second reviewer
gating a merge here) that doesn't buy additional safety given the kill
switch. If a post-deploy issue does surface, `git bisect` across the
three sections' own commits (each section's work is being committed
as a single deliberate commit) remains the fallback for isolating it.

**Scope of the kill-switch argument, stated precisely** (`pm-reviewer`
verification-pass finding): it only contains the *order-placement*
failure mode -- an accidental live buy/sell. It says nothing about a
non-order-placement regression from this same push (a `report.py`
display bug, a `journal.py` migration issue, `screener.py` reading
`xbrl.py` incorrectly once M37 wires it in): those aren't gated by the
kill switch at all, and `git bisect`/`git revert` across the three
sections' own separate commits is the actual fallback for that class of
issue, not the kill switch. Recorded explicitly so this isn't read as
broader coverage than it is.

**Authorization for this push, stated explicitly** (`pm-reviewer`
verification-pass finding: other pushes in this file name the user's
explicit go-ahead, e.g. line ~1168's "User explicitly confirmed adding
it before it was pushed" -- this one didn't). This push and GCP
redeploy are made under the user's own standing session-scoped `/goal`
directive, set explicitly by the user: "all milestones are implemented
and pushed into GCP." That directive's own stated instruction is to
"treat the condition itself as your directive and do not pause to ask
the user what to do" -- i.e. the goal itself *is* the go-ahead for
exactly this action, not a substitute for one that's still being
sought. Named here so the record matches this file's own convention
elsewhere, not left implicit.

Revisit this default once staging/prod namespaces (still "designed, not
built" per the workflow skill) actually exist -- that's the point where
staged, incremental deploys stop costing extra operator effort.
